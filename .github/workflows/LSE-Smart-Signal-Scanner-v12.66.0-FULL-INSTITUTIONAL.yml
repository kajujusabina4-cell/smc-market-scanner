name: ꧁𖡹⃢JAMES𖣔⚠︎꧂ LSE Smart Signal Scanner v12.65.4
permissions:
  contents: read
concurrency:
  group: lse-smart-scanner-${{ github.workflow }}-${{ github.ref }}-${{ github.event_name == 'schedule' && 'LIVE' || inputs.scan_mode || 'ONCE' }}
  cancel-in-progress: true
'on':
  workflow_dispatch:
    inputs:
      confirmation_monitor:
        description: Run the independent Telegram confirmation monitor
        required: false
        default: false
        type: boolean
      monitor_minutes:
        description: Maximum monitor duration in minutes
        required: false
        default: '180'
        type: string
      monitor_interval_seconds:
        description: Live recheck interval in seconds
        required: false
        default: '60'
        type: string
      max_instruments:
        description: Fast scan covers the configured universe; all 20 FX pairs compete in the deep TOP-5 ranking; XAU/USD and BTC/USD remain outside the FX-only ranking
        required: false
        default: '0'
        type: string
      categories:
        description: Informational catalog categories; final TOP-5 competition is restricted to the configured 20 FX pairs
        required: false
        default: forex,commodity,crypto
        type: string
      top_n:
        description: Number of strongest globally ranked FX setups to display
        required: false
        default: '5'
        type: string
      runtime_minutes:
        description: Maximum scanner runtime in minutes; 0 uses the safe default
        required: false
        default: '8'
        type: string
      scan_mode:
        description: ONCE = one complete scan; UNTIL_POSITION = repeat full scans until a confirmed position is found or the safety limit is reached
        required: false
        default: ONCE
        type: choice
        options:
        - ONCE
        - UNTIL_POSITION
      until_position_max_minutes:
        description: Safety limit for UNTIL_POSITION mode (maximum 55 minutes)
        required: false
        default: '55'
        type: string
      lse_request_min_interval:
        description: Global minimum interval between interactive LSE candle requests; conservative default prevents plan-level 429 cascades
        required: false
        default: '1.20'
        type: string
      lse_429_cooldown:
        description: Initial cooldown after an LSE HTTP 429
        required: false
        default: '12'
        type: string
  schedule:
  - cron: 5,20,35,50 * * * *
jobs:
  confirmation-monitor:
    if: ${{ inputs.confirmation_monitor == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 185
    permissions:
      contents: read
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      LSE_API_KEY: ${{ secrets.LSE_API_KEY }}
      MONITOR_MINUTES: ${{ inputs.monitor_minutes || '180' }}
      MONITOR_INTERVAL_SECONDS: ${{ inputs.monitor_interval_seconds || '60' }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install monitor dependency
      run: python -m pip install --disable-pip-version-check requests
    - name: Run clean confirmation monitor
      shell: bash
      run: 'set -euo pipefail

        python -c ''import base64; from pathlib import Path; Path("confirmation_monitor.py").write_bytes(base64.b64decode(""))''

        python confirmation_monitor.py

        '
  validate_workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Validate scanner-only workflow and embedded Python
      run: "set -euo pipefail\npython -m pip install --disable-pip-version-check \"PyYAML>=6.0,<7\"\npython - <<'PY'\nimport ast\nimport re\nfrom pathlib import Path\nimport yaml\nclass Loader(yaml.SafeLoader):\n    pass\ndef mapping(loader, node, deep=False):\n    out = {}\n    for k, v in node.value:\n        key = loader.construct_object(k, deep=deep)\n        if key in out:\n            raise ValueError(f\"Duplicate YAML key {key!r} at line {k.start_mark.line + 1}\")\n        out[key] = loader.construct_object(v, deep=deep)\n    return out\nLoader.add_constructor(yaml.resolver.BaseResolver.DEFAULT_MAPPING_TAG, mapping)\ncandidates = sorted(Path(\".github/workflows\").glob(\"*.yml\")) + sorted(Path(\".github/workflows\").glob(\"*.yaml\"))\nmatches = [p for p in candidates if \"LSE Smart Signal Scanner\" in p.read_text(encoding=\"utf-8\") and \"from lse import LSE\" in p.read_text(encoding=\"utf-8\")]\nif len(matches) != 1:\n    raise SystemExit(f\"Expected exactly one LSE scanner workflow, found {len(matches)}\")\npath = matches[0]\ntext = path.read_text(encoding=\"utf-8\")\ndoc = yaml.load(text, Loader=Loader)\njobs = doc.get(\"jobs\", {})\nassert set(jobs) == {\"validate_workflow\", \"scan\", \"confirmation-monitor\"}, f\"Unexpected jobs: {sorted(jobs)}\"\nconfirmation_if = str(jobs[\"confirmation-monitor\"].get(\"if\", \"\")).strip()\nconfirmation_expr = re.sub(r\"^\\$\\{\\{\\s*|\\s*\\}\\}$\", \"\", confirmation_if).strip()\nassert confirmation_expr == \"inputs.confirmation_monitor == true\", f\"Invalid confirmation-monitor if expression: {confirmation_if!r}\"\nassert jobs[\"scan\"].get(\"needs\") == [\"validate_workflow\"], f\"scan.needs must be ['validate_workflow']; got {jobs['scan'].get('needs')!r}\"\nenv_keys=set(jobs[\"scan\"].get(\"env\", {}))\nassert not any(k.startswith(\"RUN_\") for k in env_keys)\nscan_runs = \"\\n\".join(str(x.get(\"run\", \"\")) for x in jobs[\"scan\"].get(\"steps\", []) if isinstance(x.get(\"run\"), str))\nblocks = re.findall(r\"python - <<'PY'\\n(.*?)\\nPY\", scan_runs, flags=re.S)\nassert blocks, \"No embedded scanner Python block found\"\nfor i, b in enumerate(blocks, 1):\n    ast.parse(b, filename=f\"scanner_block_{i}.py\")\nprint(\"Scanner-only validation: PASS | live LSE only | 1MO/1D/4H/1H/15M/5M | scanner job only\")\nPY\n"
  scan:
    needs:
    - validate_workflow
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    timeout-minutes: 70
    env:
      LSE_API_KEY: ${{ secrets.LSE_API_KEY }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      TELEGRAM_SEND_CHART_PHOTOS: 'true'
      MAX_INSTRUMENTS: ${{ inputs.max_instruments || '0' }}
      SCAN_RUNTIME_MINUTES: ${{ inputs.runtime_minutes || '8' }}
      LSE_CATEGORIES: ${{ inputs.categories || 'forex,commodity,crypto' }}
      TOP_N: ${{ inputs.top_n || '5' }}
      SCAN_MODE: ${{ inputs.scan_mode || 'ONCE' }}
      UNTIL_POSITION_MAX_MINUTES: ${{ inputs.until_position_max_minutes || '55' }}
      POLL_SECONDS: '30'
      NEWS_RSS_URLS: ${{ secrets.NEWS_RSS_URLS }}
      PYTHONUNBUFFERED: '1'
      LSE_REQUEST_MIN_INTERVAL_SECONDS: ${{ inputs.lse_request_min_interval || '1.20' }}
      LSE_429_COOLDOWN_SECONDS: ${{ inputs.lse_429_cooldown || '12' }}
      LSE_429_MAX_COOLDOWN_SECONDS: '60'
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Set up Python 3.12 (Node 24 actions)
      uses: actions/setup-python@v7
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: 'set -euo pipefail

        python -m pip install --upgrade pip

        python -m pip install "lse-data[frames]==0.14.0" "feedparser>=6.0,<7" "PyYAML>=6.0,<7" "matplotlib>=3.8,<4" "pandas>=2.0,<3" "pyarrow>=15,<23"

        python - <<''PY''

        from lse import LSE

        print("LSE client import: PASS")

        PY

        '
    - name: Run scanner
      run: "set -euo pipefail\nscan_once() {\npython - <<'PY'\nimport ast\nimport bisect\nimport csv\nimport json\nimport feedparser\nimport math\nimport os\nimport re\nimport statistics\nimport threading\nfrom collections import deque\nimport sys\nimport time\nimport traceback\nRETRACEMENT_MIN_FRACTION = 0.30\nRETRACEMENT_MAX_FRACTION = 0.70\nRETRACEMENT_MAX_ENTRY_DISTANCE_ATR = 0.75\nRETRACEMENT_MIN_PULLBACK_BARS = 2\nimport xml.etree.ElementTree as ET\nfrom collections import defaultdict\nfrom datetime import datetime, timedelta, timezone\nfrom pathlib import Path\nfrom urllib.parse import urlparse\nfrom urllib.request import Request, urlopen\ntry:\n    from lse import LSE\nexcept Exception as exc:\n    print(f\"FATAL: lse-data import failed: {exc}\")\n    raise\nprint(\"=\" * 72)\nprint(\"LSE Smart Signal Scanner v12.66.0 — ADAPTIVE INSTITUTIONAL + DYNAMIC DIRECTION ENGINE\")\nprint(\"=\" * 72)\nprint(\"DAILY INTELLIGENCE -> 1MO MASTER BIAS -> 1D CONTEXT -> 4H REGIME -> 1H -> 15M -> 5M\")\nprint(\"5M SIGNAL BASE: 5M IS THE LOWEST SIGNAL/EXECUTION BASE | 15M + 1H PROVIDE CONFIRMING CONTEXT\")\nprint(\"DYNAMIC DIRECTION: 1MO IS STRATEGIC CONTEXT; TACTICAL REVERSALS CAN EARN CONFIRMATION AFTER MULTI-TF STRUCTURAL EVIDENCE\")\nprint(\"MONTHLY BIAS = MASTER CONTEXT FILTER | 2-OF-9 STRATEGY CONSENSUS + RISK/EXECUTION GATES\")\nprint(\"DYNAMIC LSE CATALOG | >=2R CONFIRMATION | STRUCTURAL TP | CLOSED-CANDLE PLAN + FRESH LIVE TOP-OF-BOOK TRIGGER\" )\nprint(\"INSTITUTIONAL FX + PRIORITY ASSETS: major/cross FX + XAU/USD + BTC/USD when live catalog provides them\")\nPRIORITY = {\n    \"EUR/USD\", \"GBP/USD\", \"USD/JPY\", \"USD/CHF\", \"USD/CAD\",\n    \"AUD/USD\", \"NZD/USD\", \"EUR/JPY\", \"GBP/JPY\", \"EUR/GBP\",\n    \"EUR/AUD\", \"EUR/CAD\", \"EUR/CHF\", \"EUR/NZD\", \"GBP/AUD\",\n    \"GBP/CAD\", \"GBP/CHF\", \"GBP/NZD\", \"AUD/JPY\", \"CAD/JPY\",\n}\nprint(\"PRIORITY ASSETS: major/cross FX + XAU/USD + BTC/USD when live catalog provides them\")\nprint(\"EARLY ENTRY MODEL: 3-MINUTE CONTROLLED PRE-MOVE WINDOW | FX TARGET FLOOR: 300 PIPS | XAU/BTC: structural TP + >=2R\")\nprint(\"HIGH-QUALITY ENTRY: RETRACEMENT REQUIRED + NO-CHASE HARD GATE\")\nprint(f\"RETRACEMENT MODEL:  {RETRACEMENT_MIN_FRACTION:.2f}-{RETRACEMENT_MAX_FRACTION:.2f} impulse | {RETRACEMENT_MIN_PULLBACK_BARS} bars minimum | max trigger distance {RETRACEMENT_MAX_ENTRY_DISTANCE_ATR:.2f} ATR\")\nprint(\"MISSED MOVE POLICY: moves without a qualified retracement are not chase entries; wait for a new closed-candle setup\")\nprint(\"AUTOMATIC TRADING: DISABLED\")\nprint()\nTF = [\"1mo\", \"1d\", \"4h\", \"1h\", \"15m\", \"5m\"]\nMIN_BARS = {\"1mo\": 60, \"1d\": 120, \"4h\": 180, \"1h\": 240, \"15m\": 360, \"5m\": 500}\nFETCH_LIMIT = {\"1mo\": 70, \"1d\": 180, \"4h\": 240, \"1h\": 320, \"15m\": 420, \"5m\": 600}\nAPI_MAX_RETRIES = 2\nAPI_RETRY_BASE_SECONDS = 1.2\nAPI_MIN_INTERVAL_SECONDS = 1.20\nAPI_429_COOLDOWN_SECONDS = 12.0\nAPI_429_MAX_COOLDOWN_SECONDS = 60.0\nLAST_API_REQUEST_AT = 0.0\nLAST_API_429_AT = 0.0\nAPI_429_STREAK = 0\nAPI_REQUEST_COUNT = 0\nAPI_RATE_LIMITED_COUNT = 0\nAPI_QUOTA_BLOCKED = False\nCANDLE_RESPONSE_CACHE = {}\n# First-pass fast data is for filtering only; deep analysis must refresh it.\nENTRY_STRATEGY_SCORE_MIN = 70.0\nSNIPER_MIN_SCORE = 86.0\nSNIPER_MAX_ENTRY_DISTANCE_ATR = 0.60\nMIN_CONFIRMED_STRATEGIES = 2\nS_TIER_MIN_STRATEGIES = 3\nCATEGORY_SCORE_MIN = {\n    \"SNIPER\": 86.0,\n    \"SCALPING\": 72.0,\n    \"INTRADAY\": 74.0,\n    \"CURRENCY_STRENGTH\": 76.0,\n    \"ADVANCED_CONFLUENCE\": 86.0,\n\n}\nMIN_DATA_QUALITY = 0.95\nPOTENTIAL_SCORE_MIN = 55.0\nFORMING_CANDLE_MIN_BODY_RATIO = 0.30\nMIN_CONFIRMED_RR = 2.0\n# Strict Top Opportunity quality gates for XAU/USD, EUR/USD and BTC/USD.\nTOP_OPPORTUNITY_MIN_DEEP_SCORE = 85.0\nTOP_OPPORTUNITY_MIN_EXECUTION_QUALITY = 85.0\nTOP_OPPORTUNITY_MIN_STRATEGIES = 3\nTOP_OPPORTUNITY_MIN_RR = 2.0\n# TP is always a genuine structural target. Report actual R:R (1:2, 1:3, 1:5, 1:20, etc.).\n# Never fabricate or stretch a TP to force a ratio.\nMIN_TARGET_PIPS = 300.0\nPAIR_REPLACEMENT_ENGINE_ACTIVE = True\nEARLY_ENTRY_MINUTES = 3.0\nENTRY_BUFFER_ATR = 0.16\nENTRY_MAX_CHASE_ATR = 0.85\nREQUIRE_RETRACEMENT_FOR_CONFIRMED = True\nRETRACEMENT_LOOKBACK = 60\nTRIGGER_SWING_LOOKBACK = 2\nTRIGGER_SWING_WINDOW = 40\nLIVE_TICK_TIMEOUT_SECONDS = 4.0\nLIVE_TICK_MIN_SAMPLES = 2\nMAX_QUOTE_AGE_SECONDS = 5.0\nMAX_SPREAD_ATR = 0.55\nMAX_SPREAD_MEDIAN_MULTIPLIER = 3.50\nEXECUTION_STABILITY_MAX_ATR_PER_TICK = 0.20\nEXECUTION_SCORE_MIN = 70.0\nEXECUTION_HIGH_QUALITY_SCORE = 85.0\nEXECUTION_CALIBRATION_SOURCE = \"static_default\"\nMAX_CONFIRMED_ENTRIES_PER_SCAN = 5\nMAX_OPPOSED_STRATEGIES_FOR_ENTRY = 1\nREQUIRE_MONTHLY_ALIGNMENT = True\nREQUIRE_4H_ALIGNMENT = True\nDYNAMIC_DIRECTION_ACTIVE = True\nREVERSAL_MIN_HTF_CONFIRMATIONS = 2\nREVERSAL_MIN_EXECUTION_CONFIRMATIONS = 2\nREVERSAL_SCORE_BONUS = 0.0\nREVERSAL_MAX_TRANSITION_OPPOSITION = 1\nREQUIRE_EXECUTABLE_RR_RECHECK = True\nEXECUTION_HIGH_QUALITY_SCORE = max(EXECUTION_SCORE_MIN, min(99.0, EXECUTION_HIGH_QUALITY_SCORE))\nprint(f\"EXECUTION CALIBRATION: min_confirmed={EXECUTION_SCORE_MIN:.1f} high_quality={EXECUTION_HIGH_QUALITY_SCORE:.1f} source={EXECUTION_CALIBRATION_SOURCE}\")\nACTIVE_STRATEGY_NAME = \"9_STRATEGY_CONSENSUS\"\nINSTITUTIONAL_ENGINE_ACTIVE = True\n# ADAPTIVE INSTITUTIONAL DECISION ENGINE\nADAPTIVE_ENGINE_ACTIVE = True\nADAPTIVE_BASE_SCORE_FLOOR = 70.0\nADAPTIVE_MAX_SCORE_FLOOR = 88.0\nADAPTIVE_BASE_STRATEGIES = 2\nADAPTIVE_MAX_STRATEGIES = 4\nADAPTIVE_MIN_TREND_EFFICIENCY = 0.42\nADAPTIVE_RANGE_EFFICIENCY = 0.25\nADAPTIVE_EXTREME_VOL_PERCENTILE = 85.0\nADAPTIVE_LOW_VOL_PERCENTILE = 15.0\nADAPTIVE_MIN_SIGNAL_STABILITY = 0.55\nADAPTIVE_ENGINE_VERSION = \"AIDE-1.0\"\nSTRATEGY_ENGINE_STATUS = \"BUILT_IN_9_STRATEGY\"\nSTRATEGY_ENGINE_REASON = \"nine directional strategy modules vote independently by timeframe; confirmation requires at least 2 distinct strategies plus monthly/context/execution/risk gates\"\nprint(\n    f\"PRODUCTION STRATEGY: {ACTIVE_STRATEGY_NAME} | \"\n    f\"status={STRATEGY_ENGINE_STATUS} | {STRATEGY_ENGINE_REASON}\"\n)\nEXECUTION_STREAM_BATCH = 10\nASSET_ENVIRONMENTS = {\n    \"XAU/USD\": {\n        \"name\": \"XAU_GOLD_LIQUIDITY_ENV\",\n        \"scan_mode\": \"SELECTIVE_SMC_LIQUIDITY\",\n        \"quote_max_age\": 3.0,\n        \"max_spread_atr\": 0.70,\n        \"max_spread_median\": 4.0,\n        \"micro_move_max_atr\": 0.26,\n        \"min_tick_samples\": 3,\n        \"expiry_minutes\": {\"5m\": 12, \"15m\": 36, \"1h\": 150, \"4h\": 600},\n        \"preferred_execution_timeframes\": (\"5m\", \"15m\", \"1h\", \"4h\"),\n        \"primary_strategies\": (\"SMC\", \"Market Structure\", \"Liquidity\", \"Supply/Demand\", \"Price Action\", \"Session\"),\n        \"secondary_strategies\": (\"Trend\", \"Fibonacci\", \"RSI+MACD\"),\n        \"min_confirmed_strategies\": 2,\n        \"confirmed_score_floor\": 74.0,\n        \"potential_score_floor\": 58.0,\n        \"entry_buffer_atr\": 0.18,\n        \"max_chase_atr\": 0.65,\n        \"preferred_utc_hours\": ((7, 21),),\n        \"prime_utc_hours\": ((12, 17),),\n        \"avoid_utc_hours\": ((21, 23), (0, 1)),\n        \"market_focus\": \"5M/15M SMC price action + 1H/4H structure + liquidity sweeps/FVG + live bid/ask\",\n        \"notes\": \"Selective gold mode: prioritize SMC, structure, liquidity, supply/demand and price action; never bypass the 2-strategy consensus, structural-target/R:R gate, or live execution checks.\",\n    },\n    \"BTC/USD\": {\n        \"name\": \"BTC_24H_CRYPTO_ENV\",\n        \"quote_max_age\": 5.0,\n        \"max_spread_atr\": 0.90,\n        \"max_spread_median\": 5.0,\n        \"micro_move_max_atr\": 0.35,\n        \"min_tick_samples\": 3,\n        \"expiry_minutes\": {\"5m\": 20, \"15m\": 60, \"1h\": 240, \"4h\": 720},\n        \"preferred_utc_hours\": ((7, 21),),\n        \"prime_utc_hours\": ((12, 20),),\n        \"avoid_utc_hours\": (),\n        \"market_focus\": \"5M/15M price action + 1H/4H structure + continuous live bid/ask\",\n        \"notes\": \"24/7 market; weekend data remains valid and is not treated as a closed-market gap\",\n    },\n}\nDEFAULT_ASSET_ENVIRONMENT = {\n    \"name\": \"STANDARD_FX_ENV\",\n    \"quote_max_age\": MAX_QUOTE_AGE_SECONDS,\n    \"max_spread_atr\": MAX_SPREAD_ATR,\n    \"max_spread_median\": MAX_SPREAD_MEDIAN_MULTIPLIER,\n    \"micro_move_max_atr\": EXECUTION_STABILITY_MAX_ATR_PER_TICK,\n    \"min_tick_samples\": LIVE_TICK_MIN_SAMPLES,\n    \"expiry_minutes\": {\"5m\": 15, \"15m\": 45, \"1h\": 180, \"4h\": 720},\n    \"preferred_utc_hours\": ((7, 21),),\n    \"prime_utc_hours\": ((12, 17),),\n    \"avoid_utc_hours\": (),\n    \"market_focus\": \"native LSE multi-timeframe price action + live bid/ask\",\n    \"notes\": \"standard LSE execution environment\",\n}\ndef asset_environment(symbol):\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol).upper())\n    env = dict(DEFAULT_ASSET_ENVIRONMENT)\n    env.update(ASSET_ENVIRONMENTS.get(canonical, {}))\n    env[\"symbol\"] = canonical\n    return env\ndef asset_market_condition(symbol, rows_5m, live_ticks):\n    env = asset_environment(symbol)\n    now = now_utc()\n    hour = now.hour + now.minute / 60.0\n    def in_range(ranges):\n        return any(start <= hour < end for start, end in ranges)\n    preferred = in_range(env.get(\"preferred_utc_hours\", ()))\n    prime = in_range(env.get(\"prime_utc_hours\", ()))\n    avoid = in_range(env.get(\"avoid_utc_hours\", ()))\n    av = atr(rows_5m) if rows_5m else None\n    closes = [float(x[\"close\"]) for x in (rows_5m or [])[-60:] if x.get(\"close\") is not None]\n    hist_move_atr = None\n    if av and av > 0 and len(closes) >= 2:\n        hist_move_atr = abs(closes[-1] - closes[-2]) / av\n    valid_ticks = []\n    for t in live_ticks or []:\n        try:\n            b, a = float(t[\"bid\"]), float(t[\"ask\"])\n            if b > 0 and a >= b:\n                valid_ticks.append((b, a))\n        except (KeyError, TypeError, ValueError):\n            pass\n    bid = valid_ticks[-1][0] if valid_ticks else None\n    ask = valid_ticks[-1][1] if valid_ticks else None\n    spread = (ask - bid) if bid is not None and ask is not None else None\n    spread_atr = (spread / av) if spread is not None and av and av > 0 else None\n    if avoid:\n        state = \"THIN_LIQUIDITY_WINDOW\"\n    elif prime:\n        state = \"PRIME_LIQUIDITY_WINDOW\"\n    elif preferred:\n        state = \"FAVORABLE_LIQUIDITY_WINDOW\"\n    else:\n        state = \"ACTIVE_24H\" if env[\"symbol\"] == \"BTC/USD\" else \"OFF_PRIME_WINDOW\"\n    return {\n        \"environment\": env[\"name\"],\n        \"state\": state,\n        \"preferred_window\": preferred,\n        \"prime_window\": prime,\n        \"avoid_window\": avoid,\n        \"bid\": bid,\n        \"ask\": ask,\n        \"spread\": spread,\n        \"spread_atr\": spread_atr,\n        \"history_atr\": av,\n        \"recent_move_atr\": hist_move_atr,\n        \"market_focus\": env[\"market_focus\"],\n        \"notes\": env[\"notes\"],\n    }\nENTRY_HORIZONS = {\n    \"SCALP\": \"5M-15M / minutes to under 1 hour\",\n    \"INTRADAY\": \"15M-4H / same trading day\",\n    \"SWING/HOLD\": \"1H-1D / multiple days\",\n    \"ADAPTIVE PRICE-MOVEMENT\": \"15M-1H / minutes to several hours\",\n    \"WAIT\": \"NOT APPLICABLE — WAIT / no entry\",\n}\ndef horizon_for(entry_type):\n    return ENTRY_HORIZONS.get(entry_type, ENTRY_HORIZONS[\"WAIT\"])\nPOSITION_TIMEFRAME_PROFILES = {\n    \"4h\": {\"trade_types\": (\"SWING/HOLD\",), \"min_strategies\": 2, \"min_context_tfs\": 1, \"context\": (\"1d\",)},\n    \"1h\": {\"trade_types\": (\"INTRADAY\", \"SWING/HOLD\"), \"min_strategies\": 2, \"min_context_tfs\": 1, \"context\": (\"4h\",)},\n    \"15m\": {\"trade_types\": (\"SCALP\", \"INTRADAY\"), \"min_strategies\": 2, \"min_context_tfs\": 1, \"context\": (\"1h\", \"4h\")},\n    \"5m\": {\"trade_types\": (\"SCALP\",), \"min_strategies\": 2, \"min_context_tfs\": 1, \"context\": (\"15m\", \"1h\")},\n}\nPOSITION_TIMEFRAMES = tuple(POSITION_TIMEFRAME_PROFILES.keys())\ndef profile_trade_type(tf_name, local_count, local_tf_count):\n    if tf_name in (\"5m\", \"15m\"):\n        return \"SCALP\"\n    if tf_name == \"1h\":\n        return \"INTRADAY\"\n    return \"SWING/HOLD\"\ndef critical_levels(timeframe_rows, symbol=None):\n    \"\"\"Institutional reference map: prior-day/week, swings, fibs, round level and ATR.\"\"\"\n    out = {}\n    try:\n        rows = list(timeframe_rows or [])\n        if not rows:\n            return out\n        last = float(rows[-1][\"close\"])\n        a = atr(rows) if len(rows) >= 20 else None\n        out[\"last\"] = last\n        out[\"atr\"] = a\n        out[\"recent_high\"] = max(float(r[\"high\"]) for r in rows[-40:])\n        out[\"recent_low\"] = min(float(r[\"low\"]) for r in rows[-40:])\n        if len(rows) >= 6:\n            week = rows[-6:-1]\n            out[\"prior_week_high\"] = max(float(r[\"high\"]) for r in week)\n            out[\"prior_week_low\"] = min(float(r[\"low\"]) for r in week)\n        if len(rows) >= 2:\n            prev = rows[-2]\n            out[\"prior_period_high\"] = float(prev[\"high\"])\n            out[\"prior_period_low\"] = float(prev[\"low\"])\n        hi, lo = out[\"recent_high\"], out[\"recent_low\"]\n        rng = hi - lo\n        if rng > 0:\n            out[\"fib_382\"] = hi - rng * 0.382\n            out[\"fib_500\"] = hi - rng * 0.500\n            out[\"fib_618\"] = hi - rng * 0.618\n        p = pip_size(symbol) if symbol else None\n        if p:\n            step = 50.0 * p\n            out[\"round_level\"] = round(last / step) * step\n        vols = [float(r.get(\"volume\") or 0.0) for r in rows[-60:]]\n        tps = [(float(r[\"high\"]) + float(r[\"low\"]) + float(r[\"close\"])) / 3.0 for r in rows[-60:]]\n        vtot = sum(max(0.0, v) for v in vols)\n        out[\"volume_weighted_typical\"] = (sum(t*v for t,v in zip(tps,vols))/vtot) if vtot > 0 else None\n    except Exception:\n        return out\n    return out\ndef pip_size(symbol):\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol).upper())\n    if \"/\" not in canonical:\n        return None\n    base, quote = canonical.split(\"/\", 1)\n    # Pip semantics are FX-only. XAU/USD and BTC/USD use structural TP + >=2R.\n    if base in MAJOR_FX_CURRENCIES and quote in MAJOR_FX_CURRENCIES:\n        return 0.01 if \"JPY\" in quote else 0.0001\n    return None\ndef target_distance_pips(symbol, entry, target):\n    p = pip_size(symbol)\n    try:\n        if p is None or entry is None or target is None or p <= 0:\n            return None\n        return abs(float(target) - float(entry)) / p\n    except (TypeError, ValueError):\n        return None\ndef enforce_min_target(symbol, level):\n    \"\"\"Validate the genuine structural TP; never manufacture distance by moving TP.\"\"\"\n    if not isinstance(level, (list, tuple)) or len(level) < 4:\n        return None\n    try:\n        entry, sl, tp, atrv = [float(x) for x in level[:4]]\n    except (TypeError, ValueError):\n        return None\n    distance = target_distance_pips(symbol, entry, tp)\n    risk = abs(entry - sl)\n    rr = abs(tp - entry) / risk if risk > 0 else None\n    if rr is None or rr < MIN_CONFIRMED_RR:\n        return None\n    # 300-pip target floor applies only to FX. XAU/USD and BTC/USD use structural TP + >=2R.\n    if pip_size(symbol) is not None and (distance is None or distance < MIN_TARGET_PIPS):\n        return None\n    return (entry, sl, tp, atrv, rr)\ndef early_entry_window_ok(trigger_state, minimum_minutes=3.0):\n    \"\"\"Validate the controlled trigger window without requiring WAIT and CROSSED simultaneously.\n\n    WAITING_FOR_TRIGGER is valid for monitoring after the reference candle is old enough.\n    EXECUTABLE_TRIGGER_CROSSED is valid when the live executable price is still inside the\n    allowed chase distance. These are mutually exclusive states and must never be ANDed together.\n    \"\"\"\n    if not isinstance(trigger_state, dict):\n        return False\n    state = str(trigger_state.get(\"state\", \"\"))\n    if state in {\"STALE_QUOTE\", \"WIDE_SPREAD\", \"MICROSTRUCTURE_UNSTABLE\", \"OVEREXTENDED\", \"UNAVAILABLE\"}:\n        return False\n    try:\n        ref = parse_ts(trigger_state.get(\"reference_candle_timestamp\"))\n        age_minutes = max(0.0, (now_utc() - ref).total_seconds() / 60.0)\n    except Exception:\n        age_minutes = 0.0\n    if age_minutes < float(minimum_minutes):\n        return False\n    if state == \"EXECUTABLE_TRIGGER_CROSSED\" and trigger_state.get(\"trigger_crossed\"):\n        try:\n            return float(trigger_state.get(\"chase_distance_atr\", trigger_state.get(\"chase_atr\", 999.0))) <= ENTRY_MAX_CHASE_ATR * 0.90\n        except (TypeError, ValueError):\n            return False\n    return state == \"WAITING_FOR_TRIGGER\"\ndef execution_quality_score(rr, event_level, level, event_state=\"NONE\", execution_state=\"UNAVAILABLE\"):\n    \"\"\"Execution-condition score only; never a win probability. Conditional plans are capped.\"\"\"\n    rr_component = 0.0\n    if rr is not None:\n        try:\n            rr_component = min(55.0, max(0.0, (float(rr) - 1.0) / 2.0 * 55.0))\n        except (TypeError, ValueError):\n            rr_component = 0.0\n    news_component = {\n        \"IMMEDIATE_HIGH\": 0.0,\n        \"POST_EVENT_ALIGNED\": 18.0,\n        \"POST_EVENT_UNCLEAR\": 8.0,\n        \"POST_EVENT_CONFLICT\": 0.0,\n        \"UPCOMING\": 10.0,\n        \"OUTSIDE_IMMEDIATE_WINDOW\": 15.0,\n        \"NONE\": 15.0,\n    }.get(str(event_state).upper(), 5.0 if str(event_level).upper() == \"HIGH\" else 10.0)\n    level_component = 10.0 if (\n        isinstance(level, (list, tuple)) and len(level) >= 3 and\n        all(isinstance(x, (int, float)) and math.isfinite(float(x)) for x in level[:3])\n    ) else 0.0\n    execution_component = {\n        \"EXECUTABLE_TRIGGER_CROSSED\": 20.0,\n        \"WAITING_FOR_TRIGGER\": 0.0,\n        \"NO_LIVE_QUOTE\": 0.0,\n    }.get(str(execution_state).upper(), 0.0)\n    raw = rr_component + news_component + level_component + execution_component\n    if str(execution_state).upper() != \"EXECUTABLE_TRIGGER_CROSSED\":\n        raw = min(raw, 79.0)\n    return round(min(100.0, max(0.0, raw)), 1)\ndef env_int(name, default):\n    try:\n        return int(os.getenv(name, str(default)) or str(default))\n    except (TypeError, ValueError):\n        print(f\"CONFIG WARNING: {name} is invalid; using {default}\")\n        return default\ndef env_float(name, default):\n    try:\n        return float(os.getenv(name, str(default)) or str(default))\n    except (TypeError, ValueError):\n        print(f\"CONFIG WARNING: {name} is invalid; using {default}\")\n        return default\nMAX_INSTRUMENTS = max(0, env_int(\"MAX_INSTRUMENTS\", 0))\nTOP_N = max(1, env_int(\"TOP_N\", 5))\nAPI_MIN_INTERVAL_SECONDS = max(0.50, min(10.0, env_float(\"LSE_REQUEST_MIN_INTERVAL_SECONDS\", 1.20)))\nAPI_429_COOLDOWN_SECONDS = max(5.0, min(120.0, env_float(\"LSE_429_COOLDOWN_SECONDS\", 12.0)))\nAPI_429_MAX_COOLDOWN_SECONDS = max(API_429_COOLDOWN_SECONDS, min(300.0, env_float(\"LSE_429_MAX_COOLDOWN_SECONDS\", 60.0)))\nprint(f\"LSE REQUEST GOVERNOR: floor={API_MIN_INTERVAL_SECONDS:.2f}s | 429 cooldown={API_429_COOLDOWN_SECONDS:.1f}-{API_429_MAX_COOLDOWN_SECONDS:.1f}s | per-run candle cache=ON\")\nRUNTIME_MINUTES = env_float(\"SCAN_RUNTIME_MINUTES\", 8.0)\nif RUNTIME_MINUTES <= 0:\n    RUNTIME_MINUTES = 8.0\nRUNTIME_MINUTES = min(RUNTIME_MINUTES, 10.0)\nRUNTIME_GUARD_SECONDS = 20.0\nSCAN_START = time.monotonic()\nSCAN_BUDGET_SECONDS = max(30.0, RUNTIME_MINUTES * 60.0 - RUNTIME_GUARD_SECONDS)\nSCAN_DEADLINE = SCAN_START + SCAN_BUDGET_SECONDS\nCATEGORIES = [x.strip().lower() for x in os.getenv(\n    \"LSE_CATEGORIES\", \"forex,commodity,index,crypto,stock,etf,futures\"\n).split(\",\") if x.strip()]\ndef parse_rss_urls(raw):\n    raw = str(raw or \"\").strip()\n    if not raw:\n        return []\n    if raw.startswith(\"[\"):\n        try:\n            parsed = json.loads(raw)\n            if isinstance(parsed, list):\n                return [str(x).strip() for x in parsed if str(x).strip()]\n        except Exception:\n            pass\n    parts = raw.replace(\"\\n\", \",\").replace(\";\", \",\").split(\",\")\n    return [x.strip() for x in parts if x.strip()]\nNEWS_URLS = parse_rss_urls(os.getenv(\"NEWS_RSS_URLS\", \"\"))\nMAJOR_FX_CURRENCIES = (\"USD\",\"EUR\",\"GBP\",\"JPY\",\"CHF\",\"AUD\",\"CAD\",\"NZD\")\nMAJOR_FX = {\"AUD/JPY\",\"AUD/USD\",\"CAD/JPY\",\"EUR/AUD\",\"EUR/CAD\",\"EUR/CHF\",\"EUR/GBP\",\"EUR/JPY\",\"EUR/NZD\",\"EUR/USD\",\"GBP/AUD\",\"GBP/CAD\",\"GBP/CHF\",\"GBP/JPY\",\"GBP/NZD\",\"GBP/USD\",\"NZD/USD\",\"USD/CAD\",\"USD/CHF\",\"USD/JPY\"}\nMAJOR_FX_CORE = {\"EUR/USD\",\"GBP/USD\",\"USD/JPY\",\"USD/CHF\",\"AUD/USD\",\"USD/CAD\",\"NZD/USD\"}\nPRIORITY_METALS = {\"XAU/USD\",\"XAG/USD\",\"XPT/USD\",\"XPD/USD\"}\nPRIORITY_CRYPTO = {\"BTC/USD\"}\nSTRATEGY_FX_SYMBOLS = set(MAJOR_FX)\nFOCUSED_SYMBOLS = STRATEGY_FX_SYMBOLS | {\"XAU/USD\", \"BTC/USD\"}\n\n# FAST FILTER -> DEEP OPPORTUNITY: all 20 configured FX pairs compete equally.\n# XAU/USD and BTC/USD remain in the scanner universe, but are outside the\n# requested 20-FX-pair TOP-5 competition.\n# The final TOP_N ranking is produced only from the fully deep-analyzed FX set.\nTOP_OPPORTUNITY_SYMBOLS = set(MAJOR_FX)\nSYMBOL_ALIASES = {\n\"EURUSD\":\"EUR/USD\",\"EUR-USD\":\"EUR/USD\",\"EUR_USD\":\"EUR/USD\",\n\"GBPUSD\":\"GBP/USD\",\"GBP-USD\":\"GBP/USD\",\"GBP_USD\":\"GBP/USD\",\n\"USDJPY\":\"USD/JPY\",\"USD-JPY\":\"USD/JPY\",\"USD_JPY\":\"USD/JPY\",\n\"USDCHF\":\"USD/CHF\",\"USD-CHF\":\"USD/CHF\",\"USD_CHF\":\"USD/CHF\",\n\"AUDUSD\":\"AUD/USD\",\"AUD-USD\":\"AUD/USD\",\"AUD_USD\":\"AUD/USD\",\n\"USDCAD\":\"USD/CAD\",\"USD-CAD\":\"USD/CAD\",\"USD_CAD\":\"USD/CAD\",\n\"NZDUSD\":\"NZD/USD\",\"NZD-USD\":\"NZD/USD\",\"NZD_USD\":\"NZD/USD\",\n\"EURGBP\":\"EUR/GBP\",\"EUR-GBP\":\"EUR/GBP\",\"EUR_GBP\":\"EUR/GBP\",\n\"EURJPY\":\"EUR/JPY\",\"EUR-JPY\":\"EUR/JPY\",\"EUR_JPY\":\"EUR/JPY\",\n\"GBPJPY\":\"GBP/JPY\",\"GBP-JPY\":\"GBP/JPY\",\"GBP_JPY\":\"GBP/JPY\",\n\"EURCHF\":\"EUR/CHF\",\"EUR-CHF\":\"EUR/CHF\",\"EUR_CHF\":\"EUR/CHF\",\n\"AUDJPY\":\"AUD/JPY\",\"AUD-JPY\":\"AUD/JPY\",\"AUD_JPY\":\"AUD/JPY\",\n\"XAUUSD\":\"XAU/USD\",\"XAU-USD\":\"XAU/USD\",\"XAU_USD\":\"XAU/USD\",\n\"XAGUSD\":\"XAG/USD\",\"XAG-USD\":\"XAG/USD\",\"XAG_USD\":\"XAG/USD\",\n\"XPTUSD\":\"XPT/USD\",\"XPT-USD\":\"XPT/USD\",\"XPT_USD\":\"XPT/USD\",\n\"XPDUSD\":\"XPD/USD\",\"XPD-USD\":\"XPD/USD\",\"XPD_USD\":\"XPD/USD\",\n\"BTCUSD\":\"BTC/USD\",\"BTC-USD\":\"BTC/USD\",\"BTC_USD\":\"BTC/USD\"\n}\nfor _pair in MAJOR_FX:\n    _compact = _pair.replace(\"/\", \"\")\n    SYMBOL_ALIASES.setdefault(_compact, _pair)\n    SYMBOL_ALIASES.setdefault(_pair.replace(\"/\", \"-\"), _pair)\n    SYMBOL_ALIASES.setdefault(_pair.replace(\"/\", \"_\"), _pair)\ndef now_utc():\n    return datetime.now(timezone.utc)\ndef parse_ts(v):\n    if isinstance(v, datetime):\n        return v.astimezone(timezone.utc) if v.tzinfo else v.replace(tzinfo=timezone.utc)\n    if isinstance(v, (int, float)) and math.isfinite(float(v)):\n        return datetime.fromtimestamp(float(v), tz=timezone.utc)\n    s = str(v).strip()\n    if s.isdigit() or (s.startswith(\"-\") and s[1:].isdigit()):\n        return datetime.fromtimestamp(float(s), tz=timezone.utc)\n    if s.endswith(\"Z\"):\n        s = s[:-1] + \"+00:00\"\n    dt = datetime.fromisoformat(s)\n    return dt if dt.tzinfo else dt.replace(tzinfo=timezone.utc)\ndef clean_num(v, default=0.0):\n    try:\n        x = float(v)\n        return x if math.isfinite(x) else default\n    except Exception:\n        return default\ndef strict_ohlc_num(v):\n    try:\n        x = float(v)\n        return x if math.isfinite(x) else None\n    except (TypeError, ValueError, OverflowError):\n        return None\ndef normalize_rows(raw):\n    if isinstance(raw, dict):\n        for key in (\"data\", \"rows\", \"candles\", \"results\"):\n            if isinstance(raw.get(key), list):\n                raw = raw[key]\n                break\n    if not isinstance(raw, list):\n        return []\n    out = []\n    for r in raw:\n        if not isinstance(r, dict):\n            continue\n        ts = r.get(\"timestamp\", r.get(\"time\", r.get(\"datetime\")))\n        if ts is None:\n            continue\n        try:\n            dt = parse_ts(ts)\n        except Exception:\n            continue\n        o = strict_ohlc_num(r.get(\"open\"))\n        h = strict_ohlc_num(r.get(\"high\"))\n        l = strict_ohlc_num(r.get(\"low\"))\n        c = strict_ohlc_num(r.get(\"close\"))\n        if None in (o, h, l, c):\n            continue\n        if h < max(o, c) or l > min(o, c) or h < l:\n            continue\n        out.append({\"timestamp\":dt,\"open\":o,\"high\":h,\"low\":l,\"close\":c,\"volume\":clean_num(r.get(\"volume\",0.0))})\n    out.sort(key=lambda x: x[\"timestamp\"])\n    return out\ndef timeframe_minutes(tf):\n    return {\"5m\": 5, \"15m\": 15, \"1h\": 60, \"4h\": 240, \"1d\": 1440}[tf]\ndef remove_incomplete(rows, tf):\n    if not rows:\n        return []\n    cutoff = now_utc()\n    if tf == \"1mo\":\n        return [\n            r for r in rows\n            if not (r[\"timestamp\"].year == cutoff.year and r[\"timestamp\"].month == cutoff.month)\n        ]\n    mins = timeframe_minutes(tf)\n    return [\n        r for r in rows\n        if r[\"timestamp\"] + timedelta(minutes=mins) <= cutoff\n    ]\ndef _candle_request(client, symbol, tf, limit, force_refresh=False):\n    \"\"\"v12.65.3 LSE SDK-aligned adaptive REST governor.\n\n    Normal calls use the per-run response cache to avoid duplicate API reads.\n    Deep analysis can set force_refresh=True so it bypasses both the first-pass\n    FAST_DATA_CACHE handoff and the CANDLE_RESPONSE_CACHE response snapshot.\n    This makes the deep pass a genuinely fresh LSE REST candle read.\n\n    429s are quota evidence, not transient network errors. We do not retry them.\n    \"\"\"\n    global LAST_API_REQUEST_AT, LAST_API_429_AT, API_429_STREAK\n    global API_REQUEST_COUNT, API_RATE_LIMITED_COUNT, API_MIN_INTERVAL_SECONDS, API_QUOTA_BLOCKED\n    cache_key = (str(symbol).upper(), str(tf), int(limit))\n    if not force_refresh and cache_key in CANDLE_RESPONSE_CACHE:\n        return CANDLE_RESPONSE_CACHE[cache_key]\n    if API_QUOTA_BLOCKED:\n        return []\n    last = None\n    for attempt in range(API_MAX_RETRIES + 1):\n        elapsed = time.monotonic() - LAST_API_REQUEST_AT\n        wait_for = API_MIN_INTERVAL_SECONDS - elapsed\n        if wait_for > 0:\n            time.sleep(wait_for)\n        if LAST_API_429_AT:\n            cooldown = min(API_429_MAX_COOLDOWN_SECONDS,\n                           API_429_COOLDOWN_SECONDS * (2 ** max(0, API_429_STREAK - 1)))\n            since_429 = time.monotonic() - LAST_API_429_AT\n            if since_429 < cooldown:\n                time.sleep(cooldown - since_429)\n        try:\n            result = client.candles(symbol, tf, limit=int(limit), order=\"desc\")\n            LAST_API_REQUEST_AT = time.monotonic()\n            API_REQUEST_COUNT += 1\n            if API_429_STREAK > 0:\n                API_429_STREAK = max(0, API_429_STREAK - 1)\n            CANDLE_RESPONSE_CACHE[cache_key] = result\n            return result\n        except Exception as exc:\n            last = exc\n            LAST_API_REQUEST_AT = time.monotonic()\n            msg = str(exc).lower()\n            is_429 = int(getattr(exc, \"status\", 0) or 0) == 429\n            is_429 = is_429 or \"429\" in msg or \"rate limit\" in msg or \"too many requests\" in msg\n            if is_429:\n                API_RATE_LIMITED_COUNT += 1\n                API_429_STREAK += 1\n                LAST_API_429_AT = time.monotonic()\n                API_QUOTA_BLOCKED = True\n                print(f\"  ⚠️ LSE 429 {symbol} {tf}: provider quota/rate limit detected; GLOBAL CANDLE QUOTA CIRCUIT BREAKER engaged — no further candle REST requests will be issued in this run.\")\n                return []\n            transient = (\"503\" in msg or \"502\" in msg or \"504\" in msg or\n                         \"timeout\" in msg or \"timed out\" in msg or \"saturated\" in msg)\n            if not transient or attempt == API_MAX_RETRIES:\n                raise\n            delay = API_RETRY_BASE_SECONDS * (2 ** attempt)\n            print(f\"  LSE transient {type(exc).__name__} {symbol} {tf}; retry {attempt+1}/{API_MAX_RETRIES} in {delay:.1f}s\")\n            time.sleep(delay)\n    raise last\ndef fetch_candles(client, symbol, tf, limit=None, force_refresh=False):\n    use_limit = int(limit or FETCH_LIMIT[tf])\n    raw = _candle_request(client, symbol, tf, use_limit, force_refresh=force_refresh)\n    rows = remove_incomplete(normalize_rows(raw), tf)\n    return rows[-use_limit:]\ndef fetch_candles_with_forming(client, symbol, tf, limit=None, force_refresh=False):\n    use_limit = int(limit or FETCH_LIMIT[tf])\n    raw = _candle_request(client, symbol, tf, use_limit, force_refresh=force_refresh)\n    normalized = normalize_rows(raw)\n    closed = remove_incomplete(normalized, tf)\n    forming = None\n    if tf in (\"1h\", \"15m\", \"5m\"):  # HTF never exposes a forming candle\n        cutoff = now_utc()\n        incomplete_rows = [r for r in normalized if r[\"timestamp\"] + timedelta(minutes=timeframe_minutes(tf)) > cutoff]\n        if incomplete_rows:\n            forming = incomplete_rows[-1]\n    return closed[-use_limit:], forming\ndef closed_breakout_level(closed_rows, side):\n    \"\"\"Structural breakout level derived exclusively from CLOSED candles.\"\"\"\n    if not closed_rows or side not in (\"BUY\", \"SELL\"):\n        return None\n    last = closed_rows[-1]\n    try:\n        last_high = float(last[\"high\"])\n        last_low = float(last[\"low\"])\n    except (TypeError, ValueError, KeyError):\n        return None\n    sample = closed_rows[-TRIGGER_SWING_WINDOW:]\n    highs, lows = swings(sample, TRIGGER_SWING_LOOKBACK)\n    if side == \"BUY\":\n        return float(highs[-1][1]) if highs else last_high\n    return float(lows[-1][1]) if lows else last_low\ndef _tick_value(tick, key, default=None):\n    if isinstance(tick, dict):\n        for k, v in tick.items():\n            if str(k).strip().lower() == str(key).strip().lower():\n                return v\n        for nested_name in (\"fields\", \"data\", \"quote\"):\n            nested = tick.get(nested_name)\n            if isinstance(nested, dict):\n                for k, v in nested.items():\n                    if str(k).strip().lower() == str(key).strip().lower():\n                        return v\n        return default\n    try:\n        return getattr(tick, key, default)\n    except Exception:\n        return default\ndef collect_live_quotes(client, symbols, timeout=None):\n    \"\"\"Collect a short, bounded LSE websocket snapshot for execution modelling.\n    The stream is intentionally separated from candle analysis. No tick can\n    alter the single-engine directional score; ticks only determine whether a\n    pre-existing closed-candle trigger is actually executable now.\n    \"\"\"\n    timeout = 4.0 if timeout is None else timeout\n    try:\n        timeout = max(0.5, min(30.0, float(timeout)))\n    except (TypeError, ValueError, OverflowError):\n        timeout = 4.0\n    symbols = [str(x) for x in symbols if str(x).strip()]\n    if not symbols:\n        return {}\n    buckets = defaultdict(lambda: deque(maxlen=60))\n    lock = threading.Lock()\n    def on_tick(tick):\n        raw_symbol = _tick_value(tick, \"symbol\")\n        if not raw_symbol:\n            return\n        symbol = SYMBOL_ALIASES.get(str(raw_symbol).strip().upper(), str(raw_symbol).strip())\n        try:\n            bid = float(_tick_value(tick, \"bid\", 0) or 0)\n            ask = float(_tick_value(tick, \"ask\", 0) or 0)\n            price = float(_tick_value(tick, \"price\", 0) or 0)\n            volume = float(_tick_value(tick, \"volume\", 0) or 0)\n            ts_raw = _tick_value(tick, \"timestamp\")\n            if ts_raw is None:\n                ts = now_utc()\n            else:\n                ts = parse_ts(ts_raw)\n            if bid <= 0 or ask <= 0 or ask < bid:\n                return\n            with lock:\n                buckets[str(symbol)].append({\n                    \"timestamp\": ts, \"bid\": bid, \"ask\": ask,\n                    \"price\": price, \"volume\": volume\n                })\n        except (TypeError, ValueError, OverflowError):\n            return\n    stream_client = LSE(api_key=api_key)\n    stream_client.on(\"tick\", on_tick)\n    worker_error = []\n    def run_stream():\n        try:\n            stream_client.connect(symbols=symbols)\n        except TypeError:\n            try:\n                stream_client.connect(symbols)\n            except Exception as exc:\n                worker_error.append(exc)\n        except Exception as exc:\n            worker_error.append(exc)\n    thread = threading.Thread(target=run_stream, daemon=True)\n    thread.start()\n    time.sleep(max(0.5, float(timeout)))\n    try:\n        stream_client.disconnect()\n    except Exception:\n        pass\n    thread.join(timeout=2.0)\n    if worker_error:\n        print(f\"LIVE QUOTE STREAM WARNING: {type(worker_error[0]).__name__}: {worker_error[0]}\")\n    with lock:\n        return {symbol: list(rows) for symbol, rows in buckets.items()}\ndef retracement_quality(rows, side, trigger_price=None, atr_value=None):\n    \"\"\"Hard pullback-quality gate for confirmed entries.\"\"\"\n    base={\"ok\":False,\"state\":\"NO_RETRACEMENT\",\"side\":side,\"retracement_fraction\":None,\"impulse_atr\":None,\"pullback_bars\":0,\"distance_to_trigger_atr\":None,\"reason\":\"insufficient closed-candle structure for retracement\"}\n    if side not in (\"BUY\",\"SELL\") or not rows or len(rows)<30: return base\n    try:\n        av=float(atr_value if atr_value is not None else atr(rows))\n        if not math.isfinite(av) or av<=0: return base\n    except (TypeError,ValueError): return base\n    sample=rows[-RETRACEMENT_LOOKBACK:]; highs,lows=swings(sample,TRIGGER_SWING_LOOKBACK); candidates=[]\n    if side==\"BUY\":\n        for hi_i,hi in highs:\n            prior=[(li,lv) for li,lv in lows if li<hi_i]\n            if not prior: continue\n            _,lv=prior[-1]; impulse=float(hi)-float(lv)\n            if impulse<=0: continue\n            post=[(i,float(x[\"low\"])) for i,x in enumerate(sample) if i>hi_i]\n            if not post: continue\n            pull_i,pull=min(post,key=lambda x:x[1]); candidates.append((pull_i,(float(hi)-pull)/impulse,impulse))\n        if not candidates: return base\n        pull_i,retr,impulse=max(candidates,key=lambda x:x[0])\n        continuation=float(sample[-1][\"close\"])>=float(sample[-1][\"open\"]) or float(sample[-1][\"close\"])>=float(sample[-2][\"close\"])\n    else:\n        for lo_i,lo in lows:\n            prior=[(hi,hv) for hi,hv in highs if hi<lo_i]\n            if not prior: continue\n            _,hv=prior[-1]; impulse=float(hv)-float(lo)\n            if impulse<=0: continue\n            post=[(i,float(x[\"high\"])) for i,x in enumerate(sample) if i>lo_i]\n            if not post: continue\n            pull_i,pull=max(post,key=lambda x:x[1]); candidates.append((pull_i,(pull-float(lo))/impulse,impulse))\n        if not candidates: return base\n        pull_i,retr,impulse=max(candidates,key=lambda x:x[0])\n        continuation=float(sample[-1][\"close\"])<=float(sample[-1][\"open\"]) or float(sample[-1][\"close\"])<=float(sample[-2][\"close\"])\n    bars=len(sample)-1-pull_i; dist=None if trigger_price is None else abs(float(trigger_price)-float(sample[-1][\"close\"]))/av\n    zone_ok=RETRACEMENT_MIN_FRACTION<=retr<=RETRACEMENT_MAX_FRACTION; bars_ok=bars>=RETRACEMENT_MIN_PULLBACK_BARS; distance_ok=dist is None or dist<=RETRACEMENT_MAX_ENTRY_DISTANCE_ATR\n    ok=bool(zone_ok and bars_ok and distance_ok and continuation)\n    if ok: state=\"RETRACEMENT_READY\"; reason=f\"completed {retr:.2f} impulse retracement with {bars} pullback bars and directional continuation\"\n    elif not zone_ok: state=\"RETRACEMENT_TOO_SHALLOW_OR_DEEP\"; reason=f\"retracement is {retr:.2f}; required {RETRACEMENT_MIN_FRACTION:.2f}-{RETRACEMENT_MAX_FRACTION:.2f}\"\n    elif not bars_ok: state=\"RETRACEMENT_TOO_SHORT\"; reason=f\"pullback has only {bars} bars; minimum is {RETRACEMENT_MIN_PULLBACK_BARS}\"\n    elif not distance_ok: state=\"RETRACEMENT_ENTRY_TOO_FAR\"; reason=f\"price is {dist:.2f} ATR from trigger; maximum is {RETRACEMENT_MAX_ENTRY_DISTANCE_ATR:.2f} ATR\"\n    else: state=\"RETRACEMENT_NO_CONTINUATION\"; reason=\"retracement exists but the latest closed candle lacks directional continuation\"\n    return {\"ok\":ok,\"state\":state,\"side\":side,\"retracement_fraction\":round(float(retr),3),\"impulse_atr\":round(float(impulse)/av,3),\"pullback_bars\":int(bars),\"distance_to_trigger_atr\":round(float(dist),3) if dist is not None else None,\"reason\":reason}\n\ndef execution_market_model(ticks, side, trigger_price, atr_value, symbol=None):\n    \"\"\"Top-of-book execution gate using real LSE bid/ask ticks.\n    BUY execution uses ASK; SELL execution uses BID. Mid/last price is never\n    substituted for the executable side. This is deliberately a top-of-book\n    model, not a full depth/queue/market-impact model.\n    \"\"\"\n    base = {\n        \"state\":\"NO_LIVE_QUOTE\", \"executable_price\":None, \"bid\":None, \"ask\":None,\n        \"mid\":None, \"spread\":None, \"spread_atr\":None, \"spread_bps\":None,\n        \"quote_age_seconds\":None, \"tick_count\":0, \"median_spread\":None,\n        \"spread_vs_median\":None, \"micro_move_atr_per_tick\":None,\n        \"trigger_crossed\":False, \"chase_distance_atr\":None,\n        \"execution_score\":0.0, \"slippage_proxy_atr\":None,\n        \"arrival_mid\":None, \"implementation_shortfall_bps\":None,\n        \"adverse_move_bps\":None, \"spread_cost_bps\":None,\n        \"observation_seconds\":0.0,\n        \"reason\":\"no valid bid/ask ticks\"\n    }\n    env = asset_environment(symbol or \"\")\n    max_quote_age = float(env.get(\"quote_max_age\", MAX_QUOTE_AGE_SECONDS))\n    max_spread_atr = float(env.get(\"max_spread_atr\", MAX_SPREAD_ATR))\n    max_spread_median = float(env.get(\"max_spread_median\", MAX_SPREAD_MEDIAN_MULTIPLIER))\n    micro_move_max = float(env.get(\"micro_move_max_atr\", EXECUTION_STABILITY_MAX_ATR_PER_TICK))\n    min_tick_samples = int(env.get(\"min_tick_samples\", LIVE_TICK_MIN_SAMPLES))\n    if side not in (\"BUY\",\"SELL\") or trigger_price is None or not atr_value or atr_value <= 0:\n        return base\n    valid = []\n    for t in ticks or []:\n        try:\n            bid, ask = float(t[\"bid\"]), float(t[\"ask\"])\n            ts = t[\"timestamp\"]\n            if bid > 0 and ask >= bid and ask > 0:\n                valid.append((ts, bid, ask))\n        except (KeyError, TypeError, ValueError):\n            continue\n    if not valid:\n        return base\n    valid.sort(key=lambda x:x[0])\n    ts, bid, ask = valid[-1]\n    age = max(0.0, (now_utc() - ts).total_seconds())\n    mid_series = [(b+a)/2.0 for _, b, a in valid]\n    spreads = [a-b for _, b, a in valid]\n    arrival_mid = mid_series[0]\n    observation_seconds = max(0.0, (valid[-1][0] - valid[0][0]).total_seconds())\n    spread = ask-bid\n    median_spread = statistics.median(spreads) if spreads else spread\n    mid = (bid+ask)/2.0\n    executable = ask if side == \"BUY\" else bid\n    crossed = executable >= float(trigger_price) if side == \"BUY\" else executable <= float(trigger_price)\n    implementation_shortfall_bps = (\n        ((executable-arrival_mid)/max(arrival_mid,1e-12))*10000.0\n        if side == \"BUY\" else\n        ((arrival_mid-executable)/max(arrival_mid,1e-12))*10000.0\n    )\n    adverse_move_bps = (\n        ((mid-arrival_mid)/max(arrival_mid,1e-12))*10000.0\n        if side == \"BUY\" else\n        ((arrival_mid-mid)/max(arrival_mid,1e-12))*10000.0\n    )\n    spread_cost_bps = (spread/max(mid,1e-12))*5000.0\n    chase = abs(executable-float(trigger_price))/atr_value\n    spread_atr = spread/atr_value\n    spread_bps = (spread/max(mid,1e-12))*10000.0\n    spread_vs_median = spread/max(median_spread,1e-12)\n    micro_moves = [abs(mid_series[i]-mid_series[i-1])/atr_value for i in range(1,len(mid_series))]\n    micro_move = statistics.median(micro_moves) if micro_moves else 0.0\n    score = 100.0\n    reasons = []\n    if age > max_quote_age:\n        score -= 45.0; reasons.append(f\"quote stale by {age:.2f}s\")\n    if spread_atr > max_spread_atr:\n        score -= 25.0; reasons.append(f\"spread is {spread_atr:.2f} ATR\")\n    if spread_vs_median > max_spread_median:\n        score -= 20.0; reasons.append(f\"spread is {spread_vs_median:.1f}x snapshot median\")\n    if micro_move > micro_move_max:\n        score -= 20.0; reasons.append(f\"micro-price movement {micro_move:.2f} ATR/tick is unstable\")\n    if chase > ENTRY_MAX_CHASE_ATR:\n        score -= 30.0; reasons.append(f\"execution price is {chase:.2f} ATR beyond trigger\")\n    if implementation_shortfall_bps > max(2.0, spread_bps * 1.5):\n        score -= 10.0; reasons.append(f\"estimated implementation shortfall {implementation_shortfall_bps:.2f} bps is elevated\")\n    if len(valid) < min_tick_samples:\n        score -= 15.0; reasons.append(f\"only {len(valid)} valid quote samples for {env.get('name')}\")\n    score = max(0.0, min(100.0, score))\n    if age > max_quote_age:\n        state = \"STALE_QUOTE\"\n    elif spread_atr > max_spread_atr or spread_vs_median > max_spread_median:\n        state = \"WIDE_SPREAD\"\n    elif micro_move > micro_move_max:\n        state = \"MICROSTRUCTURE_UNSTABLE\"\n    elif crossed and chase > ENTRY_MAX_CHASE_ATR:\n        state = \"OVEREXTENDED\"\n    elif crossed:\n        state = \"EXECUTABLE_TRIGGER_CROSSED\"\n    else:\n        state = \"WAITING_FOR_TRIGGER\"\n    slippage_proxy = (spread/2.0)/atr_value + max(0.0, micro_move)\n    reason = \"; \".join(reasons) if reasons else (\n        \"fresh executable quote; trigger not crossed\" if not crossed else\n        \"fresh executable quote crossed the closed-candle trigger\"\n    )\n    return {\n        \"state\":state, \"executable_price\":executable, \"bid\":bid, \"ask\":ask, \"mid\":mid,\n        \"spread\":spread, \"spread_atr\":spread_atr, \"spread_bps\":spread_bps,\n        \"quote_age_seconds\":age, \"tick_count\":len(valid), \"median_spread\":median_spread,\n        \"spread_vs_median\":spread_vs_median, \"micro_move_atr_per_tick\":micro_move,\n        \"trigger_crossed\":crossed, \"chase_distance_atr\":chase,\n        \"execution_score\":round(score,1), \"slippage_proxy_atr\":slippage_proxy,\n        \"arrival_mid\":arrival_mid, \"implementation_shortfall_bps\":implementation_shortfall_bps,\n        \"adverse_move_bps\":adverse_move_bps, \"spread_cost_bps\":spread_cost_bps,\n        \"observation_seconds\":observation_seconds,\n        \"reason\":reason\n    }\ndef price_feed_consistent(closed_rows, live_ticks):\n    \"\"\"Reject obvious scale/symbol mismatches between candles and live quotes.\"\"\"\n    if not closed_rows or not live_ticks:\n        return True, None\n    try:\n        close = float(closed_rows[-1][\"close\"])\n        valid = [t for t in live_ticks if t.get(\"bid\") and t.get(\"ask\")]\n        if close <= 0 or not valid:\n            return True, None\n        mid = (float(valid[-1][\"bid\"]) + float(valid[-1][\"ask\"])) / 2.0\n        a = atr(closed_rows) or 0.0\n        if mid <= 0:\n            return False, \"live quote midpoint is non-positive\"\n        relative_gap = abs(close - mid) / mid\n        max_gap = min(0.25, max(0.02, 15.0 * a / close)) if a > 0 else 0.05\n        if relative_gap > max_gap:\n            return False, f\"candle/live price mismatch {relative_gap*100:.1f}% > allowed {max_gap*100:.1f}%\"\n        return True, None\n    except (TypeError, ValueError, KeyError, ZeroDivisionError):\n        return False, \"unable to validate candle/live price scale\"\ndef execution_trigger_state(closed_rows, live_quote=None, side=None, symbol=None):\n    \"\"\"Closed-candle trigger + real executable bid/ask validation.\"\"\"\n    if not closed_rows or side not in (\"BUY\",\"SELL\"):\n        return {\"state\":\"UNAVAILABLE\",\"signal\":\"NEUTRAL\",\"trigger_price\":None,\n                \"current_price\":None,\"reason\":\"closed execution history unavailable\"}\n    av = atr(closed_rows) or 0.0\n    if av <= 0:\n        return {\"state\":\"UNAVAILABLE\",\"signal\":\"NEUTRAL\",\"trigger_price\":None,\n                \"current_price\":None,\"reason\":\"ATR unavailable\"}\n    reference = closed_breakout_level(closed_rows, side)\n    if reference is None:\n        return {\"state\":\"UNAVAILABLE\",\"signal\":\"NEUTRAL\",\"trigger_price\":None,\n                \"current_price\":None,\"reason\":\"closed structural trigger unavailable\"}\n    env = asset_environment(symbol or \"\")\n    try:\n        entry_buffer_atr = float(env.get(\"entry_buffer_atr\", ENTRY_BUFFER_ATR))\n    except (TypeError, ValueError, OverflowError):\n        entry_buffer_atr = ENTRY_BUFFER_ATR\n    if not math.isfinite(entry_buffer_atr) or entry_buffer_atr < 0:\n        entry_buffer_atr = ENTRY_BUFFER_ATR\n    trigger = float(reference + av * entry_buffer_atr) if side == \"BUY\" else float(reference - av * entry_buffer_atr)\n    model = execution_market_model((live_quote or {}).get(\"ticks\", []), side, trigger, av, symbol=symbol)\n    model.update({\n        \"trigger_price\":trigger,\n        \"reference_candle_timestamp\":str(closed_rows[-1].get(\"timestamp\")),\n        \"signal\": side if model.get(\"state\") == \"EXECUTABLE_TRIGGER_CROSSED\" and model.get(\"execution_score\",0) >= EXECUTION_SCORE_MIN else \"NEUTRAL\",\n        \"current_price\":model.get(\"executable_price\"),\n    })\n    return model\ndef closed_candle_bias(rows):\n    \"\"\"Directional candle evidence from the last CLOSED candle only.\"\"\"\n    return candle_signal(rows)\ndef ema(values, period):\n    if len(values) < period:\n        return [None] * len(values)\n    alpha = 2.0 / (period + 1)\n    out = [None] * (period - 1)\n    e = statistics.fmean(values[:period])\n    out.append(e)\n    for v in values[period:]:\n        e = alpha * v + (1 - alpha) * e\n        out.append(e)\n    return out\ndef rsi(values, period=14):\n    if len(values) <= period:\n        return None\n    gains, losses = [], []\n    for i in range(1, len(values)):\n        d = values[i] - values[i - 1]\n        gains.append(max(d, 0))\n        losses.append(max(-d, 0))\n    ag = statistics.fmean(gains[:period])\n    al = statistics.fmean(losses[:period])\n    for g, l in zip(gains[period:], losses[period:]):\n        ag = (ag * (period - 1) + g) / period\n        al = (al * (period - 1) + l) / period\n    if al == 0:\n        return 100.0\n    return 100.0 - (100.0 / (1.0 + ag / al))\ndef atr(rows, period=14):\n    if len(rows) <= period:\n        return None\n    trs = []\n    for i in range(1, len(rows)):\n        h, l, pc = rows[i][\"high\"], rows[i][\"low\"], rows[i-1][\"close\"]\n        trs.append(max(h-l, abs(h-pc), abs(l-pc)))\n    return statistics.fmean(trs[-period:])\ndef macd(values):\n    e12, e26 = ema(values, 12), ema(values, 26)\n    series = [(a-b) for a, b in zip(e12, e26) if a is not None and b is not None]\n    if len(series) < 9:\n        return 0.0, 0.0\n    sig = ema(series, 9)\n    return series[-1], sig[-1] if sig[-1] is not None else 0.0\ndef swings(rows, lookback=2):\n    highs, lows = [], []\n    for i in range(lookback, len(rows)-lookback):\n        h = rows[i][\"high\"]\n        l = rows[i][\"low\"]\n        if h == max(x[\"high\"] for x in rows[i-lookback:i+lookback+1]):\n            highs.append((i, h))\n        if l == min(x[\"low\"] for x in rows[i-lookback:i+lookback+1]):\n            lows.append((i, l))\n    return highs, lows\ndef direction(rows):\n    if len(rows) < 30:\n        return \"NEUTRAL\"\n    c = [x[\"close\"] for x in rows]\n    e20, e50 = ema(c, 20), ema(c, 50)\n    if e20[-1] is None or e50[-1] is None:\n        return \"NEUTRAL\"\n    if c[-1] > e20[-1] > e50[-1]:\n        return \"BUY\"\n    if c[-1] < e20[-1] < e50[-1]:\n        return \"SELL\"\n    return \"NEUTRAL\"\ndef structure_signal(rows):\n    if len(rows) < 20:\n        return \"NEUTRAL\", \"insufficient structure\"\n    highs, lows = swings(rows[-80:], 2)\n    if len(highs) < 2 or len(lows) < 2:\n        return \"NEUTRAL\", \"insufficient swing structure\"\n    last_h, prev_h = highs[-1][1], highs[-2][1]\n    last_l, prev_l = lows[-1][1], lows[-2][1]\n    if last_h > prev_h and last_l > prev_l:\n        return \"BUY\", \"bullish HH + HL\"\n    if last_h < prev_h and last_l < prev_l:\n        return \"SELL\", \"bearish LH + LL\"\n    return \"NEUTRAL\", \"mixed structure\"\ndef fresh_structural_break(rows, side, lookback=2):\n    \"\"\"Detect a fresh break on the latest CLOSED candle only.\n    BUY is invalidated by a bearish close below the latest prior swing low;\n    SELL is invalidated by a bullish close above the latest prior swing high.\n    No synthetic candles/prices are used.\n    \"\"\"\n    if side not in (\"BUY\", \"SELL\") or len(rows) < 25:\n        return {\"invalidated\": False, \"state\": \"UNAVAILABLE\", \"reference\": None}\n    try:\n        closed = [r for r in rows if all(\n            r.get(k) is not None and math.isfinite(float(r.get(k)))\n            for k in (\"open\", \"high\", \"low\", \"close\")\n        )]\n        if len(closed) < 25:\n            return {\"invalidated\": False, \"state\": \"UNAVAILABLE\", \"reference\": None}\n        window = closed[-80:]\n        highs, lows = swings(window, lookback)\n        last_close = float(window[-1][\"close\"])\n        prior_lows = [float(v) for i, v in lows if i < len(window) - 1]\n        prior_highs = [float(v) for i, v in highs if i < len(window) - 1]\n        if side == \"BUY\" and prior_lows:\n            ref = prior_lows[-1]\n            if last_close < ref:\n                return {\"invalidated\": True, \"state\": \"FRESH_BEARISH_BREAK\",\n                        \"reference\": ref, \"close\": last_close,\n                        \"timestamp\": str(window[-1].get(\"timestamp\"))}\n        if side == \"SELL\" and prior_highs:\n            ref = prior_highs[-1]\n            if last_close > ref:\n                return {\"invalidated\": True, \"state\": \"FRESH_BULLISH_BREAK\",\n                        \"reference\": ref, \"close\": last_close,\n                        \"timestamp\": str(window[-1].get(\"timestamp\"))}\n        return {\"invalidated\": False, \"state\": \"STRUCTURE_INTACT\",\n                \"reference\": (prior_lows[-1] if side == \"BUY\" and prior_lows else\n                              prior_highs[-1] if side == \"SELL\" and prior_highs else None),\n                \"close\": last_close, \"timestamp\": str(window[-1].get(\"timestamp\"))}\n    except (TypeError, ValueError, OverflowError):\n        return {\"invalidated\": False, \"state\": \"UNAVAILABLE\", \"reference\": None}\n\ndef candle_signal(rows):\n    if len(rows) < 3:\n        return \"NEUTRAL\", \"insufficient candles\"\n    a, b = rows[-2], rows[-1]\n    body = abs(b[\"close\"] - b[\"open\"])\n    rng = max(b[\"high\"] - b[\"low\"], 1e-12)\n    bull = b[\"close\"] > b[\"open\"]\n    bear = b[\"close\"] < b[\"open\"]\n    engulf_bull = bull and a[\"close\"] < a[\"open\"] and b[\"close\"] >= a[\"open\"] and b[\"open\"] <= a[\"close\"]\n    engulf_bear = bear and a[\"close\"] > a[\"open\"] and b[\"open\"] >= a[\"close\"] and b[\"close\"] <= a[\"open\"]\n    if engulf_bull or (bull and body/rng >= 0.65):\n        return \"BUY\", \"bullish impulse/engulfing\"\n    if engulf_bear or (bear and body/rng >= 0.65):\n        return \"SELL\", \"bearish impulse/engulfing\"\n    return \"NEUTRAL\", \"no strong price action\"\ndef liquidity_signal(rows):\n    if len(rows) < 25:\n        return \"NEUTRAL\", \"insufficient liquidity history\"\n    prior = rows[-21:-1]\n    last = rows[-1]\n    hi = max(x[\"high\"] for x in prior)\n    lo = min(x[\"low\"] for x in prior)\n    if last[\"low\"] < lo and last[\"close\"] > lo:\n        return \"BUY\", \"sell-side liquidity sweep\"\n    if last[\"high\"] > hi and last[\"close\"] < hi:\n        return \"SELL\", \"buy-side liquidity sweep\"\n    return \"NEUTRAL\", \"no fresh sweep\"\ndef fib_signal(rows):\n    if len(rows) < 40:\n        return \"NEUTRAL\", \"insufficient swing range\"\n    highs, lows = swings(rows[-100:], 2)\n    if not highs or not lows:\n        return \"NEUTRAL\", \"no usable swing\"\n    h = highs[-1][1]\n    l = lows[-1][1]\n    span = h - l\n    if span <= 0:\n        return \"NEUTRAL\", \"invalid swing range\"\n    p = rows[-1][\"close\"]\n    retr = (p-l)/span\n    if 0.382 <= retr <= 0.618:\n        d = direction(rows)\n        return d, \"price in 0.382-0.618 retracement zone\" if d != \"NEUTRAL\" else \"fib zone, trend unclear\"\n    return \"NEUTRAL\", \"fib zone not near\"\ndef trend_signal(rows):\n    d = direction(rows)\n    return d, (\"EMA alignment\" if d != \"NEUTRAL\" else \"mixed EMA alignment\")\ndef indicator_signal(rows):\n    c = [x[\"close\"] for x in rows]\n    rv = rsi(c)\n    m, s = macd(c)\n    if rv is None:\n        return \"NEUTRAL\", \"RSI unavailable\"\n    if rv >= 55 and m > s:\n        return \"BUY\", f\"RSI {rv:.1f} / MACD bullish\"\n    if rv <= 45 and m < s:\n        return \"SELL\", f\"RSI {rv:.1f} / MACD bearish\"\n    return \"NEUTRAL\", f\"RSI {rv:.1f} / MACD mixed\"\ndef session_signal(rows, category):\n    if not rows:\n        return \"NEUTRAL\", \"session unavailable\"\n    h = rows[-1][\"timestamp\"].hour\n    if str(category).lower() == \"commodity\":\n        if 13 <= h < 17:\n            return direction(rows), \"XAU London + New York prime overlap\"\n        if 7 <= h < 13:\n            return direction(rows), \"XAU London liquidity window\"\n        if 17 <= h < 21:\n            return direction(rows), \"XAU New York continuation\"\n        return \"NEUTRAL\", \"XAU outside preferred liquidity window\"\n    if 13 <= h < 16:\n        return direction(rows), \"London + New York overlap\"\n    if 7 <= h < 16:\n        return direction(rows), \"London session\"\n    if 13 <= h < 21:\n        return direction(rows), \"New York session\"\n    return \"NEUTRAL\", \"outside preferred session\"\n\ndef current_trading_session(now=None):\n    \"\"\"Current global FX session for reporting only; never changes strategy qualification.\"\"\"\n    now = now or now_utc()\n    h = now.hour + now.minute / 60.0\n    if 13 <= h < 16:\n        name, quality = \"LONDON + NEW YORK OVERLAP\", \"PRIME LIQUIDITY\"\n    elif 7 <= h < 13:\n        name, quality = \"LONDON\", \"ACTIVE\"\n    elif 16 <= h < 21:\n        name, quality = \"NEW YORK\", \"ACTIVE\"\n    elif 0 <= h < 7:\n        name, quality = \"ASIA/PACIFIC\", \"ACTIVE\"\n    else:\n        name, quality = \"TRANSITION / OFF-PRIME\", \"LOWER LIQUIDITY\"\n    eat = timezone(timedelta(hours=3))\n    return {\"name\": name, \"quality\": quality, \"utc\": now.isoformat(), \"eat\": now.astimezone(eat).isoformat(), \"source\": \"CURRENT_UTC_CLOCK\"}\ndef smc_signal(rows):\n    if len(rows) < 30:\n        return \"NEUTRAL\", \"insufficient MSS history\"\n    last = rows[-1]\n    hi = max(x[\"high\"] for x in rows[-21:-1])\n    lo = min(x[\"low\"] for x in rows[-21:-1])\n    if last[\"close\"] > hi:\n        return \"BUY\", \"bullish MSS / structure break\"\n    if last[\"close\"] < lo:\n        return \"SELL\", \"bearish MSS / structure break\"\n    return \"NEUTRAL\", \"no confirmed MSS\"\ndef supply_demand_signal(rows):\n    if len(rows) < 30:\n        return \"NEUTRAL\", \"insufficient impulse history\"\n    a, b = rows[-2], rows[-1]\n    atrv = atr(rows)\n    if atrv is None:\n        return \"NEUTRAL\", \"ATR unavailable\"\n    body = abs(b[\"close\"] - b[\"open\"])\n    if body >= 1.2 * atrv:\n        return (\"BUY\", \"demand zone + bullish impulse\") if b[\"close\"] > b[\"open\"] else (\"SELL\", \"supply zone + bearish impulse\")\n    return \"NEUTRAL\", \"no strong impulse\"\ndef fair_value_gaps(rows, lookback=80):\n    \"\"\"Detect real closed-candle three-candle imbalances.\"\"\"\n    if len(rows) < 3:\n        return []\n    sample = rows[-lookback:]\n    out = []\n    for i in range(2, len(sample)):\n        a, b, c = sample[i-2], sample[i-1], sample[i]\n        av = atr(sample[:i+1])\n        if c[\"low\"] > a[\"high\"]:\n            gap = c[\"low\"] - a[\"high\"]\n            out.append({\"side\":\"BUY\",\"timestamp\":c[\"timestamp\"],\"low\":a[\"high\"],\"high\":c[\"low\"],\"gap\":gap,\"quality\":gap/av if av and av > 0 else 0.0,\"mitigated\":any(x[\"low\"] <= a[\"high\"] for x in sample[i+1:])})\n        if c[\"high\"] < a[\"low\"]:\n            gap = a[\"low\"] - c[\"high\"]\n            out.append({\"side\":\"SELL\",\"timestamp\":c[\"timestamp\"],\"low\":c[\"high\"],\"high\":a[\"low\"],\"gap\":gap,\"quality\":gap/av if av and av > 0 else 0.0,\"mitigated\":any(x[\"high\"] >= a[\"low\"] for x in sample[i+1:])})\n    return out\ndef fvg_signal(rows):\n    fresh = [g for g in fair_value_gaps(rows) if not g[\"mitigated\"]]\n    if not fresh:\n        return \"NEUTRAL\", \"no fresh unmitigated FVG\", None\n    g = fresh[-1]\n    strength = \"strong\" if g[\"quality\"] >= 0.25 else \"normal\"\n    return g[\"side\"], f\"{strength} unmitigated FVG {g['low']:.6g}-{g['high']:.6g}\", g\ndef _rng(c): return max(float(c[\"high\"])-float(c[\"low\"]),1e-12)\ndef _body(c): return abs(float(c[\"close\"])-float(c[\"open\"]))\ndef _displacement(rows):\n    if len(rows)<25: return False\n    av=atr(rows)\n    if not av: return False\n    c=rows[-1]; med=statistics.median([_rng(x) for x in rows[-21:-1]])\n    return _body(c)/_rng(c)>=0.55 and _rng(c)>=1.15*av and _rng(c)>=1.20*med\ndef _rejection(rows,side):\n    if not rows:return False\n    c=rows[-1]; r=_rng(c); o,h,l,cl=map(float,(c[\"open\"],c[\"high\"],c[\"low\"],c[\"close\"]))\n    return ((side==\"BUY\" and cl>o and (cl-l)/r>=.70 and (h-cl)/r<=.25) or\n            (side==\"SELL\" and cl<o and (h-cl)/r>=.70 and (cl-l)/r<=.25))\ndef _range(rows,n=20):\n    if len(rows)<n+1:return None\n    s=rows[-n-1:-1]; return max(float(x[\"high\"]) for x in s),min(float(x[\"low\"]) for x in s)\ndef _ema_pullback(rows):\n    if len(rows)<60:return \"NEUTRAL\",\"insufficient EMA history\"\n    c=[]\n    for x in rows:\n        try:\n            v=float(x.get(\"close\"))\n            if math.isfinite(v):\n                c.append(v)\n        except (TypeError,ValueError,AttributeError):\n            continue\n    if len(c)<60:return \"NEUTRAL\",\"insufficient valid EMA closes\"\n    e20_series,e50_series=ema(c,20),ema(c,50)\n    try:\n        av=atr(rows)\n    except (TypeError,ValueError,KeyError):\n        av=None\n    if not av:return \"NEUTRAL\",\"EMA/ATR unavailable\"\n    e20=e20_series[-1] if isinstance(e20_series,list) else e20_series\n    e50=e50_series[-1] if isinstance(e50_series,list) else e50_series\n    if e20 is None or e50 is None:return \"NEUTRAL\",\"EMA values unavailable\"\n    p=c[-1]\n    if e20>e50 and abs(p-e20)<=.45*av and p>=e50:return \"BUY\",\"EMA20/50 bullish pullback\"\n    if e20<e50 and abs(p-e20)<=.45*av and p<=e50:return \"SELL\",\"EMA20/50 bearish pullback\"\n    return \"NEUTRAL\",\"no EMA20/50 pullback\"\ndef _bollinger(rows):\n    if len(rows)<25:return \"NEUTRAL\",\"insufficient Bollinger history\"\n    c=[float(x[\"close\"]) for x in rows[-20:]]; mid=statistics.fmean(c); sd=statistics.pstdev(c)\n    if sd<=0:return \"NEUTRAL\",\"zero Bollinger deviation\"\n    p=c[-1]\n    if p<=mid-2*sd and p>c[-2]:return \"BUY\",\"lower-band rejection\"\n    if p>=mid+2*sd and p<c[-2]:return \"SELL\",\"upper-band rejection\"\n    return \"NEUTRAL\",\"no Bollinger reversal\"\ndef _stochastic(rows):\n    if len(rows)<20:return \"NEUTRAL\",\"insufficient stochastic history\"\n    s=rows[-14:]; hi=max(float(x[\"high\"]) for x in s); lo=min(float(x[\"low\"]) for x in s)\n    k=100*(float(s[-1][\"close\"])-lo)/max(hi-lo,1e-12); kp=100*(float(s[-2][\"close\"])-lo)/max(hi-lo,1e-12)\n    if k<20 and k>kp:return \"BUY\",f\"stochastic reversal {k:.1f}\"\n    if k>80 and k<kp:return \"SELL\",f\"stochastic reversal {k:.1f}\"\n    return \"NEUTRAL\",f\"stochastic {k:.1f}\"\ndef _vwap(rows):\n    vols=[float(x.get(\"volume\") or 0) for x in rows[-50:]]\n    if sum(vols)<=0:return \"NEUTRAL\",\"VWAP unavailable: no usable volume\"\n    pv=sum((((float(x[\"high\"])+float(x[\"low\"])+float(x[\"close\"]))/3)*float(x.get(\"volume\") or 0)) for x in rows[-50:])\n    vv=sum(vols); v=pv/vv; p=float(rows[-1][\"close\"]); av=atr(rows)\n    if not av:return \"NEUTRAL\",\"ATR unavailable\"\n    if p<v and p>float(rows[-1][\"open\"]) and abs(p-v)<=av:return \"BUY\",\"VWAP reversion\"\n    if p>v and p<float(rows[-1][\"open\"]) and abs(p-v)<=av:return \"SELL\",\"VWAP reversion\"\n    return \"NEUTRAL\",\"no VWAP reversion\"\ndef _rsi_div(rows):\n    if len(rows)<60:return \"NEUTRAL\",\"insufficient divergence history\"\n    c=[float(x[\"close\"]) for x in rows]; rv=[rsi(c[:i+1]) for i in range(len(c))]\n    hs,ls=swings(rows[-80:],2); off=max(0,len(rows)-80)\n    if len(ls)>=2:\n        a,b=ls[-2],ls[-1]; ia,ib=off+a[0],off+b[0]\n        if all(0<=i<len(rv) and rv[i] is not None for i in (ia,ib)) and b[1]<a[1] and rv[ib]>rv[ia]+2:return \"BUY\",\"bullish RSI divergence\"\n    if len(hs)>=2:\n        a,b=hs[-2],hs[-1]; ia,ib=off+a[0],off+b[0]\n        if all(0<=i<len(rv) and rv[i] is not None for i in (ia,ib)) and b[1]>a[1] and rv[ib]<rv[ia]-2:return \"SELL\",\"bearish RSI divergence\"\n    return \"NEUTRAL\",\"no RSI divergence\"\ndef _break_retest(rows):\n    rr=_range(rows)\n    if not rr:return \"NEUTRAL\",\"insufficient range\"\n    hi,lo=rr;p=float(rows[-1][\"close\"]);av=atr(rows)\n    if not av:return \"NEUTRAL\",\"ATR unavailable\"\n    if p>hi and abs(p-hi)<=.60*av:return \"BUY\",\"range breakout/retest\"\n    if p<lo and abs(p-lo)<=.60*av:return \"SELL\",\"range breakout/retest\"\n    return \"NEUTRAL\",\"no breakout/retest\"\ndef _range_reject(rows):\n    rr=_range(rows)\n    if not rr:return \"NEUTRAL\",\"insufficient range\"\n    hi,lo=rr;c=rows[-1]\n    if float(c[\"low\"])<=lo and float(c[\"close\"])>lo and _rejection(rows,\"BUY\"):return \"BUY\",\"range-low rejection\"\n    if float(c[\"high\"])>=hi and float(c[\"close\"])<hi and _rejection(rows,\"SELL\"):return \"SELL\",\"range-high rejection\"\n    return \"NEUTRAL\",\"no range rejection\"\ndef _momentum(rows):\n    if len(rows)<30:return \"NEUTRAL\",\"insufficient momentum history\"\n    av=atr(rows);c=rows[-1]\n    if not av:return \"NEUTRAL\",\"ATR unavailable\"\n    if _rng(c)>=1.15*av and _body(c)/_rng(c)>=.65:\n        return (\"BUY\",\"bullish momentum continuation\") if float(c[\"close\"])>float(c[\"open\"]) else (\"SELL\",\"bearish momentum continuation\")\n    return \"NEUTRAL\",\"no qualifying momentum\"\ndef _session_breakout(rows):\n    if len(rows)<30:return \"NEUTRAL\",\"insufficient session history\"\n    now=rows[-1][\"timestamp\"]\n    if not 9<=now.hour<=17:return \"NEUTRAL\",\"outside session window\"\n    s=[x for x in rows[-120:] if x[\"timestamp\"].date()==now.date() and 7<=x[\"timestamp\"].hour<9]\n    if len(s)<2:return \"NEUTRAL\",\"opening range unavailable\"\n    hi=max(float(x[\"high\"]) for x in s);lo=min(float(x[\"low\"]) for x in s);p=float(rows[-1][\"close\"])\n    if p>hi:return \"BUY\",\"opening/session range breakout\"\n    if p<lo:return \"SELL\",\"opening/session range breakout\"\n    return \"NEUTRAL\",\"inside opening range\"\ndef _prevday(rows,daily,mode=\"sweep\"):\n    if len(daily)<3:return \"NEUTRAL\",\"previous-day level unavailable\"\n    hi,lo=float(daily[-2][\"high\"]),float(daily[-2][\"low\"]);c=rows[-1]\n    if mode==\"sweep\":\n        if float(c[\"high\"])>hi and float(c[\"close\"])<hi:return \"SELL\",\"previous-day-high sweep\"\n        if float(c[\"low\"])<lo and float(c[\"close\"])>lo:return \"BUY\",\"previous-day-low sweep\"\n    else:\n        if float(c[\"close\"])>hi:return \"BUY\",\"previous-day-high breakout\"\n        if float(c[\"close\"])<lo:return \"SELL\",\"previous-day-low breakout\"\n    return \"NEUTRAL\",\"no previous-day event\"\ndef _asian(rows):\n    if not rows:return \"NEUTRAL\",\"Asian range unavailable\"\n    now=rows[-1][\"timestamp\"];s=[x for x in rows[-180:] if x[\"timestamp\"].date()==now.date() and 0<=x[\"timestamp\"].hour<7]\n    if len(s)<3:return \"NEUTRAL\",\"insufficient Asian range\"\n    hi=max(float(x[\"high\"]) for x in s);lo=min(float(x[\"low\"]) for x in s);c=rows[-1]\n    if float(c[\"low\"])<lo and float(c[\"close\"])>lo:return \"BUY\",\"Asian-low sweep\"\n    if float(c[\"high\"])>hi and float(c[\"close\"])<hi:return \"SELL\",\"Asian-high sweep\"\n    return \"NEUTRAL\",\"no Asian sweep\"\ndef _order_block(rows):\n    if len(rows)<15:return \"NEUTRAL\",None,\"insufficient order-block history\"\n    av=atr(rows)\n    if not av:return \"NEUTRAL\",None,\"ATR unavailable\"\n    for i in range(len(rows)-2,max(-1,len(rows)-10),-1):\n        b,imp=rows[i],rows[i+1]\n        if _body(imp)<1.2*av:continue\n        bull=float(imp[\"close\"])>float(imp[\"open\"]); bb=float(b[\"close\"])<float(b[\"open\"]); br=float(b[\"close\"])>float(b[\"open\"])\n        p=float(rows[-1][\"close\"]);z=(float(b[\"low\"]),float(b[\"high\"]))\n        if bull and bb and z[0]-.25*av<=p<=z[1]+.25*av:return \"BUY\",z,\"bullish OB retest\"\n        if not bull and br and z[0]-.25*av<=p<=z[1]+.25*av:return \"SELL\",z,\"bearish OB retest\"\n    return \"NEUTRAL\",None,\"no fresh OB retest\"\ndef _breaker(rows):\n    if len(rows)<25:return \"NEUTRAL\",\"insufficient breaker history\"\n    sig,z,_=_order_block(rows[:-3]);av=atr(rows)\n    if sig==\"NEUTRAL\" or not z or not av:return \"NEUTRAL\",\"no breaker candidate\"\n    p=float(rows[-1][\"close\"])\n    if sig==\"SELL\" and z[0]<p<=z[1]+.35*av:return \"BUY\",\"bullish breaker retest\"\n    if sig==\"BUY\" and z[0]-.35*av<=p<z[1]:return \"SELL\",\"bearish breaker retest\"\n    return \"NEUTRAL\",\"no breaker retest\"\ndef _near_fvg(rows,side):\n    sig,_,g=fvg_signal(rows)\n    if sig!=side or not g:return False\n    av=atr(rows);p=float(rows[-1][\"close\"])\n    return bool(av and g[\"low\"]-.60*av<=p<=g[\"high\"]+.60*av)\ndef _srec(name,cat,tier,side,score,evidence):\n    return {\"name\":name,\"category\":cat,\"tier\":tier,\"side\":side,\"score\":float(score),\"evidence\":evidence}\ndef normalize_strategy_rows(rows):\n    \"\"\"Return only candles with finite OHLC values for strategy math; never coerce None to 0.\"\"\"\n    clean = []\n    for r in rows or []:\n        if not isinstance(r, dict):\n            continue\n        try:\n            vals = {k: float(r.get(k)) for k in (\"open\",\"high\",\"low\",\"close\")}\n            if not all(math.isfinite(v) for v in vals.values()):\n                continue\n            if vals[\"high\"] < max(vals[\"open\"], vals[\"close\"]) or vals[\"low\"] > min(vals[\"open\"], vals[\"close\"]) or vals[\"high\"] < vals[\"low\"]:\n                continue\n            x = dict(r)\n            x.update(vals)\n            clean.append(x)\n        except (TypeError, ValueError):\n            continue\n    return clean\n\ndef _return_efficiency(rows, lookback=20):\n    \"\"\"Directional efficiency: net displacement divided by total path.\"\"\"\n    if not rows or len(rows) < lookback + 1:\n        return None\n    sample = rows[-(lookback + 1):]\n    try:\n        closes = [float(x[\"close\"]) for x in sample]\n        net = abs(closes[-1] - closes[0])\n        path = sum(abs(closes[i] - closes[i-1]) for i in range(1, len(closes)))\n        return net / path if path > 0 else 0.0\n    except (TypeError, ValueError, KeyError):\n        return None\n\ndef _volatility_percentile_from_rows(rows, lookback=60):\n    \"\"\"Current ATR/price percentile versus its own recent distribution.\"\"\"\n    if not rows or len(rows) < 60:\n        return None\n    vals = []\n    start = max(30, len(rows) - lookback)\n    for i in range(start, len(rows)):\n        try:\n            a = atr(rows[:i+1])\n            px = float(rows[i][\"close\"])\n            if a and a > 0 and px > 0:\n                vals.append(a / px)\n        except (TypeError, ValueError, KeyError):\n            continue\n    if len(vals) < 20:\n        return None\n    cur = vals[-1]\n    return 100.0 * sum(v <= cur for v in vals) / len(vals)\n\ndef adaptive_market_state(rows_by_tf, category=\"\"):\n    \"\"\"Classify the observed LSE market state for context-aware decisions.\"\"\"\n    h4 = rows_by_tf.get(\"4h\", []) if isinstance(rows_by_tf, dict) else []\n    h1 = rows_by_tf.get(\"1h\", []) if isinstance(rows_by_tf, dict) else []\n    m15 = rows_by_tf.get(\"15m\", []) if isinstance(rows_by_tf, dict) else []\n    m5 = rows_by_tf.get(\"5m\", []) if isinstance(rows_by_tf, dict) else []\n    eff4 = _return_efficiency(h4, 20)\n    eff1 = _return_efficiency(h1, 24)\n    eff15 = _return_efficiency(m15, 24)\n    vol4 = _volatility_percentile_from_rows(h4)\n    vol1 = _volatility_percentile_from_rows(h1)\n    vol15 = _volatility_percentile_from_rows(m15)\n    vol5 = _volatility_percentile_from_rows(m5)\n    effs = [x for x in (eff4, eff1, eff15) if x is not None]\n    vols = [x for x in (vol4, vol1, vol15, vol5) if x is not None]\n    efficiency = sum(effs) / len(effs) if effs else None\n    vol_pct = sum(vols) / len(vols) if vols else None\n    directions = [direction(x) for x in (h4, h1, m15) if len(x) >= 30]\n    non_neutral = [d for d in directions if d in (\"BUY\", \"SELL\")]\n    directional_agreement = (\n        max(non_neutral.count(\"BUY\"), non_neutral.count(\"SELL\")) / len(non_neutral)\n        if non_neutral else 0.0\n    )\n    if vol_pct is not None and vol_pct >= ADAPTIVE_EXTREME_VOL_PERCENTILE:\n        regime = \"STRESSED_HIGH_VOL\"\n    elif efficiency is not None and efficiency >= ADAPTIVE_MIN_TREND_EFFICIENCY and directional_agreement >= 0.67:\n        regime = \"DIRECTIONAL_TREND\"\n    elif efficiency is not None and efficiency <= ADAPTIVE_RANGE_EFFICIENCY and (vol_pct is None or vol_pct <= 60):\n        regime = \"BALANCED_RANGE\"\n    elif vol_pct is not None and vol_pct <= ADAPTIVE_LOW_VOL_PERCENTILE:\n        regime = \"COMPRESSED_LOW_VOL\"\n    else:\n        regime = \"TRANSITIONAL\"\n    return {\n        \"engine\": ADAPTIVE_ENGINE_VERSION,\n        \"regime\": regime,\n        \"efficiency\": round(efficiency, 3) if efficiency is not None else None,\n        \"volatility_percentile\": round(vol_pct, 1) if vol_pct is not None else None,\n        \"directional_agreement\": round(directional_agreement, 3),\n        \"timeframe_directions\": directions,\n    }\n\ndef dynamic_direction_state(rows_by_tf, tf_consensus, monthly_side):\n    \"\"\"Determine strategic bias vs tactical direction and whether a genuine reversal has earned confirmation.\n\n    The 1MO direction remains strategic context, not a permanent directional lock.\n    A reversal requires multi-timeframe evidence and fresh structural invalidation\n    of the old side when the 4H has not yet fully flipped.\n    \"\"\"\n    ctx = {k: tf_consensus.get(k, \"NEUTRAL\") for k in (\"1mo\", \"1d\", \"4h\", \"1h\", \"15m\", \"5m\")}\n    tactical = [ctx[k] for k in (\"4h\", \"1h\", \"15m\", \"5m\") if ctx[k] in (\"BUY\", \"SELL\")]\n    counts = {\"BUY\": tactical.count(\"BUY\"), \"SELL\": tactical.count(\"SELL\")}\n    if counts[\"BUY\"] == counts[\"SELL\"]:\n        tactical_side = \"NEUTRAL\"\n    else:\n        tactical_side = \"BUY\" if counts[\"BUY\"] > counts[\"SELL\"] else \"SELL\"\n\n    strategic = monthly_side if monthly_side in (\"BUY\", \"SELL\") else \"NEUTRAL\"\n    old_side = strategic\n    new_side = tactical_side if tactical_side in (\"BUY\", \"SELL\") else \"NEUTRAL\"\n    reversal = False\n    transition = False\n    h4_flip = False\n    htf_confirmations = 0\n    execution_confirmations = sum(1 for k in (\"1h\", \"15m\", \"5m\") if ctx[k] == new_side) if new_side in (\"BUY\", \"SELL\") else 0\n    invalidation = {\"state\": \"NONE\", \"invalidated\": False}\n\n    if strategic in (\"BUY\", \"SELL\") and new_side in (\"BUY\", \"SELL\") and new_side != strategic:\n        htf_confirmations = sum(1 for k in (\"4h\", \"1h\") if ctx[k] == new_side)\n        if len(rows_by_tf.get(\"4h\", [])) >= 25:\n            invalidation = fresh_structural_break(rows_by_tf[\"4h\"], strategic)\n        h4_flip = ctx[\"4h\"] == new_side\n        strong_tactical = execution_confirmations >= REVERSAL_MIN_EXECUTION_CONFIRMATIONS\n        structural_flip = h4_flip or bool(invalidation.get(\"invalidated\"))\n        if (htf_confirmations >= REVERSAL_MIN_HTF_CONFIRMATIONS and strong_tactical) or (\n            bool(invalidation.get(\"invalidated\")) and strong_tactical\n        ):\n            reversal = True\n        else:\n            transition = strong_tactical or bool(invalidation.get(\"invalidated\")) or ctx[\"1h\"] == new_side\n\n    if strategic == \"NEUTRAL\" and new_side in (\"BUY\", \"SELL\"):\n        # With no monthly anchor, require 4H + 1H plus at least one lower-TF confirmation.\n        htf_confirmations = sum(1 for k in (\"4h\", \"1h\") if ctx[k] == new_side)\n        reversal = htf_confirmations >= REVERSAL_MIN_HTF_CONFIRMATIONS and execution_confirmations >= REVERSAL_MIN_EXECUTION_CONFIRMATIONS\n        transition = not reversal and (execution_confirmations >= 1 or ctx[\"4h\"] == new_side)\n\n    if strategic == new_side and new_side in (\"BUY\", \"SELL\"):\n        mode = \"CONTINUATION\"\n        active_direction = new_side\n    elif reversal:\n        mode = \"REVERSAL_CONFIRMED\"\n        active_direction = new_side\n    elif transition:\n        mode = \"REVERSAL_WATCH\"\n        active_direction = new_side\n    else:\n        mode = \"NEUTRAL\" if new_side == \"NEUTRAL\" else \"TACTICAL\"\n        active_direction = new_side\n\n    return {\n        \"active_direction\": active_direction,\n        \"strategic_bias\": strategic,\n        \"tactical_direction\": new_side,\n        \"mode\": mode,\n        \"reversal_confirmed\": reversal,\n        \"transition_watch\": transition,\n        \"old_direction\": old_side,\n        \"new_direction\": new_side,\n        \"htf_confirmations\": htf_confirmations,\n        \"execution_confirmations\": execution_confirmations,\n        \"h4_flip\": h4_flip,\n        \"structural_invalidation\": invalidation,\n        \"timeframe_directions\": ctx,\n    }\n\ndef adaptive_strategy_weight(name, regime):\n    \"\"\"Regime-aware evidence weights; a weight never creates a directional vote.\"\"\"\n    weights = {\n        \"DIRECTIONAL_TREND\": {\n            \"SMC\":1.35,\"Market Structure\":1.35,\"Liquidity\":1.30,\"Supply/Demand\":1.15,\n            \"Price Action\":1.10,\"Fibonacci\":0.95,\"Trend\":1.30,\"RSI+MACD\":0.85,\"Session\":1.00},\n        \"BALANCED_RANGE\": {\n            \"SMC\":1.30,\"Market Structure\":1.05,\"Liquidity\":1.25,\"Supply/Demand\":1.25,\n            \"Price Action\":1.20,\"Fibonacci\":1.10,\"Trend\":0.80,\"RSI+MACD\":1.10,\"Session\":1.00},\n        \"STRESSED_HIGH_VOL\": {\n            \"SMC\":1.40,\"Market Structure\":1.30,\"Liquidity\":1.35,\"Supply/Demand\":1.15,\n            \"Price Action\":1.25,\"Fibonacci\":0.75,\"Trend\":1.00,\"RSI+MACD\":0.65,\"Session\":1.10},\n        \"COMPRESSED_LOW_VOL\": {\n            \"SMC\":1.25,\"Market Structure\":1.00,\"Liquidity\":1.10,\"Supply/Demand\":1.15,\n            \"Price Action\":1.05,\"Fibonacci\":1.10,\"Trend\":0.85,\"RSI+MACD\":1.00,\"Session\":1.05},\n        \"TRANSITIONAL\": {\n            \"SMC\":1.30,\"Market Structure\":1.15,\"Liquidity\":1.20,\"Supply/Demand\":1.15,\n            \"Price Action\":1.10,\"Fibonacci\":1.00,\"Trend\":1.00,\"RSI+MACD\":0.90,\"Session\":1.00},\n    }\n    return weights.get(regime, weights[\"TRANSITIONAL\"]).get(str(name or \"\"), 1.0)\n\ndef adaptive_decision_profile(state):\n    \"\"\"Conservative regime policy: adaptation can tighten, never weaken, hard gates.\"\"\"\n    regime = str(state.get(\"regime\", \"TRANSITIONAL\"))\n    profiles = {\n        \"DIRECTIONAL_TREND\":{\"score_floor\":70.0,\"strategies\":2,\"opposed\":1,\"stability\":0.50},\n        \"BALANCED_RANGE\":{\"score_floor\":78.0,\"strategies\":3,\"opposed\":1,\"stability\":0.60},\n        \"STRESSED_HIGH_VOL\":{\"score_floor\":84.0,\"strategies\":3,\"opposed\":0,\"stability\":0.70},\n        \"COMPRESSED_LOW_VOL\":{\"score_floor\":76.0,\"strategies\":3,\"opposed\":1,\"stability\":0.60},\n        \"TRANSITIONAL\":{\"score_floor\":80.0,\"strategies\":3,\"opposed\":1,\"stability\":0.65},\n    }\n    p = profiles.get(regime, profiles[\"TRANSITIONAL\"])\n    return {\n        \"regime\":regime,\n        \"score_floor\":max(ADAPTIVE_BASE_SCORE_FLOOR, min(ADAPTIVE_MAX_SCORE_FLOOR, p[\"score_floor\"])),\n        \"required_strategies\":max(ADAPTIVE_BASE_STRATEGIES, min(ADAPTIVE_MAX_STRATEGIES, p[\"strategies\"])),\n        \"max_opposed\":p[\"opposed\"],\n        \"min_signal_stability\":max(ADAPTIVE_MIN_SIGNAL_STABILITY, p[\"stability\"]),\n    }\n\ndef adaptive_consensus_score(strategy_maps, side, state):\n    \"\"\"Weighted cross-timeframe evidence score with conflict penalty.\"\"\"\n    regime = str(state.get(\"regime\", \"TRANSITIONAL\"))\n    weighted = 0.0\n    possible = 0.0\n    names, tfs, opposed = set(), set(), set()\n    for tf_name, methods in strategy_maps.items():\n        for m in methods:\n            name, sig = m.get(\"name\"), m.get(\"signal\")\n            w = adaptive_strategy_weight(name, regime)\n            possible += w\n            if sig == side:\n                names.add(name); tfs.add(tf_name); weighted += w\n            elif sig in (\"BUY\",\"SELL\") and sig != side:\n                opposed.add(name); weighted -= 0.75*w\n    ratio = weighted / possible if possible else 0.0\n    breadth = min(22.0, len(names) * 5.5)\n    tf_bonus = min(18.0, max(0, len(tfs)-1) * 6.0)\n    conflict_penalty = min(18.0, len(opposed) * 4.0)\n    raw = 50.0 + ratio*30.0 + breadth + tf_bonus - conflict_penalty\n    return round(max(0.0, min(100.0, raw)), 1)\n\ndef nine_strategy_signals(rows, category=\"\", smc_result=None):\n    \"\"\"Nine directional strategy modules. Neutral modules do not vote against a side.\n    These are rule modules, not nine independent market data sources. Confirmation requires\n    at least two distinct aligned strategy names plus the separate monthly/context/execution gates.\n    \"\"\"\n    out=[]\n    try:\n        smc = smc_result if isinstance(smc_result, dict) else smc_core(rows)\n        out.append({\"name\":\"SMC\",\"signal\":smc.get(\"side\") if smc.get(\"passed\") else \"NEUTRAL\",\n                    \"why\":smc.get(\"why\",\"SMC not passed\"),\"score\":float(smc.get(\"score\",0) or 0),\"category\":\"SMC\"})\n    except Exception as exc:\n        out.append({\"name\":\"SMC\",\"signal\":\"NEUTRAL\",\"why\":f\"error: {type(exc).__name__}\",\"score\":0.0,\"category\":\"SMC\"})\n    funcs=[\n        (\"Market Structure\", lambda x: structure_signal(x)),\n        (\"Liquidity\", lambda x: liquidity_signal(x)),\n        (\"Supply/Demand\", lambda x: supply_demand_signal(x)),\n        (\"Price Action\", lambda x: candle_signal(x)),\n        (\"Fibonacci\", lambda x: fib_signal(x)),\n        (\"Trend\", lambda x: trend_signal(x)),\n        (\"RSI+MACD\", lambda x: indicator_signal(x)),\n        (\"Session\", lambda x: session_signal(x, category)),\n    ]\n    for name, fn in funcs:\n        try:\n            sig, why = fn(rows)\n        except Exception as exc:\n            sig, why = \"NEUTRAL\", f\"error: {type(exc).__name__}\"\n        out.append({\"name\":name,\"signal\":sig if sig in (\"BUY\",\"SELL\") else \"NEUTRAL\",\n                    \"why\":why,\"score\":100.0 if sig in (\"BUY\",\"SELL\") else 0.0,\"category\":name})\n    return out\n\ndef strategy_catalog(data,symbol,currency_strength):\n    chain=(\"1mo\",\"1d\",\"4h\",\"1h\",\"15m\",\"5m\")\n    results=[]\n    for tf in chain:\n        rows=data.get(tf,[])\n        if len(rows)<30:\n            continue\n        smc=None\n        try: smc=smc_core(rows)\n        except Exception: smc=None\n        for item in nine_strategy_signals(rows, \"\", smc):\n            if item.get(\"signal\") in (\"BUY\",\"SELL\"):\n                results.append({\n                    \"name\":item[\"name\"],\"category\":item.get(\"category\",item[\"name\"]),\n                    \"tier\":\"A\" if item.get(\"name\") in (\"SMC\",\"Market Structure\",\"Liquidity\") else \"B\",\n                    \"side\":item[\"signal\"],\"score\":float(item.get(\"score\",0) or 0),\n                    \"timeframe\":tf,\"evidence\":[item.get(\"why\",\"\")]\n                })\n    return results\ndef strategy_summary(matches,side):\n    m=[x for x in matches if x.get(\"side\")==side]\n    by_name={}\n    for x in m:\n        by_name.setdefault(x.get(\"name\"),[]).append(x)\n    names=sorted([n for n in by_name if n])\n    tfs=sorted(set(x.get(\"timeframe\") for x in m if x.get(\"timeframe\")))\n    best=max([float(x.get(\"score\",0) or 0) for x in m],default=0.0)\n    breadth=min(30.0,max(0.0,(len(names)-2)*8.0))\n    tf_bonus=min(20.0,max(0.0,(len(tfs)-1)*5.0))\n    score=min(100.0,45.0+len(names)*8.0+breadth+tf_bonus) if names else 0.0\n    confirmed=len(names)>=MIN_CONFIRMED_STRATEGIES\n    tier=\"A\" if len(names)>=S_TIER_MIN_STRATEGIES and score>=85 else (\"B\" if confirmed else \"C\")\n    return {\"confirmed\":confirmed,\"count\":len(names),\"strategies\":names,\"categories\":names,\n            \"tier\":tier,\"score\":round(score,1),\"best\":max(m,key=lambda x:float(x.get(\"score\",0) or 0)) if m else None,\n            \"all_matches\":sorted(m,key=lambda x:(float(x.get(\"score\",0) or 0),x.get(\"timeframe\",\"\") ),reverse=True)[:30],\n            \"timeframes\":tfs}\ndef data_quality_report(rows):\n    \"\"\"Validate candle integrity before using precision levels.\"\"\"\n    if not rows:\n        return {\"score\":0.0,\"ok\":False,\"issues\":[\"NO_DATA\"],\"duplicate_timestamps\":0,\"bad_ohlc\":0}\n    bad_ohlc = 0\n    duplicate = 0\n    ts = []\n    for r in rows:\n        try:\n            o,h,l,c = [float(r[k]) for k in (\"open\",\"high\",\"low\",\"close\")]\n            if not all(math.isfinite(x) for x in (o,h,l,c)) or h < max(o,c) or l > min(o,c) or h < l:\n                bad_ohlc += 1\n        except Exception:\n            bad_ohlc += 1\n        try: ts.append(parse_ts(r.get(\"timestamp\")))\n        except Exception: pass\n    for a,b in zip(ts, ts[1:]):\n        if b <= a: duplicate += 1\n    score = max(0.0, 1.0 - (bad_ohlc + duplicate) / max(len(rows),1))\n    issues = ([f\"BAD_OHLC={bad_ohlc}\"] if bad_ohlc else []) + ([f\"NON_MONOTONIC_TS={duplicate}\"] if duplicate else [])\n    ok = len(rows) >= 80 and score >= MIN_DATA_QUALITY\n    if not ok and not issues: issues.append(\"INSUFFICIENT_HISTORY\")\n    return {\"score\":round(score,4),\"ok\":ok,\"issues\":issues,\"duplicate_timestamps\":duplicate,\"bad_ohlc\":bad_ohlc}\ndef volatility_regime(rows):\n    \"\"\"Classify current ATR/price against its recent distribution.\"\"\"\n    if len(rows) < 60: return {\"state\":\"UNKNOWN\",\"ratio\":None,\"percentile\":None}\n    vals=[]\n    for i in range(30,len(rows)):\n        a=atr(rows[:i+1])\n        try: px=float(rows[i][\"close\"])\n        except Exception: continue\n        if a and px>0: vals.append(a/px)\n    if len(vals)<20: return {\"state\":\"UNKNOWN\",\"ratio\":None,\"percentile\":None}\n    cur=vals[-1]; rank=sum(x<=cur for x in vals)/len(vals)*100.0\n    state=\"EXTREME_HIGH\" if rank>=85 else \"HIGH\" if rank>=65 else \"LOW\" if rank<=15 else \"NORMAL\"\n    return {\"state\":state,\"ratio\":cur,\"percentile\":round(rank,1)}\ndef liquidity_sweep_setup(rows):\n    \"\"\"Detect a closed-candle sweep through a recent swing followed by a reclaim.\"\"\"\n    if len(rows)<30: return None\n    sample=rows[-50:]; last=sample[-1]; av=atr(sample)\n    if not av or av<=0: return None\n    highs,lows=swings(sample[:-2],2)\n    candidates=[]\n    for level in [float(x[1]) for x in lows][-5:]:\n        if float(last[\"low\"])<level<float(last[\"close\"]):\n            rng=max(float(last[\"high\"])-float(last[\"low\"]),1e-12)\n            candidates.append((\"BUY\",level,(level-float(last[\"low\"]))/av,abs(float(last[\"close\"])-float(last[\"open\"]))/rng))\n    for level in [float(x[1]) for x in highs][-5:]:\n        if float(last[\"high\"])>level>float(last[\"close\"]):\n            rng=max(float(last[\"high\"])-float(last[\"low\"]),1e-12)\n            candidates.append((\"SELL\",level,(float(last[\"high\"])-level)/av,abs(float(last[\"close\"])-float(last[\"open\"]))/rng))\n    if not candidates: return None\n    side,level,w,b=max(candidates,key=lambda x:(x[2],x[3]))\n    return {\"side\":side,\"liquidity_level\":level,\"wick_atr\":round(w,3),\"body_ratio\":round(b,3),\"timestamp\":str(last.get(\"timestamp\"))}\ndef sniper_style(sniper, side, position_tf):\n    \"\"\"Classify a precision setup as scalp/intraday without treating every WAIT as a confirmed entry.\"\"\"\n    if not isinstance(sniper, dict) or str(sniper.get(\"type\", \"\")).upper() != \"SNIPER\":\n        return \"NONE\"\n    sniper_side = str(sniper.get(\"side\", \"\")).upper()\n    if sniper_side not in (\"BUY\", \"SELL\") or sniper_side != str(side or \"\").upper():\n        return \"NONE\"\n    score = float(sniper.get(\"score\", 0.0) or 0.0)\n    tf = str(position_tf or \"\").lower()\n    rr = sniper.get(\"rr\")\n    precision_ready = (\n        score >= float(SNIPER_MIN_SCORE)\n        and rr is not None\n        and float(rr) >= float(MIN_CONFIRMED_RR)\n        and bool(sniper.get(\"displacement\"))\n        and bool(sniper.get(\"fvg\"))\n    )\n    if precision_ready and tf == \"5m\":\n        return \"SNIPER SCALP\"\n    if precision_ready and tf in (\"15m\", \"1h\", \"4h\"):\n        return \"SNIPER INTRADAY\"\n    if score >= float(SNIPER_MIN_SCORE):\n        return \"SNIPER\"\n    return \"NONE\"\n\ndef sniper_opportunity(trigger_rows, setup_rows, symbol=None, category=\"\"):\n    \"\"\"Precision sniper entry. For XAU/USD, SMC is mandatory and exactly one supporting strategy must align.\"\"\"\n    is_xau = str(symbol or \"\").upper().replace(\" \", \"\") in {\"XAU/USD\", \"XAUUSD\"}\n    if len(trigger_rows)<80 or len(setup_rows)<80: return None\n    if not data_quality_report(trigger_rows)[\"ok\"] or not data_quality_report(setup_rows)[\"ok\"]: return None\n    sweep=liquidity_sweep_setup(trigger_rows)\n    if not sweep: return None\n    av=atr(trigger_rows)\n    if not av or av<=0: return None\n    last=trigger_rows[-1]; rng=max(float(last[\"high\"])-float(last[\"low\"]),1e-12); body=abs(float(last[\"close\"])-float(last[\"open\"]))\n    prior=[float(x[\"high\"])-float(x[\"low\"]) for x in trigger_rows[-30:-1]]\n    med=statistics.median(prior) if prior else rng\n    displacement=(body/rng>=0.55 and rng>=1.15*av and rng>=1.20*med)\n    mss_sig,mss_why=smc_signal(trigger_rows)\n    structure_sig,structure_why=structure_signal(trigger_rows)\n    fvg_sig,fvg_why,fvg=fvg_signal(trigger_rows)\n    fvg_ok=fvg is not None and fvg_sig==sweep[\"side\"] and fvg.get(\"quality\",0)>=0.12\n    htf_sig,_=smc_signal(setup_rows)\n    support_methods = method_scores(trigger_rows)\n    support_candidates = [m for m in support_methods if m.get(\"signal\") == sweep[\"side\"] and m.get(\"name\") in (\"Price Action\",\"Supply/Demand\",\"Trend\",\"Fibonacci\",\"RSI+MACD\",\"Session\") and float(m.get(\"score\",0) or 0) > 85.0]\n    support_priority = (\"Price Action\", \"Supply/Demand\", \"Trend\", \"Fibonacci\", \"RSI+MACD\", \"Session\")\n    supporting = next((m for name in support_priority for m in support_candidates if m.get(\"name\") == name), None)\n    support_name = supporting.get(\"name\") if supporting else None\n    support_why = supporting.get(\"why\") if supporting else \"no independent supporting strategy aligned\"\n    support_ok = supporting is not None\n    score=min(100.0, (28 if sweep else 0)+(24 if displacement else 0)+(20 if mss_sig==sweep[\"side\"] else 0)+(12 if structure_sig==sweep[\"side\"] else 0)+(16 if fvg_ok else 0)+(10 if htf_sig==sweep[\"side\"] else 0)+(10 if support_ok else 0))\n    if fvg_ok:\n        lo,hi=float(fvg[\"low\"]),float(fvg[\"high\"]); entry=(lo+hi)/2.0; zone=[lo,hi]\n    else:\n        entry=float(last[\"close\"]); zone=[entry,entry]\n    sl=sweep[\"liquidity_level\"]-0.20*av if sweep[\"side\"]==\"BUY\" else sweep[\"liquidity_level\"]+0.20*av\n    risk=abs(entry-sl)\n    if risk<=0: return None\n    highs,lows=swings(trigger_rows[-80:],2)\n    targets=sorted([float(x[1]) for x in highs if float(x[1])>entry]) if sweep[\"side\"]==\"BUY\" else sorted([float(x[1]) for x in lows if float(x[1])<entry],reverse=True)\n    structural_targets=[x for x in targets if abs(x-entry)/risk>=MIN_CONFIRMED_RR]\n    if not structural_targets: return {\"type\":\"SNIPER\",\"side\":sweep[\"side\"],\"score\":round(score,1),\"status\":\"WAIT/TP_UNSUPPORTED\",\"entry\":entry,\"entry_zone\":zone,\"sl\":sl,\"tp\":None,\"rr\":None,\"atr\":av,\"distance_atr\":abs(float(last[\"close\"])-entry)/av,\"in_entry_zone\":False,\"liquidity_sweep\":sweep,\"displacement\":displacement,\"mss\":mss_sig,\"structure\":structure_sig,\"fvg\":fvg,\"higher_tf_context\":htf_sig,\"reason\":\"sniper confluence present but no real opposing structural target supports the minimum required R:R\"}\n    structural_target=structural_targets[0]\n    tp=structural_target\n    rr=abs(tp-entry)/risk; current=float(last[\"close\"]); distance=abs(current-entry)/av\n    in_zone=fvg_ok and distance<=SNIPER_MAX_ENTRY_DISTANCE_ATR\n    smc_core_ok = mss_sig==sweep[\"side\"] and structure_sig==sweep[\"side\"] and sweep is not None and (fvg_ok or displacement) and (float(smc_core(trigger_rows).get(\"score\",0) or 0) > 85.0 if is_xau else True)\n    combo_ok = support_ok if is_xau else True\n    confirmed=score>85.0 and smc_core_ok and combo_ok and rr>=MIN_CONFIRMED_RR and in_zone\n    status=(f\"SNIPER_{sweep['side']}\" if confirmed else f\"WAIT_SNIPER_{sweep['side']}\")\n    return {\"type\":\"SNIPER\",\"side\":sweep[\"side\"],\"score\":round(score,1),\"status\":status,\"entry\":entry,\"entry_zone\":zone,\"sl\":sl,\"tp\":tp,\"rr\":rr,\"atr\":av,\"distance_atr\":distance,\"in_entry_zone\":in_zone,\"liquidity_sweep\":sweep,\"displacement\":displacement,\"mss\":mss_sig,\"mss_why\":mss_why,\"structure\":structure_sig,\"structure_why\":structure_why,\"fvg\":fvg,\"fvg_signal\":fvg_sig,\"fvg_why\":fvg_why,\"higher_tf_context\":htf_sig,\"higher_tf_agrees\":htf_sig==sweep[\"side\"],\n            \"smc_core\":{\"required\":True,\"passed\":smc_core_ok,\"components\":[\"Liquidity Sweep\",\"MSS/CHoCH\",\"Structure\",\"FVG/Displacement\"]},\n            \"supporting_strategy\":support_name,\"supporting_strategy_reason\":support_why,\n            \"strategy_combo\":f\"SMC + {support_name}\" if support_name else \"SMC + NONE\",\n            \"reason\":(\"XAU sniper: SMC core + exactly one independent supporting strategy\" if is_xau else \"sweep + displacement/MSS + fresh FVG\")}\ndef opportunity_type_classifier(side, methods, sniper=None):\n    if side not in (\"BUY\",\"SELL\"): return []\n    sig={m.get(\"name\"):m.get(\"signal\") for m in methods}; labels=[]\n    if sniper and sniper.get(\"side\")==side: labels.append(\"SNIPER\")\n    if sig.get(\"Trend\")==side and sig.get(\"Price Action\")==side: labels.append(\"TREND/MOMENTUM\")\n    if sig.get(\"Trend\")==side and sig.get(\"Supply/Demand\")==side: labels.append(\"PULLBACK\")\n    if sig.get(\"Liquidity\")==side and sig.get(\"Market Structure\")==side: labels.append(\"LIQUIDITY REVERSAL\")\n    if sig.get(\"Price Action\")==side and sig.get(\"Market Structure\")==side and sig.get(\"Trend\")!=side: labels.append(\"BREAKOUT/STRUCTURE SHIFT\")\n    if sig.get(\"RSI+MACD\")==side and sig.get(\"Trend\")!=side: labels.append(\"MEAN REVERSION\")\n    return labels or [\"MULTI-TF CONSENSUS\"]\ndef displacement_signal(rows):\n    \"\"\"Closed-candle displacement direction; neutral when the candle is not directional enough.\"\"\"\n    if len(rows) < 25 or not _displacement(rows):\n        return \"NEUTRAL\", \"no qualifying displacement\"\n    c = rows[-1]\n    if float(c[\"close\"]) > float(c[\"open\"]):\n        return \"BUY\", \"bullish displacement\"\n    if float(c[\"close\"]) < float(c[\"open\"]):\n        return \"SELL\", \"bearish displacement\"\n    return \"NEUTRAL\", \"flat displacement candle\"\ndef smc_core(rows):\n    \"\"\"Primary institutional SMC engine with structure, MSS, liquidity, FVG and validated displacement evidence.\"\"\"\n    structure_sig, structure_why = structure_signal(rows)\n    mss_sig, mss_why = smc_signal(rows)\n    liq_sig, liq_why = liquidity_signal(rows)\n    fvg_sig, fvg_why, fvg = fvg_signal(rows)\n    displacement_sig, displacement_why = displacement_signal(rows)\n    votes = [\n        (\"Structure\", structure_sig, structure_why),\n        (\"MSS/CHoCH\", mss_sig, mss_why),\n        (\"Liquidity\", liq_sig, liq_why),\n        (\"FVG\", fvg_sig, fvg_why),\n        (\"Displacement\", displacement_sig, displacement_why),\n    ]\n    candidates = []\n    for side in (\"BUY\", \"SELL\"):\n        aligned_names = [name for name, sig, _ in votes if sig == side]\n        opposed = sum(1 for _, sig, _ in votes if sig in (\"BUY\",\"SELL\") and sig != side)\n        has_structure = structure_sig == side\n        has_mss = mss_sig == side\n        has_liquidity = liq_sig == side\n        has_fvg = fvg_sig == side\n        has_displacement = displacement_sig == side\n        component_count = len(aligned_names)\n        structural_anchor = has_structure or has_mss\n        strong_combo = (\n            (has_mss and (has_structure or has_liquidity or has_fvg)) or\n            (has_structure and (has_liquidity or has_fvg or has_displacement)) or\n            (has_liquidity and has_fvg) or\n            (has_displacement and (has_mss or has_structure))\n        )\n        passed = structural_anchor and component_count >= 2 and opposed <= 1\n        score = (\n            (30 if has_mss else 0) +\n            (25 if has_fvg else 0) +\n            (25 if has_structure else 0) +\n            (20 if has_liquidity else 0) +\n            (12 if has_displacement else 0)\n        )\n        if strong_combo:\n            score += 5\n        score = min(100, score)\n        candidates.append((score, side, passed, component_count, opposed))\n    candidates.sort(key=lambda x: (x[0], x[3], -x[4]), reverse=True)\n    if not candidates or candidates[0][0] == 0:\n        return {\"side\":\"NEUTRAL\",\"passed\":False,\"score\":0,\"aligned\":0,\"opposed\":0,\"evidence\":votes,\"why\":\"no actionable SMC core\"}\n    top = candidates[0]\n    tied = [x for x in candidates if x[0] == top[0] and x[3] == top[3] and x[4] == top[4]]\n    if len(tied) > 1:\n        return {\n            \"side\":\"NEUTRAL\",\"passed\":False,\"score\":top[0],\"aligned\":top[3],\"opposed\":top[4],\n            \"evidence\":votes,\n            \"why\":\"SMC BUY/SELL evidence is tied; no directional edge\"\n        }\n    score, side, passed, aligned, opposed = top\n    if not passed:\n        return {\n            \"side\":side,\"passed\":False,\"score\":score,\"aligned\":aligned,\"opposed\":opposed,\n            \"evidence\":votes,\n            \"why\":\"SMC evidence is developing; at least two aligned SMC components with a structural anchor are required\"\n        }\n    return {\n        \"side\":side,\"passed\":True,\"score\":score,\"aligned\":aligned,\"opposed\":opposed,\n        \"evidence\":votes,\n        \"why\":\"primary SMC structure/MSS/liquidity/FVG gate passed\"\n    }\ndef setup_stage_from_evidence(smc, score, trigger_state=None, rr=None):\n    \"\"\"More signal visibility without weakening CONFIRMED_ENTRY gates.\"\"\"\n    if not smc or smc.get(\"side\") not in (\"BUY\", \"SELL\"):\n        return \"WATCH\"\n    aligned = int(smc.get(\"aligned\", 0) or 0)\n    if trigger_state and trigger_state.get(\"trigger_crossed\") and rr is not None and float(rr) >= MIN_CONFIRMED_RR:\n        return \"CONFIRMED_CANDIDATE\"\n    if bool(smc.get(\"passed\")) and score >= 70 and aligned >= 3:\n        return \"SETUP_READY\"\n    if bool(smc.get(\"passed\")) or (score >= 58 and aligned >= 2):\n        return \"DEVELOPING\"\n    return \"WATCH\"\ndef smc_setup_snapshot(rows):\n    \"\"\"Compact, auditable setup evidence used by fast watch/developing alerts.\"\"\"\n    try:\n        smc = smc_core(rows)\n        sweep = liquidity_sweep_setup(rows)\n        disp, disp_why = displacement_signal(rows)\n        return {\n            \"side\": smc.get(\"side\", \"NEUTRAL\"), \"score\": float(smc.get(\"score\", 0) or 0),\n            \"passed\": bool(smc.get(\"passed\")), \"aligned\": int(smc.get(\"aligned\", 0) or 0),\n            \"opposed\": int(smc.get(\"opposed\", 0) or 0), \"displacement\": disp,\n            \"liquidity_sweep\": sweep is not None,\n            \"stage\": setup_stage_from_evidence(smc, float(smc.get(\"score\",0) or 0)),\n            \"why\": smc.get(\"why\", \"\"), \"displacement_why\": disp_why,\n        }\n    except Exception as exc:\n        return {\"side\":\"NEUTRAL\",\"score\":0.0,\"passed\":False,\"aligned\":0,\"opposed\":0,\"stage\":\"WATCH\",\"why\":f\"snapshot error: {type(exc).__name__}\"}\ndef method_scores(rows):\n    \"\"\"Supporting confirmation layer only. SMC components are evaluated separately by smc_core.\"\"\"\n    methods = []\n    funcs = [\n        (\"Supply/Demand\", supply_demand_signal),\n        (\"Price Action\", candle_signal),\n        (\"Fibonacci\", fib_signal),\n        (\"Trend\", trend_signal),\n        (\"RSI+MACD\", indicator_signal),\n        (\"Session\", lambda x: session_signal(x, \"\")),\n    ]\n    for name, fn in funcs:\n        try:\n            sig, why = fn(rows)\n        except Exception as exc:\n            sig, why = \"NEUTRAL\", f\"method error: {type(exc).__name__}\"\n        points = 1 if sig in (\"BUY\", \"SELL\") else 0\n        methods.append({\"name\": name, \"signal\": sig, \"why\": why, \"points\": points})\n    return methods\ndef method_side_score(methods, side):\n    \"\"\"Directional evidence from supporting methods only; SMC remains primary.\"\"\"\n    if side not in (\"BUY\", \"SELL\"):\n        return 0.0\n    aligned = sum(1 for m in methods if m[\"signal\"] == side)\n    opposed = sum(1 for m in methods if m[\"signal\"] in (\"BUY\", \"SELL\") and m[\"signal\"] != side)\n    total = max(len(methods), 1)\n    return max(0.0, min(100.0, (aligned / total) * 100.0 - (opposed / total) * 20.0))\ndef dominant_signal(methods, minimum=3):\n    b = sum(m[\"points\"] for m in methods if m[\"signal\"] == \"BUY\")\n    s = sum(m[\"points\"] for m in methods if m[\"signal\"] == \"SELL\")\n    if b > s and b >= minimum:\n        return \"BUY\"\n    if s > b and s >= minimum:\n        return \"SELL\"\n    return \"NEUTRAL\"\ndef select_structural_target(targets, price, risk, min_rr=MIN_CONFIRMED_RR):\n    \"\"\"Return the nearest genuine opposing structural target that satisfies R:R.\n\n    The caller supplies targets in directional nearest-first order:\n    BUY  -> ascending highs above entry\n    SELL -> descending lows below entry\n\n    Preserve that order. Never sort a SELL target back into ascending order,\n    because that can accidentally choose a far-away target and make the plan\n    look better than the nearest real market objective.\n    \"\"\"\n    ordered = []\n    seen = set()\n    for raw in targets:\n        try:\n            x = float(raw)\n        except (TypeError, ValueError):\n            continue\n        if not math.isfinite(x) or x in seen:\n            continue\n        seen.add(x)\n        ordered.append(x)\n    if not ordered or risk <= 0:\n        return None\n    qualifying = [\n        x for x in ordered\n        if abs(x - float(price)) / float(risk) >= float(min_rr)\n    ]\n    # No qualifying target means there is no valid plan yet. Never return a\n    # sub-2R target and let a later stage accidentally treat it as tradable.\n    return qualifying[0] if qualifying else None\ndef structural_targets_from_rows(row_sets, side, price):\n    \"\"\"Collect genuine opposing swing targets across supplied CLOSED timeframes.\n\n    Entry and stop remain anchored to the execution timeframe; this function\n    only expands target discovery to higher confirmed market structure.\n    No synthetic/fabricated 2R target is ever created.\n    \"\"\"\n    candidates = []\n    seen = set()\n    for tf_rows in row_sets or []:\n        if not isinstance(tf_rows, list) or len(tf_rows) < 10:\n            continue\n        try:\n            highs, lows = swings(tf_rows[-200:], 2)\n        except Exception:\n            continue\n        raw_levels = ([x[1] for x in highs if x[1] > price] if side == \"BUY\"\n                      else [x[1] for x in lows if x[1] < price])\n        for raw in raw_levels:\n            try:\n                x = float(raw)\n            except (TypeError, ValueError):\n                continue\n            if math.isfinite(x) and x not in seen:\n                seen.add(x)\n                candidates.append(x)\n    return sorted(candidates) if side == \"BUY\" else sorted(candidates, reverse=True)\ndef levels(rows, side, entry_price=None):\n    \"\"\"Build SL and minimum required R:R TP only when a real opposing structural target supports 2R; otherwise return WAIT/TP_UNSUPPORTED.\"\"\"\n    a = atr(rows)\n    if a is None or a <= 0:\n        return None\n    if entry_price is not None:\n        try:\n            candidate_price = float(entry_price)\n        except (TypeError, ValueError):\n            candidate_price = rows[-1][\"close\"]\n        price = candidate_price if math.isfinite(candidate_price) and candidate_price > 0 else rows[-1][\"close\"]\n    else:\n        price = rows[-1][\"close\"]\n    try:\n        base_close = float(rows[-1][\"close\"])\n        if not math.isfinite(base_close) or base_close <= 0:\n            return None\n        relative_gap = abs(float(price) - base_close) / base_close\n        max_reasonable_gap = min(0.35, max(0.10, 12.0 * a / base_close))\n        if relative_gap > max_reasonable_gap:\n            return None\n    except (TypeError, ValueError):\n        return None\n    highs, lows = swings(rows[-100:], 2)\n    if side == \"BUY\":\n        stop_candidates = [x[1] for x in lows if x[1] < price]\n        if not stop_candidates:\n            return None\n        sl = max(stop_candidates[-3:]) - 0.25 * a\n        risk = price - sl\n        if risk <= 0:\n            return None\n        targets = sorted(set(x[1] for x in highs if x[1] > price))\n        structural_target = select_structural_target(targets, price, risk, MIN_CONFIRMED_RR)\n        if structural_target is None:\n            return price, sl, None, a\n        return price, sl, structural_target, a\n    if side == \"SELL\":\n        stop_candidates = [x[1] for x in highs if x[1] > price]\n        if not stop_candidates:\n            return None\n        sl = min(stop_candidates[-3:]) + 0.25 * a\n        risk = sl - price\n        if risk <= 0:\n            return None\n        targets = sorted(set(x[1] for x in lows if x[1] < price), reverse=True)\n        structural_target = select_structural_target(targets, price, risk, MIN_CONFIRMED_RR)\n        if structural_target is None:\n            return price, sl, None, a\n        return price, sl, structural_target, a\n    return None\ndef rr_ok(level):\n    if not level:\n        return False\n    entry, sl, tp, _ = level\n    if entry is None or sl is None or tp is None:\n        return False\n    risk = abs(entry-sl)\n    if risk <= 0:\n        return False\n    reward = abs(tp-entry)\n    return (reward / risk) >= (2.0 - 1e-9)\ndef candidate_plan(rows, side, current_price=None, symbol=None, target_rows=None):\n    \"\"\"Conditional plan from CLOSED candles.\n\n    Entry/SL come from the execution timeframe. TP discovery may use the\n    execution timeframe plus supplied higher-timeframe CLOSED structure.\n    A target must still be a real opposing swing and must achieve >=2R.\n    \"\"\"\n    if side not in (\"BUY\", \"SELL\") or not rows:\n        return None\n    av = atr(rows)\n    if not av or av <= 0:\n        return None\n    xau_mode = str(symbol or \"\").upper() == \"XAU/USD\"\n    xau_env = asset_environment(\"XAU/USD\") if xau_mode else {}\n    entry_buffer_atr = float(xau_env.get(\"entry_buffer_atr\", ENTRY_BUFFER_ATR)) if xau_mode else ENTRY_BUFFER_ATR\n    max_chase_atr = float(xau_env.get(\"max_chase_atr\", ENTRY_MAX_CHASE_ATR)) if xau_mode else ENTRY_MAX_CHASE_ATR\n    last = rows[-1]\n    reference = closed_breakout_level(rows, side)\n    if reference is None:\n        return None\n    trigger = float(reference + av * entry_buffer_atr) if side == \"BUY\" else float(reference - av * entry_buffer_atr)\n    level = levels(rows, side, entry_price=trigger)\n    if not level:\n        return None\n    entry, sl, structural_tp, atrv = level\n    risk = abs(entry-sl)\n    if risk <= 0:\n        return None\n    # Discover TP from genuine structure across 15m + higher confirmed TFs.\n    # The entry and SL are NOT moved to force the ratio.\n    target_sets = [rows]\n    if target_rows:\n        target_sets.extend(x for x in target_rows if x is not rows)\n    all_targets = structural_targets_from_rows(target_sets, side, entry)\n    structural_tp = select_structural_target(all_targets, entry, risk, MIN_CONFIRMED_RR)\n    # TP is the genuine structural target; calculate and report actual R:R.\n    tp = structural_tp\n    rr = abs(tp-entry)/max(risk,1e-12) if tp is not None else None\n    if tp is None:\n        return {\"entry\":entry,\"sl\":sl,\"tp\":None,\"structural_tp\":None,\"rr\":None,\"atr\":atrv,\"entry_trigger\":entry,\n                \"current_price\":None,\"trigger_crossed\":False,\"distance_atr\":None,\"entry_mode\":\"STRUCTURAL_BREAKOUT_FROM_CLOSED_CANDLE\",\n                \"reference_candle\":str(last.get(\"timestamp\")),\"status\":\"WAIT\",\"tp_status\":\"TP_UNSUPPORTED\",\"tp_reason\":\"No genuine opposing structural target is available\"}\n    if rr < MIN_CONFIRMED_RR:\n        return {\"entry\":entry,\"sl\":sl,\"tp\":None,\"structural_tp\":structural_tp,\"rr\":rr,\"atr\":atrv,\"entry_trigger\":entry,\n                \"current_price\":None,\"trigger_crossed\":False,\"distance_atr\":None,\"entry_mode\":\"STRUCTURAL_BREAKOUT_FROM_CLOSED_CANDLE\",\n                \"reference_candle\":str(last.get(\"timestamp\")),\"status\":\"WAIT\",\"tp_status\":\"TP_DEVELOPING\",\"tp_reason\":\"Genuine structural target exists but does not yet meet minimum required R:R\"}\n    current = None\n    if current_price is not None:\n        try:\n            candidate = float(current_price)\n            if math.isfinite(candidate) and candidate > 0:\n                current = candidate\n        except (TypeError, ValueError):\n            current = None\n    if current is None:\n        return {\"entry\":entry,\"sl\":sl,\"tp\":tp,\"structural_tp\":structural_tp,\"rr\":rr,\"atr\":atrv,\"entry_trigger\":entry,\n                \"current_price\":None,\"trigger_crossed\":False,\"distance_atr\":None,\n                \"entry_mode\":\"STRUCTURAL_BREAKOUT_FROM_CLOSED_CANDLE\",\n                \"reference_candle\":str(last.get(\"timestamp\")),\"status\":\"PENDING_LIVE_PRICE\"}\n    distance = abs(current-entry)/max(av,1e-12)\n    crossed = current >= entry if side == \"BUY\" else current <= entry\n    state = \"TRIGGERED\" if crossed and distance <= max_chase_atr else (\"OVEREXTENDED\" if crossed else \"PENDING\")\n    return {\"entry\":entry,\"sl\":sl,\"tp\":tp,\"structural_tp\":structural_tp,\"rr\":rr,\"atr\":atrv,\"entry_trigger\":entry,\n            \"current_price\":current,\"trigger_crossed\":crossed,\"distance_atr\":distance,\n            \"entry_mode\":\"STRUCTURAL_BREAKOUT_FROM_CLOSED_CANDLE\",\"reference_candle\":str(last.get(\"timestamp\")),\"status\":state}\ndef safe_call(fn, *args, **kwargs):\n    \"\"\"Bounded retry for transient LSE vault saturation/timeouts; never retries bad requests indefinitely.\"\"\"\n    last = None\n    for attempt in range(API_MAX_RETRIES):\n        try:\n            return fn(*args, **kwargs)\n        except Exception as exc:\n            last = exc\n            msg = str(exc).lower()\n            transient = (\n                \"503\" in msg or \"502\" in msg or \"504\" in msg or\n                \"timeout\" in msg or \"timed out\" in msg or\n                \"temporarily\" in msg or \"saturated\" in msg\n            )\n            if not transient or attempt == API_MAX_RETRIES - 1:\n                break\n            time.sleep(API_RETRY_BASE_SECONDS * (2 ** attempt))\n    return {\"_error\": f\"{type(last).__name__}: {last}\"}\ndef as_list(x):\n    if isinstance(x, list):\n        return x\n    if isinstance(x, dict):\n        for k in (\"data\", \"rows\", \"results\"):\n            if isinstance(x.get(k), list):\n                return x[k]\n    return []\ndef external_news():\n    \"\"\"Fetch a small, bounded RSS set without letting one feed stall the scan.\"\"\"\n    items = []\n    seen = set()\n    for url in NEWS_URLS[:8]:\n        try:\n            req = Request(\n                url,\n                headers={\"User-Agent\": \"LSE-Smart-Scanner/12.2\"},\n            )\n            with urlopen(req, timeout=8) as response:\n                raw = response.read(1_500_000)\n            feed = feedparser.parse(raw)\n            source = urlparse(url).netloc or url\n            if getattr(feed, \"bozo\", False) and not getattr(feed, \"entries\", []):\n                print(f\"RSS WARNING: {source}: malformed/empty feed\")\n                continue\n            for entry in feed.entries[:8]:\n                title = str(entry.get(\"title\", \"\")).strip()\n                link = str(entry.get(\"link\", \"\")).strip()\n                if not title:\n                    continue\n                key = (title.casefold(), link)\n                if key in seen:\n                    continue\n                seen.add(key)\n                published_at = None\n                for field in (\"published_parsed\", \"updated_parsed\"):\n                    parsed = entry.get(field)\n                    if parsed:\n                        try:\n                            published_at = datetime(*parsed[:6], tzinfo=timezone.utc).isoformat()\n                            break\n                        except Exception:\n                            pass\n                items.append({\n                    \"title\": title,\n                    \"link\": link,\n                    \"source\": source,\n                    \"published_at\": published_at,\n                })\n        except Exception as exc:\n            print(f\"RSS WARNING: {urlparse(url).netloc or url}: {type(exc).__name__}\")\n            continue\n    return items[:30]\ndef return_series(rows):\n    closes = [x[\"close\"] for x in rows if x.get(\"close\") is not None]\n    if len(closes) < 3:\n        return []\n    return [(closes[i] / closes[i-1]) - 1.0 for i in range(1, len(closes)) if closes[i-1] != 0]\ndef pearson(a, b):\n    n = min(len(a), len(b))\n    if n < 20:\n        return None\n    a, b = a[-n:], b[-n:]\n    ma, mb = statistics.fmean(a), statistics.fmean(b)\n    da = [x - ma for x in a]\n    db = [x - mb for x in b]\n    den = math.sqrt(sum(x*x for x in da) * sum(x*x for x in db))\n    return (sum(x*y for x, y in zip(da, db)) / den) if den else None\ndef correlation_context(symbol, rows, benchmark_rows):\n    base = return_series(rows)\n    vals = []\n    for bench, brow in benchmark_rows.items():\n        if bench == symbol:\n            continue\n        c = pearson(base, return_series(brow))\n        if c is not None:\n            vals.append((bench, c))\n    vals.sort(key=lambda x: abs(x[1]), reverse=True)\n    return vals[:3]\ndef daily_intelligence(client, symbols=None):\n    \"\"\"Load only macro/event context needed by the promoted candidate set.\n    Calendar data is contextual evidence, never a standalone trade signal.\n    \"\"\"\n    intel = {\"calendar\": [], \"macro\": {}, \"yields\": {}, \"cot\": {}, \"news\": [],\n             \"live\": \"UNKNOWN\", \"ml\": \"LOCAL STATISTICAL MODEL\"}\n    symbols = symbols or []\n    currencies = set()\n    assets = set()\n    for sym in symbols:\n        canonical = SYMBOL_ALIASES.get(str(sym).upper(), str(sym))\n        if \"/\" in canonical:\n            base, quote = canonical.split(\"/\", 1)\n            currencies.update((base, quote))\n            if base == \"XAU\" or quote == \"XAU\": assets.add(\"XAU\")\n            if base == \"XAG\" or quote == \"XAG\": assets.add(\"XAG\")\n            if base == \"BTC\" or quote == \"BTC\": assets.add(\"BTC\")\n    region_for = {\"USD\":\"US\",\"GBP\":\"GB\",\"EUR\":\"EU\",\"JPY\":\"JP\",\"AUD\":\"AU\",\"CAD\":\"CA\",\"NZD\":\"NZ\",\"CHF\":\"CH\"}\n    regions = [region_for[c] for c in sorted(currencies) if c in region_for]\n    for region in regions:\n        data = safe_call(client.economic_calendar, region=region, start=now_utc().date().isoformat())\n        if isinstance(data, dict) and \"_error\" in data:\n            continue\n        intel[\"calendar\"].extend(as_list(data))\n    if \"USD\" in currencies or assets & {\"XAU\",\"XAG\",\"BTC\"}:\n        intel[\"macro\"][\"cpi_yoy\"] = as_list(safe_call(client.economics, \"cpi_yoy\"))\n        intel[\"macro\"][\"policy_rate\"] = as_list(safe_call(client.series, \"fdtr\"))\n        intel[\"yields\"][\"US10Y\"] = as_list(safe_call(client.bond_yields, \"US10Y\"))\n    yield_codes = {\"EUR\":\"DE10Y\",\"GBP\":\"GB10Y\",\"JPY\":\"JP10Y\",\"AUD\":\"AU10Y\",\"CAD\":\"CA10Y\",\"NZD\":\"NZ10Y\",\"CHF\":\"CH10Y\"}\n    for cur in sorted(currencies):\n        code = yield_codes.get(cur)\n        if code:\n            rows = as_list(safe_call(client.series, code))\n            if rows:\n                intel[\"yields\"][code] = rows\n    if \"XAU\" in assets:\n        rows = as_list(safe_call(client.cot, \"GC\"))\n        if rows: intel[\"cot\"][\"GC\"] = rows[-1]\n    if \"BTC\" in assets:\n        rows = as_list(safe_call(client.cot, \"ES\"))\n        if rows: intel[\"cot\"][\"ES\"] = rows[-1]\n    intel[\"news\"] = external_news()\n    return intel\ndef latest_value(rows):\n    if not rows:\n        return None\n    row = rows[-1]\n    if isinstance(row, dict):\n        for key in (\"value\", \"close\", \"yield\", \"rate\"):\n            if row.get(key) is not None:\n                try:\n                    return float(row[key])\n                except Exception:\n                    return None\n    return None\nYIELD_CODES = {\n    \"USD\": \"US10Y\", \"EUR\": \"DE10Y\", \"GBP\": \"GB10Y\", \"JPY\": \"JP10Y\",\n    \"AUD\": \"AU10Y\", \"CAD\": \"CA10Y\", \"NZD\": \"NZ10Y\", \"CHF\": \"CH10Y\"\n}\ndef yield_context(intel, symbol):\n    if \"/\" not in symbol:\n        return None\n    base, quote = symbol.split(\"/\", 1)\n    if base not in YIELD_CODES or quote not in YIELD_CODES:\n        return None\n    b = latest_value(intel[\"yields\"].get(YIELD_CODES[base], []))\n    q = latest_value(intel[\"yields\"].get(YIELD_CODES[quote], []))\n    if b is None or q is None:\n        return None\n    return b - q\nRSS_HIGH_WORDS = (\n    \"emergency\", \"intervention\", \"bank failure\", \"bank collapse\",\n    \"default\", \"war\", \"invasion\", \"missile\", \"terror\", \"crisis\",\n    \"surprise rate\", \"rate hike\", \"rate cut\", \"fomc\", \"fed decision\",\n    \"ecb decision\", \"boe decision\", \"boj decision\", \"nfp\",\n    \"nonfarm payroll\", \"cpi\", \"inflation report\", \"jobs report\",\n    \"major tariff\", \"new tariffs\", \"sanctions\",\n)\nRSS_MEDIUM_WORDS = (\n    \"central bank\", \"interest rate\", \"monetary policy\", \"powell\",\n    \"lagarde\", \"bailey\", \"ueda\", \"employment\", \"unemployment\",\n    \"retail sales\", \"pmi\", \"gdp\", \"yield\", \"treasury\",\n    \"tariff\", \"trade war\", \"oil\", \"gold\", \"bitcoin\", \"crypto\",\n)\nRSS_ALIASES = {\n    \"EUR\": (\"euro\", \"eur\", \"europe\", \"european\", \"ecb\"),\n    \"USD\": (\"dollar\", \"usd\", \"us dollar\", \"fed\", \"federal reserve\", \"fomc\", \"treasury\"),\n    \"GBP\": (\"pound\", \"gbp\", \"sterling\", \"uk\", \"boe\", \"bank of england\"),\n    \"JPY\": (\"yen\", \"jpy\", \"japan\", \"boj\", \"bank of japan\"),\n    \"CHF\": (\"franc\", \"chf\", \"switzerland\", \"snb\"),\n    \"AUD\": (\"aussie\", \"aud\", \"australia\", \"rba\"),\n    \"CAD\": (\"cad\", \"canada\", \"canadian\", \"boc\", \"bank of canada\"),\n    \"NZD\": (\"nzd\", \"new zealand\", \"rbnz\"),\n}\nRSS_ASSET_ALIASES = {\n    \"XAU/USD\": (\"gold\", \"xau\"),\n    \"XAG/USD\": (\"silver\", \"xag\"),\n    \"XPT/USD\": (\"platinum\", \"xpt\"),\n    \"XPD/USD\": (\"palladium\", \"xpd\"),\n    \"BTC/USD\": (\"bitcoin\", \"btc\", \"crypto\"),\n}\ndef _rss_relevance(title, symbol):\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol))\n    t = title.casefold()\n    if canonical in RSS_ASSET_ALIASES:\n        terms = RSS_ASSET_ALIASES.get(canonical, ())\n        if \"/\" in canonical:\n            _, quote = canonical.split(\"/\", 1)\n            terms = terms + RSS_ALIASES.get(quote, ())\n    elif \"/\" in canonical:\n        base, quote = canonical.split(\"/\", 1)\n        terms = RSS_ALIASES.get(base, ()) + RSS_ALIASES.get(quote, ())\n    else:\n        terms = RSS_ALIASES.get(canonical, ())\n    if canonical == \"BTC/USD\":\n        terms = terms + (\"risk assets\", \"digital assets\", \"sec\")\n    return any(term.casefold() in t for term in terms)\ndef _rss_severity(title):\n    t = title.casefold()\n    if any(word in t for word in RSS_HIGH_WORDS):\n        return \"HIGH\"\n    if any(word in t for word in RSS_MEDIUM_WORDS):\n        return \"MEDIUM\"\n    return \"LOW\"\ndef event_risk(intel, symbol):\n    \"\"\"Combine calendar risk with RSS as secondary risk intelligence.\n    RSS is secondary risk intelligence: it may elevate risk severity, but it\n    never creates a BUY/SELL direction and never replaces the economic calendar.\n    \"\"\"\n    now = now_utc()\n    high = []\n    medium = []\n    rss_high = []\n    rss_medium = []\n    for e in intel.get(\"calendar\", []):\n        text_blob = \" \".join(\n            str(e.get(k, \"\")) for k in\n            (\"title\", \"event\", \"name\", \"impact\", \"importance\")\n        ).lower()\n        currency_text = \" \".join(\n            str(e.get(k, \"\")) for k in (\"currency\", \"country\", \"region\")\n        ).upper()\n        canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol))\n        symbol_currencies = set(canonical.split(\"/\")) if \"/\" in canonical else set()\n        relevant_currency = (\n            (not currency_text)\n            or any(c in currency_text for c in symbol_currencies)\n        )\n        if relevant_currency and any(w in text_blob for w in (\"high\", \"red\", \"major\")):\n            dt = None\n            for k in (\"timestamp\", \"datetime\", \"date\", \"time\"):\n                if e.get(k):\n                    try:\n                        dt = parse_ts(e[k])\n                        break\n                    except Exception:\n                        pass\n            if dt is None or abs((dt - now).total_seconds()) <= 24 * 3600:\n                high.append({\n                    \"source\": \"economic-calendar\",\n                    \"title\": str(e.get(\"title\") or e.get(\"event\") or e.get(\"name\") or \"High-impact event\"),\n                })\n    for item in intel.get(\"news\", []):\n        title = str(item.get(\"title\", \"\")).strip()\n        if not title or not _rss_relevance(title, symbol):\n            continue\n        published = item.get(\"published_at\")\n        if published:\n            try:\n                dt = parse_ts(published)\n                age = (now - dt).total_seconds()\n                if age > 36 * 3600 or age < -2 * 3600:\n                    continue\n            except Exception:\n                pass\n        severity = _rss_severity(title)\n        record = {\n            \"source\": item.get(\"source\", \"rss\"),\n            \"title\": title,\n            \"link\": item.get(\"link\", \"\"),\n            \"published_at\": published,\n        }\n        if severity == \"HIGH\":\n            rss_high.append(record)\n        elif severity == \"MEDIUM\":\n            rss_medium.append(record)\n    # RSS is secondary risk intelligence: merge only risk severity/events.\n    # It is deliberately not converted into a directional BUY/SELL vote.\n    high.extend(rss_high)\n    medium.extend(rss_medium)\n    if high:\n        return \"HIGH\", high[:8]\n    if medium:\n        return \"MEDIUM\", medium[:8]\n    return \"LOW\", []\nEVENT_POSITIVE_WORDS = (\"beat\", \"beats\", \"strong\", \"stronger\", \"above forecast\", \"above estimate\", \"hawkish\", \"rate hike\", \"hikes rates\", \"tightening\", \"growth\", \"surplus\")\nEVENT_NEGATIVE_WORDS = (\"miss\", \"misses\", \"weak\", \"weaker\", \"below forecast\", \"below estimate\", \"dovish\", \"rate cut\", \"cuts rates\", \"easing\", \"recession\", \"deficit\")\ndef _number(v):\n    try:\n        x = float(str(v).replace(\",\", \"\").replace(\"%\", \"\").strip())\n        return x if math.isfinite(x) else None\n    except Exception:\n        return None\ndef _event_surprise(e):\n    actual = next((_number(e.get(k)) for k in (\"actual\", \"actual_value\", \"result\", \"value\") if _number(e.get(k)) is not None), None)\n    expected = next((_number(e.get(k)) for k in (\"forecast\", \"consensus\", \"estimate\", \"expected\") if _number(e.get(k)) is not None), None)\n    return None if actual is None or expected is None else actual - expected\ndef _event_dt(e):\n    for k in (\"timestamp\", \"datetime\", \"date\", \"time\", \"published_at\"):\n        if e.get(k):\n            try: return parse_ts(e[k])\n            except Exception: pass\n    return None\ndef _event_direction(e, symbol):\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol))\n    currencies = set(canonical.split(\"/\")) if \"/\" in canonical else set()\n    cur_text = \" \".join(str(e.get(k, \"\")) for k in (\"currency\", \"country\", \"region\")).upper()\n    cur = next((c for c in currencies if c in cur_text), None)\n    if not cur:\n        return \"NEUTRAL\"\n    title = \" \".join(str(e.get(k, \"\")) for k in (\"title\", \"event\", \"name\")).casefold()\n    surprise = _event_surprise(e)\n    lower_is_better = (\"unemployment\" in title or \"jobless claims\" in title or\n                       \"initial claims\" in title or \"continuing claims\" in title)\n    higher_is_better = (\"retail sales\" in title or \"industrial production\" in title or\n                        \"pmi\" in title or \"manufacturing\" in title or \"gdp\" in title or\n                        \"employment\" in title or \"payroll\" in title)\n    hawkish = (\"rate hike\" in title or \"hikes rates\" in title or \"tightening\" in title or\n               \"hawkish\" in title or \"higher for longer\" in title)\n    dovish = (\"rate cut\" in title or \"cuts rates\" in title or \"easing\" in title or\n              \"dovish\" in title)\n    pos = any(w in title for w in EVENT_POSITIVE_WORDS)\n    neg = any(w in title for w in EVENT_NEGATIVE_WORDS)\n    if surprise is not None and abs(surprise) > 1e-12:\n        surprise_positive = surprise < 0 if lower_is_better else True if higher_is_better else None\n        if surprise_positive is not None:\n            pos, neg = surprise_positive, not surprise_positive\n    if hawkish:\n        pos, neg = True, False\n    elif dovish:\n        pos, neg = False, True\n    if pos == neg:\n        return \"NEUTRAL\"\n    bias = \"BUY\" if pos else \"SELL\"\n    base, quote = canonical.split(\"/\", 1)\n    return bias if cur == base else (\"SELL\" if bias == \"BUY\" else \"BUY\")\ndef event_alignment(intel, symbol, side, price_rows_5m, price_rows_15m):\n    \"\"\"Event-aware gate: only immediate high-impact shock blocks; post-event reaction can qualify.\"\"\"\n    now = now_utc(); records = []\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol))\n    currencies = set(canonical.split(\"/\")) if \"/\" in canonical else set()\n    for e in intel.get(\"calendar\", []):\n        if not isinstance(e, dict): continue\n        cur_text = \" \".join(str(e.get(k, \"\")) for k in (\"currency\", \"country\", \"region\")).upper()\n        if cur_text and not any(c in cur_text for c in currencies): continue\n        dt = _event_dt(e)\n        if dt is None: continue\n        delta = (dt-now).total_seconds()/60.0\n        if delta > 180 or delta < -180: continue\n        impact = \" \".join(str(e.get(k, \"\")) for k in (\"impact\", \"importance\", \"priority\")).upper()\n        sev = \"HIGH\" if any(x in impact for x in (\"HIGH\", \"RED\", \"MAJOR\")) else (\"MEDIUM\" if \"MED\" in impact else \"LOW\")\n        records.append({\"title\":str(e.get(\"title\") or e.get(\"event\") or e.get(\"name\") or \"Economic event\"),\"timestamp\":dt.isoformat(),\"severity\":sev,\"delta_min\":delta,\"direction\":_event_direction(e,symbol)})\n    records.sort(key=lambda x: abs(x[\"delta_min\"]))\n    strongest = next((x for x in records if x[\"severity\"] == \"HIGH\"), records[0] if records else None)\n    if strongest is None: return {\"state\":\"NONE\",\"gate\":True,\"score_adjustment\":0.0,\"events\":[],\"reason\":\"No timestamped relevant event in the execution window\"}\n    d=strongest[\"delta_min\"]\n    if strongest[\"severity\"] == \"HIGH\" and -10 <= d <= 15:\n        return {\"state\":\"IMMEDIATE_HIGH\",\"gate\":True,\"score_adjustment\":-20.0,\"events\":records[:5],\"reason\":\"Immediate high-impact release window; wait for initial volatility to settle\"}\n    if -120 <= d < -10:\n        def reaction(rows,bars):\n            if len(rows)<bars+2: return 0.0\n            a,b=rows[-bars-1][\"close\"],rows[-1][\"close\"]\n            return (b/a-1.0) if a else 0.0\n        r5,r15=reaction(price_rows_5m,3),reaction(price_rows_15m,2)\n        observed=\"BUY\" if r5>0 and r15>0 else (\"SELL\" if r5<0 and r15<0 else \"NEUTRAL\")\n        eb=strongest[\"direction\"]\n        if observed==side and eb in (\"NEUTRAL\",side): return {\"state\":\"POST_EVENT_ALIGNED\",\"gate\":True,\"score_adjustment\":12.2,\"events\":records[:5],\"reason\":f\"Post-event 5M/15M reaction {observed} aligns with {side}\"}\n        if observed in (\"BUY\",\"SELL\") and observed!=side: return {\"state\":\"POST_EVENT_CONFLICT\",\"gate\":True,\"score_adjustment\":-18.0,\"events\":records[:5],\"reason\":f\"Post-event 5M/15M reaction {observed} conflicts with {side}\"}\n        return {\"state\":\"POST_EVENT_UNCLEAR\",\"gate\":True,\"score_adjustment\":-5.0,\"events\":records[:5],\"reason\":\"Event passed but the 5M/15M reaction is not decisive\"}\n    if 15 < d <= 120:\n        return {\"state\":\"UPCOMING\",\"gate\":True,\"score_adjustment\":-5.0,\"events\":records[:5],\"reason\":\"Upcoming event is known; technical alignment remains eligible\"}\n    return {\"state\":\"OUTSIDE_IMMEDIATE_WINDOW\",\"gate\":True,\"score_adjustment\":0.0,\"events\":records[:5],\"reason\":\"Event is outside the immediate execution window\"}\ndef rss_event_risk(intel, symbol):\n    \"\"\"Return RSS-only severity/events for transparent secondary risk reporting.\n    RSS is secondary risk intelligence; this function never produces direction.\n    \"\"\"\n    now = now_utc()\n    high = []\n    medium = []\n    for item in intel.get(\"news\", []):\n        title = str(item.get(\"title\", \"\")).strip()\n        if not title or not _rss_relevance(title, symbol):\n            continue\n        published = item.get(\"published_at\")\n        if published:\n            try:\n                dt = parse_ts(published)\n                age = (now - dt).total_seconds()\n                if age > 36 * 3600 or age < -2 * 3600:\n                    continue\n            except Exception:\n                pass\n        severity = _rss_severity(title)\n        record = {\n            \"source\": item.get(\"source\", \"rss\"),\n            \"title\": title,\n            \"link\": item.get(\"link\", \"\"),\n            \"published_at\": published,\n        }\n        if severity == \"HIGH\":\n            high.append(record)\n        elif severity == \"MEDIUM\":\n            medium.append(record)\n    if high:\n        return \"HIGH\", high[:8]\n    if medium:\n        return \"MEDIUM\", medium[:8]\n    return \"LOW\", []\ndef dominant(methods, minimum=2):\n    \"\"\"Return a directional consensus; default/authoritative minimum is 2 distinct strategies.\"\"\"\n    required = max(2, int(minimum or 2))\n    b = sum(1 for m in methods if m.get(\"signal\") == \"BUY\")\n    ss = sum(1 for m in methods if m.get(\"signal\") == \"SELL\")\n    if b >= required and b > ss:\n        return \"BUY\"\n    if ss >= required and ss > b:\n        return \"SELL\"\n    return \"NEUTRAL\"\ndef side_score(strategy_maps, side):\n    \"\"\"Fast, bounded score: breadth + timeframe agreement, not a fake deep score.\"\"\"\n    names, tfs, opposed = set(), set(), set()\n    for tf_name, methods in strategy_maps.items():\n        for m in methods:\n            sig = m.get(\"signal\")\n            if sig == side:\n                names.add(m.get(\"name\")); tfs.add(tf_name)\n            elif sig in (\"BUY\",\"SELL\") and sig != side:\n                opposed.add(m.get(\"name\"))\n    local = [tf for tf, methods in strategy_maps.items() if dominant(methods, 2) == side]\n    adaptive_state = adaptive_market_state(strategy_maps, \"\")\n    adaptive_score = adaptive_consensus_score(strategy_maps, side, adaptive_state)\n    base_score = 35.0 + len(names)*7.0 + max(0, len(tfs)-1)*5.0 + len(local)*6.0 - len(opposed)*2.0\n    score = max(base_score, adaptive_score)\n    return {\"side\":side, \"strategy_count\":len(names), \"strategies\":sorted(x for x in names if x),\n            \"timeframes\":sorted(tfs), \"local_consensus_tfs\":local,\n            \"opposed\":sorted(x for x in opposed if x), \"score\":round(min(100.0, max(0.0, score)),1)}\napi_key = os.getenv(\"LSE_API_KEY\", \"\").strip()\nif not api_key:\n    raise RuntimeError(\"LSE_API_KEY secret is missing. Add it in GitHub Settings -> Secrets and variables -> Actions.\")\nclient = LSE(api_key=api_key)\nintel = {\"calendar\": [], \"macro\": {}, \"yields\": {}, \"cot\": {}, \"news\": [],\n         \"live\": \"DEFERRED\", \"ml\": \"LOCAL STATISTICAL MODEL\"}\nbenchmark_rows = {}\ndef ensure_benchmarks():\n    global benchmark_rows\n    if benchmark_rows:\n        return\n    for benchmark in (\"EUR/USD\", \"GBP/USD\", \"USD/JPY\", \"XAU/USD\", \"BTC/USD\"):\n        try:\n            benchmark_rows[benchmark] = fetch_candles(client, benchmark, \"1h\", limit=120)\n        except Exception as exc:\n            benchmark_rows[benchmark] = []\n            print(f\"  BENCHMARK WARNING {benchmark}: {type(exc).__name__}: {exc}\")\ncatalog = []\ncatalog_errors = []\ntry:\n    rows = client.catalog()\n    if isinstance(rows, dict):\n        rows = rows.get(\"data\", rows.get(\"results\", []))\n    if isinstance(rows, list):\n        for row in rows:\n            if isinstance(row, dict):\n                sym = row.get(\"symbol\")\n                if sym:\n                    catalog.append({\n                        \"symbol\": sym,\n                        \"name\": row.get(\"name\", row.get(\"display_name\", sym)),\n                        \"category\": str(row.get(\"category\", \"\")).strip().lower(),\n                        \"tick_count\": row.get(\"ticks\", row.get(\"tick_count\", 0)),\n                    })\n    else:\n        catalog_errors.append(\"catalog(): unexpected response shape\")\nexcept Exception as exc:\n    catalog_errors.append(f\"catalog(): {type(exc).__name__}: {exc}\")\ncategory_aliases = {\"stocks\":\"stock\",\"stock\":\"stock\",\"forex\":\"forex\",\"crypto\":\"crypto\",\"etfs\":\"etf\",\"etf\":\"etf\",\"commodities\":\"commodity\",\"commodity\":\"commodity\",\"metals\":\"commodity\",\"metal\":\"commodity\",\"indices\":\"index\",\"index\":\"index\",\"futures\":\"futures\",\"future\":\"futures\",\"options\":\"options\"}\nrequested_categories = {category_aliases.get(x.strip().lower(), x.strip().lower()) for x in CATEGORIES if x.strip()}\n# v12.63.11 authoritative scanner universe: exactly 20 FX pairs + XAU/USD + BTC/USD.\n# Every symbol is independently gated; no pair is forced into an entry.\nTARGET_SYMBOLS = tuple(sorted(MAJOR_FX | {\"XAU/USD\",\"BTC/USD\"}))\nTARGET_SYMBOL_SET = set(TARGET_SYMBOLS)\nunique = {}\nfor row in catalog:\n    raw_symbol = str(row[\"symbol\"]).strip()\n    canonical = SYMBOL_ALIASES.get(raw_symbol.upper(), raw_symbol)\n    if canonical not in TARGET_SYMBOL_SET:\n        continue\n    normalized = category_aliases.get(str(row.get(\"category\", \"\")).strip().lower(), str(row.get(\"category\", \"\")).strip().lower())\n    # v12.53.2: the study universe is authoritative. The optional category input\n    # must never remove one of the four required symbols.\n    if not canonical:\n        continue\n    item = dict(row)\n    item[\"symbol\"] = raw_symbol\n    item[\"canonical_symbol\"] = canonical\n    item[\"category\"] = normalized\n    try:\n        item[\"_tick_count\"] = int(item.get(\"tick_count\", 0) or 0)\n    except Exception:\n        item[\"_tick_count\"] = 0\n    prev = unique.get(canonical)\n    if prev is None or (raw_symbol.upper() == canonical and prev[\"symbol\"].upper() != canonical):\n        unique[canonical] = item\npriority_order = sorted(TARGET_SYMBOL_SET)\npriority_rank = {s:i for i,s in enumerate(priority_order)}\ninstruments = sorted(unique.values(), key=lambda x:(TARGET_SYMBOLS.index(x[\"canonical_symbol\"]), x[\"symbol\"]))\n# v12.63.11 authoritative universe: every discovered member is equally eligible; no core/priority ranking affects eligibility.\n# MAX_INSTRUMENTS remains an explicit optional safety cap.\nif MAX_INSTRUMENTS > 0:\n    # Safety cap may limit non-FX assets, but it must never remove any of the mandatory 20 FX pairs.\n    fx_instruments = [x for x in instruments if x.get(\"canonical_symbol\") in MAJOR_FX]\n    non_fx_instruments = [x for x in instruments if x.get(\"canonical_symbol\") not in MAJOR_FX]\n    instruments = fx_instruments + non_fx_instruments[:MAX_INSTRUMENTS]\nmissing = sorted(TARGET_SYMBOL_SET - {x[\"canonical_symbol\"] for x in instruments})\nif missing:\n    print(f\"CATALOG NOTE: {len(missing)} configured focused symbols unavailable in the live catalog; they will not be fabricated.\")\nfor item in instruments:\n    item.pop(\"_tick_count\",None)\nif not instruments:\n    raise RuntimeError(\n        \"No focused instruments were discovered in the live LSE catalog. \"\n        \"The scanner will not fabricate symbols or candles. Check catalog access, \"\n        \"symbol availability, or LSE_API_KEY permissions.\"\n    )\nprint(f\"CATALOG CATEGORIES REQUESTED: {', '.join(CATEGORIES)}\")\nprint(\"UNIVERSE: 20 FX PAIRS + XAU/USD + BTC/USD — ALL EQUALLY ELIGIBLE\")\nprint(f\"FOCUSED SYMBOLS CONFIGURED: {len(TARGET_SYMBOLS)} | discovered in live catalog: {len(instruments)}\")\nprint(f\"DATA OWNERSHIP: strategy_scan={len(TARGET_SYMBOLS)} symbols incl. XAU/USD + BTC/USD\")\nprint(f\"MAX_INSTRUMENTS safety cap: {MAX_INSTRUMENTS if MAX_INSTRUMENTS > 0 else 'disabled'}\")\nprint(f\"RUNTIME BUDGET: {RUNTIME_MINUTES:.1f} minutes\")\ndiscovered = {x[\"canonical_symbol\"] for x in instruments}\nfor label, group in ((\"MAJOR FX\",MAJOR_FX),(\"METALS\",PRIORITY_METALS),(\"BTC\",PRIORITY_CRYPTO)):\n    found = sorted(group & discovered)\n    missing_group = sorted(group - discovered)\n    print(f\"{label} FOUND: {', '.join(found) if found else 'NONE'}\")\n    if missing_group:\n        print(f\"{label} NOT IN LIVE CATALOG: {', '.join(missing_group)}\")\nif catalog_errors:\n    print(\"CATALOG WARNINGS:\")\n    for x in catalog_errors:\n        print(\" -\",x)\nprint()\ndef _pair_return(rows, lookback=24):\n    closes = [float(r[\"close\"]) for r in rows[-lookback:] if r.get(\"close\") is not None]\n    if len(closes) < 2 or closes[0] == 0:\n        return None\n    return (closes[-1] / closes[0]) - 1.0\ndef build_currency_strength(pair_rows):\n    \"\"\"Relative major-currency strength from the same closed 1H bars used by the scanner.\"\"\"\n    scores = {c: [] for c in MAJOR_FX_CURRENCIES}\n    for pair, rows in pair_rows.items():\n        if \"/\" not in pair:\n            continue\n        base, quote = pair.split(\"/\", 1)\n        if base not in scores or quote not in scores:\n            continue\n        r = _pair_return(rows, 24)\n        if r is None or not math.isfinite(r):\n            continue\n        scores[base].append(r)\n        scores[quote].append(-r)\n    raw = {c: (statistics.fmean(v) if v else None) for c, v in scores.items()}\n    valid = [v for v in raw.values() if v is not None]\n    if not valid:\n        return {\"ranked\": [], \"scores\": raw}\n    lo, hi = min(valid), max(valid)\n    ranked = []\n    for c, v in raw.items():\n        if v is None:\n            continue\n        pct = 50.0 if hi == lo else 100.0 * (v - lo) / (hi - lo)\n        ranked.append({\"currency\": c, \"strength\": round(v * 10000.0, 3), \"score\": round(pct, 1)})\n    ranked.sort(key=lambda x: x[\"score\"], reverse=True)\n    return {\"ranked\": ranked, \"scores\": raw}\ndef currency_pair_alignment(currency_strength, symbol, side):\n    canonical = SYMBOL_ALIASES.get(str(symbol).upper(), str(symbol).upper())\n    if \"/\" not in canonical or side not in (\"BUY\", \"SELL\"):\n        return {\"state\": \"UNAVAILABLE\", \"score\": 0.0}\n    base, quote = canonical.split(\"/\", 1)\n    raw = currency_strength.get(\"scores\", {})\n    b, q = raw.get(base), raw.get(quote)\n    if b is None or q is None:\n        return {\"state\": \"UNAVAILABLE\", \"score\": 0.0}\n    edge = (b - q) if side == \"BUY\" else (q - b)\n    if edge > 0:\n        return {\"state\": \"ALIGNED\" if abs(edge) >= 0.00015 else \"MARGINAL\", \"score\": round(min(8.0, abs(edge) * 10000.0), 2)}\n    return {\"state\": \"CONFLICT\", \"score\": round(-min(8.0, abs(edge) * 10000.0), 2)}\nfast_watch = []\n# FULL-UNIVERSE INSTITUTIONAL PASS\n# Every configured FX pair is staged for analysis. No predictive score/filter is allowed to remove a pair.\n# The 1H staging read is data preparation only; it does not promote, reject, rank, or gate instruments.\nfast_candidates = [{\"symbol\": m.get(\"canonical_symbol\"), \"score\": 100.0, \"status\": \"FULL_DEEP_PASS\"}\n                   for m in instruments if m.get(\"canonical_symbol\") in TOP_OPPORTUNITY_SYMBOLS]\nfast_watch = []\nfast_errors = 0\nreserve_pool = []\nfx_strength_rows = {}\nfor meta in instruments:\n    canonical = str(meta.get(\"canonical_symbol\") or meta.get(\"symbol\") or \"\").upper()\n    if canonical not in TOP_OPPORTUNITY_SYMBOLS:\n        continue\n    symbol = meta[\"symbol\"]\n    try:\n        rows, _ = fetch_candles_with_forming(client, symbol, \"1h\", limit=FETCH_LIMIT[\"1h\"])\n        if len(rows) >= 60:\n            fx_strength_rows[canonical] = list(rows)\n    except Exception as exc:\n        fast_errors += 1\n        print(f\"1H DATA STAGING ERROR {symbol}: {type(exc).__name__}\")\n\ntop_opportunity_candidates = sorted(\n    [m for m in instruments if m.get(\"canonical_symbol\") in TOP_OPPORTUNITY_SYMBOLS],\n    key=lambda x: (x.get(\"canonical_symbol\", \"\"), x.get(\"symbol\", \"\"))\n)\nopportunity_symbols_found = {m.get(\"canonical_symbol\") for m in top_opportunity_candidates}\nopportunity_symbols_missing = set(TOP_OPPORTUNITY_SYMBOLS) - opportunity_symbols_found\nprint(f\"INSTITUTIONAL UNIVERSE PASS: {len(top_opportunity_candidates)}/{len(TOP_OPPORTUNITY_SYMBOLS)} FX pairs queued for identical deep analysis\")\n\nprint(\n    f\"TOP OPPORTUNITY UNIVERSE: {', '.join(sorted(TOP_OPPORTUNITY_SYMBOLS))} | \"\n    f\"found: {', '.join(sorted(opportunity_symbols_found)) or 'NONE'} | \"\n    f\"missing: {', '.join(sorted(opportunity_symbols_missing)) or 'NONE'}\"\n)\nif top_opportunity_candidates:\n    intel = daily_intelligence(client, [m[\"symbol\"] for m in top_opportunity_candidates])\n    print(f\"DEEP INTELLIGENCE: {len(intel.get('calendar', []))} calendar events | {len(intel.get('news', []))} news items\")\nresults = []\ncomplete = 0\nincomplete = 0\nerrors = 0\nprocessed = 0\nruntime_limited = False\nensure_benchmarks()\ndeep_live_raw = collect_live_quotes(client, [m[\"symbol\"] for m in top_opportunity_candidates]) if top_opportunity_candidates else {}\nfor idx, meta in enumerate(top_opportunity_candidates, 1):\n    if time.monotonic() >= SCAN_DEADLINE:\n        runtime_limited = True\n        print(\"⏱️ SOFT RUNTIME TARGET EXCEEDED — continuing mandatory full-universe FX analysis.\")\n    symbol = meta[\"symbol\"]\n    category = meta[\"category\"]\n    print(f\"[DEEP {idx}/{len(top_opportunity_candidates)}] {symbol} ({category})\")\n    data = {}\n    forming = {}\n    failed = []\n    for tf in TF:\n        try:\n            rows, current = fetch_candles_with_forming(client, symbol, tf)\n            print(f\"  {tf}: {len(rows)} fresh closed candles\" + (\" + forming\" if current else \"\"))\n            data[tf] = rows\n            forming[tf] = current\n        except Exception as exc:\n            data[tf] = []\n            failed.append(f\"{tf}: {type(exc).__name__}: {exc}\")\n            print(f\"  {tf}: ERROR {type(exc).__name__}\")\n    if failed:\n        errors += len(failed)\n    missing = [tf for tf in TF if len(data.get(tf, [])) < MIN_BARS[tf]]\n    if missing:\n        incomplete += 1\n        processed += 1\n        print(f\"  SKIP: incomplete timeframes: {', '.join(missing)}\")\n        continue\n    live_ticks_for_check = deep_live_raw.get(symbol, [])\n    consistent, consistency_reason = price_feed_consistent(data.get(\"15m\", []), live_ticks_for_check)\n    if not consistent:\n        errors += 1\n        processed += 1\n        print(f\"  SKIP: PRICE DATA CONSISTENCY FAILURE — {consistency_reason}\")\n        continue\n    complete += 1\n    processed += 1\n    monthly = data[\"1mo\"]\n    d = data[\"1d\"]\n    h4 = data[\"4h\"]\n    h1 = data[\"1h\"]\n    m15 = data[\"15m\"]\n    m5 = data[\"5m\"]\n    monthly_dir = direction(monthly)\n    d_dir = direction(d)\n    h4_dir = direction(h4)\n    h1_dir = direction(h1)\n    h1_methods = method_scores(h1)\n    m15_methods = method_scores(m15)\n    m5_methods = method_scores(m5)\n    m15_fvg_sig, m15_fvg_why, m15_fvg = fvg_signal(m15)\n    m5_fvg_sig, m5_fvg_why, m5_fvg = fvg_signal(m5)\n    quality = {tf: data_quality_report(data[tf]) for tf in TF}\n    volatility = {tf: volatility_regime(data[tf]) for tf in (\"4h\",\"1h\",\"15m\",\"5m\")}\n    sniper = sniper_opportunity(m5, m15, symbol=symbol, category=category)\n    def counts(methods, side):\n        aligned = sum(1 for m in methods if m[\"signal\"] == side)\n        opposed = sum(1 for m in methods if m[\"signal\"] in (\"BUY\",\"SELL\") and m[\"signal\"] != side)\n        neutral = sum(1 for m in methods if m[\"signal\"] == \"NEUTRAL\")\n        return aligned, opposed, neutral\n    smc_monthly = smc_core(monthly)\n    smc_1d = smc_core(d)\n    smc_4h = smc_core(h4)\n    smc_1h = smc_core(h1)\n    smc_15m = smc_core(m15)\n    smc_5m = smc_core(m5)\n    h1_support = method_scores(h1)\n    m15_support = method_scores(m15)\n    m5_support = method_scores(m5)\n    strategy_by_tf = {\n        \"1mo\": nine_strategy_signals(monthly, category, smc_monthly),\n        \"1d\": nine_strategy_signals(d, category, smc_1d),\n        \"4h\": nine_strategy_signals(h4, category, smc_4h),\n        \"1h\": nine_strategy_signals(h1, category, smc_1h),\n        \"15m\": nine_strategy_signals(m15, category, smc_15m),\n        \"5m\": nine_strategy_signals(m5, category, smc_5m),\n    }\n    strategy_matches = strategy_catalog(data, symbol, currency_strength)\n    def active_strategy_summary(side):\n        catalog_matches=[x for x in strategy_matches if x.get(\"side\")==side]\n        summary=strategy_summary(catalog_matches,side)\n        # Only strategies agreeing on the same side count; timeframe breadth is separate.\n        return summary\n    strategy_by_side = {s: active_strategy_summary(s) for s in (\"BUY\",\"SELL\")}\n    price_method_names = {\"Supply/Demand\",\"Price Action\",\"Fibonacci\",\"Trend\",\"RSI+MACD\"}\n    tf_consensus = {tf_name: dominant(strategy_by_tf.get(tf_name, []), minimum=2) for tf_name in TF}\n    regime_side = tf_consensus.get(\"4h\", \"NEUTRAL\")\n    monthly_side = tf_consensus.get(\"1mo\", \"NEUTRAL\")\n    operational_signal_tfs = {side_c:[tf_name for tf_name in TF if tf_name!=\"1mo\" and tf_consensus.get(tf_name)==side_c] for side_c in (\"BUY\",\"SELL\")}\n    def monthly_alignment_ok(side_c):\n        return monthly_side == side_c\n    def signal_timeframes(side_c):\n        return [tf for tf in POSITION_TIMEFRAMES if tf_consensus.get(tf)==side_c]\n    def timeframe_local_stats(tf_name, side_c):\n        methods=strategy_by_tf.get(tf_name,[])\n        aligned=sorted(set(m[\"name\"] for m in methods if m[\"signal\"]==side_c))\n        opposed=sorted(set(m[\"name\"] for m in methods if m[\"signal\"] in (\"BUY\",\"SELL\") and m[\"signal\"]!=side_c))\n        return {\"timeframe\":tf_name,\"side\":side_c,\"aligned\":aligned,\"opposed\":opposed,\n                \"aligned_count\":len(aligned),\"opposed_count\":len(opposed),\n                \"local_consensus\":dominant(methods,minimum=2)==side_c}\n    def build_timeframe_analysis(side_c):\n        analysis=[]\n        for tf_name in POSITION_TIMEFRAMES:\n            profile=POSITION_TIMEFRAME_PROFILES[tf_name]; stats=timeframe_local_stats(tf_name,side_c); context=[]\n            for ctx_tf in profile[\"context\"]:\n                if dominant(strategy_by_tf.get(ctx_tf,[]),minimum=2)==side_c: context.append(ctx_tf)\n            level=levels(data.get(tf_name,[]),side_c) if stats[\"local_consensus\"] else None\n            rr_value = (\n                abs(float(level[2]) - float(level[0])) / max(abs(float(level[0]) - float(level[1])), 1e-12)\n                if level and len(level) >= 3 and level[0] is not None and level[1] is not None and level[2] is not None\n                else None\n            )\n            ready=stats[\"aligned_count\"]>=2 and stats[\"local_consensus\"] and len(context)>=profile[\"min_context_tfs\"]\n            analysis.append({\"timeframe\":tf_name,\"side\":side_c,\"trade_type\":profile_trade_type(tf_name,stats[\"aligned_count\"],len(context)),\"strategies\":stats[\"aligned\"],\"aligned_count\":stats[\"aligned_count\"],\"opposed\":stats[\"opposed\"],\"context_timeframes\":context,\"monthly_agreement\":monthly_side==side_c,\"level\":level,\"rr\":rr_value,\"ready\":ready,\"status\":\"READY\" if ready else \"WAIT\"})\n        return analysis\n    price_method_names = {\n        \"Supply/Demand\",\"Price Action\",\"Fibonacci\",\"Trend\",\"RSI+MACD\"\n    }\n    tf_consensus = {\n        tf_name: next(\n            (m.get(\"signal\",\"NEUTRAL\") for m in strategy_by_tf.get(tf_name, [])\n             if m.get(\"name\")==ACTIVE_STRATEGY_NAME),\n            \"NEUTRAL\"\n        ) if INSTITUTIONAL_ENGINE_ACTIVE else \"NEUTRAL\"\n        for tf_name in TF\n    }\n    regime_side = tf_consensus.get(\"4h\", \"NEUTRAL\")\n    monthly_side = tf_consensus.get(\"1mo\", \"NEUTRAL\")\n    operational_signal_tfs = {\n        side_c: [\n            tf_name for tf_name in TF\n            if tf_name != \"1mo\" and tf_consensus.get(tf_name) == side_c\n        ]\n        for side_c in (\"BUY\", \"SELL\")\n    }\n    def monthly_alignment_ok(side_c):\n        return monthly_side == side_c\n    def signal_timeframes(side_c):\n        return [\n            tf for tf in POSITION_TIMEFRAMES\n            if tf_consensus.get(tf) == side_c\n        ]\n    def timeframe_local_stats(tf_name, side_c):\n        methods = strategy_by_tf.get(tf_name, [])\n        aligned = [m[\"name\"] for m in methods if m[\"signal\"] == side_c]\n        opposed = [m[\"name\"] for m in methods if m[\"signal\"] in (\"BUY\", \"SELL\") and m[\"signal\"] != side_c]\n        local_side = dominant(methods, minimum=2)\n        return {\n            \"timeframe\": tf_name,\n            \"side\": side_c,\n            \"aligned\": sorted(set(aligned)),\n            \"opposed\": sorted(set(opposed)),\n            \"aligned_count\": len(set(aligned)),\n            \"opposed_count\": len(set(opposed)),\n            \"local_consensus\": local_side == side_c,\n        }\n    def build_timeframe_analysis(side_c):\n        analysis = []\n        for tf_name in POSITION_TIMEFRAMES:\n            profile = POSITION_TIMEFRAME_PROFILES[tf_name]\n            stats = timeframe_local_stats(tf_name, side_c)\n            context = []\n            for ctx_tf in profile[\"context\"]:\n                ctx_methods = strategy_by_tf.get(ctx_tf, [])\n                if dominant(ctx_methods, minimum=2) == side_c:\n                    context.append(ctx_tf)\n            level = levels(data.get(tf_name, []), side_c) if stats[\"local_consensus\"] else None\n            rr_value = None\n            if (\n                level and len(level) >= 3 and\n                level[0] is not None and level[1] is not None and level[2] is not None\n            ):\n                try:\n                    rr_value = abs(float(level[2]) - float(level[0])) / max(\n                        abs(float(level[0]) - float(level[1])), 1e-12\n                    )\n                except (TypeError, ValueError):\n                    rr_value = None\n            context_ok = len(context) >= profile[\"min_context_tfs\"]\n            monthly_ok = True  # 1MO is informational only.\n            ready = (\n                stats[\"aligned_count\"] >= profile[\"min_strategies\"] and\n                stats[\"local_consensus\"] and\n                context_ok\n            )\n            analysis.append({\n                \"timeframe\": tf_name,\n                \"side\": side_c,\n                \"trade_type\": profile_trade_type(tf_name, stats[\"aligned_count\"], len(context)),\n                \"strategies\": stats[\"aligned\"],\n                \"aligned_count\": stats[\"aligned_count\"],\n                \"opposed\": stats[\"opposed\"],\n                \"context_timeframes\": context,\n                \"monthly_agreement\": monthly_side == side_c,\n                \"level\": level,\n                \"rr\": rr_value,\n                \"ready\": ready,\n                \"status\": \"READY\" if ready else \"WAIT\",\n            })\n        return analysis\n    def consensus_stats(side_c):\n        aligned_names = set()\n        opposed_names = set()\n        aligned_tfs = set()\n        opposed_tfs = set()\n        per_strategy_tfs = {}\n        for tf_name, methods in strategy_by_tf.items():\n            for m in methods:\n                if m[\"signal\"] == side_c:\n                    aligned_names.add(m[\"name\"])\n                    aligned_tfs.add(tf_name)\n                    per_strategy_tfs.setdefault(m[\"name\"], set()).add(tf_name)\n                elif m[\"signal\"] in (\"BUY\",\"SELL\") and m[\"signal\"] != side_c:\n                    opposed_names.add(m[\"name\"])\n                    opposed_tfs.add(tf_name)\n        price_names = aligned_names & price_method_names\n        price_tfs = set()\n        for tf_name, methods in strategy_by_tf.items():\n            if any(m[\"name\"] in price_method_names and m[\"signal\"] == side_c for m in methods):\n                price_tfs.add(tf_name)\n        return {\n            \"aligned_names\": aligned_names,\n            \"opposed_names\": opposed_names,\n            \"aligned_count\": len(aligned_names),\n            \"opposed_count\": len(opposed_names),\n            \"aligned_tfs\": aligned_tfs,\n            \"opposed_tfs\": opposed_tfs,\n            \"tf_count\": len(aligned_tfs),\n            \"price_names\": price_names,\n            \"price_count\": len(price_names),\n            \"price_tfs\": price_tfs,\n            \"price_tf_count\": len(price_tfs),\n            \"per_strategy_tfs\": per_strategy_tfs,\n        }\n    def closed_entry_price(rows):\n        if not rows:\n            return None\n        try:\n            value = float(rows[-1].get(\"close\"))\n        except (TypeError, ValueError):\n            return None\n        return value if math.isfinite(value) and value > 0 else None\n    def logical_pending_entry(rows, side):\n        \"\"\"Calculate a non-forced institutional entry from confirmed structure.\"\"\"\n        if not rows or len(rows) < 20 or side not in (\"BUY\",\"SELL\"):\n            return None\n        highs, lows = swings(rows[-80:], 2)\n        if side == \"BUY\" and lows:\n            ref = float(lows[-1][1]); high = max(float(r[\"high\"]) for r in rows[-10:])\n            return ref + (high - ref) * 0.5\n        if side == \"SELL\" and highs:\n            ref = float(highs[-1][1]); low = min(float(r[\"low\"]) for r in rows[-10:])\n            return ref - (ref - low) * 0.5\n        return None\n    def level_rr(c, strategy=\"INTRADAY\", entry_price=None):\n        execution_rows = {\n            \"INTRADAY\": m15,\n            \"SCALP\": m5,\n            \"SWING/HOLD\": h1,\n        }.get(strategy, m15)\n        if entry_price is None:\n            entry_price = logical_pending_entry(execution_rows, c)\n        level = levels(execution_rows, c, entry_price=entry_price)\n        if not level:\n            return None, None\n        entry, sl, tp, a = level\n        risk = abs(entry-sl)\n        if tp is None or risk <= 0:\n            return level, None\n        rr = abs(tp-entry) / risk\n        return level, rr\n    def target_ladder(rows, side, entry):\n        \"\"\"Return up to three real opposing swing targets beyond the reference entry.\"\"\"\n        if not rows or entry is None:\n            return []\n        highs, lows = swings(rows[-120:], 2)\n        if side == \"BUY\":\n            targets = sorted({float(x[1]) for x in highs if x[1] > entry})\n        elif side == \"SELL\":\n            targets = sorted({float(x[1]) for x in lows if x[1] < entry}, reverse=True)\n        else:\n            return []\n        return targets[:3]\n    def support_score(methods, side_c):\n        aligned, opposed, neutral = counts(methods, side_c)\n        return max(0.0, min(100.0,\n            (aligned / max(len(methods), 1)) * 100.0 -\n            (opposed / max(len(methods), 1)) * 20.0))\n    def support_reasons(methods, side_c):\n        return [f\"{m['name']}: {m['why']}\" for m in methods if m[\"signal\"] == side_c]\n    event_level, events = event_risk(intel, symbol)\n    event_info = {\"state\":\"NONE\",\"gate\":True,\"score_adjustment\":0.0,\"events\":[],\"reason\":\"No candidate side evaluated yet\"}\n    rss_news_level, rss_news_events = rss_event_risk(intel, symbol)\n    correlations = correlation_context(symbol, h1, benchmark_rows)\n    yield_diff = yield_context(intel, symbol)\n    candidate_sides = []\n    for c in (\"BUY\",\"SELL\"):\n        cs = consensus_stats(c)\n        if cs[\"aligned_count\"] >= 1 or strategy_by_side[c][\"count\"] >= 1:\n            candidate_sides.append(c)\n    entry_candidates = []\n    potential_candidates = []\n    setup_wait_candidates = []\n    structural_state_by_side = {}\n    CONTEXT_TF_KEYS = (\"1mo\", \"1d\", \"4h\", \"1h\", \"15m\", \"5m\")\n    CONTEXT_PARENT_MAP = {\n        \"4h\": (\"1d\",),\n        \"1h\": (\"4h\",),\n        \"15m\": (\"1h\", \"4h\"),\n        \"5m\": (\"15m\", \"1h\"),\n    }\n    def normalize_context(consensus):\n        src = consensus if isinstance(consensus, dict) else {}\n        return {\n            tf: (src.get(tf) if src.get(tf) in (\"BUY\", \"SELL\", \"NEUTRAL\") else \"NEUTRAL\")\n            for tf in CONTEXT_TF_KEYS\n        }\n    def resolve_context(side_c, consensus):\n        ctx = normalize_context(consensus)\n        monthly = ctx[\"1mo\"]\n        candidates = []\n        evaluations = []\n        for exec_tf in (\"5m\", \"15m\", \"1h\", \"4h\"):\n            if ctx[exec_tf] != side_c:\n                continue\n            failures = []\n            for parent_tf in CONTEXT_PARENT_MAP[exec_tf]:\n                state = ctx[parent_tf]\n                if state == \"NEUTRAL\":\n                    failures.append({\n                        \"timeframe\": parent_tf,\n                        \"state\": \"NEUTRAL_CONTEXT\",\n                        \"observed\": state,\n                        \"expected\": side_c,\n                    })\n                elif state != side_c:\n                    failures.append({\n                        \"timeframe\": parent_tf,\n                        \"state\": \"CONFLICTING_CONTEXT\",\n                        \"observed\": state,\n                        \"expected\": side_c,\n                    })\n            evaluations.append({\n                \"candidate_tf\": exec_tf,\n                \"required_parents\": list(CONTEXT_PARENT_MAP[exec_tf]),\n                \"state\": \"ALIGNED\" if not failures else \"BLOCKED\",\n                \"failures\": failures,\n            })\n            if not failures:\n                candidates.append(exec_tf)\n        return {\n            \"side\": side_c,\n            \"normalized_context\": ctx,\n            \"monthly_state\": (\n                \"NEUTRAL_CONTEXT\" if monthly == \"NEUTRAL\"\n                else \"ALIGNED\" if monthly == side_c\n                else \"CONFLICTING_CONTEXT\"\n            ),\n            \"aligned_execution_tfs\": candidates,\n            \"evaluations\": evaluations,\n            \"aligned\": bool(candidates),\n            \"trigger_5m\": {\"observed\": ctx[\"5m\"], \"state\": \"TRIGGER_ONLY\"},\n        }\n    def choose_position_timeframe(side_c):\n        resolved = resolve_context(side_c, tf_consensus)\n        if resolved[\"aligned_execution_tfs\"]:\n            return resolved[\"aligned_execution_tfs\"][0]\n        normalized = normalize_context(tf_consensus)\n        for tf_name in (\"5m\", \"15m\", \"1h\", \"4h\"):\n            if normalized.get(tf_name) == side_c:\n                return tf_name\n        return \"UNSPECIFIED\"\n    def alignment_chain(side_c):\n        resolved = resolve_context(side_c, tf_consensus)\n        agreed = [\n            tf for tf, state in resolved[\"normalized_context\"].items()\n            if state == side_c\n        ]\n        return {\n            \"agreed\": agreed,\n            \"count\": len(agreed),\n            \"ratio\": round(len(agreed) / len(CONTEXT_TF_KEYS) * 100.0, 1),\n            \"context_aligned\": resolved[\"aligned\"],\n            \"aligned_execution_tfs\": resolved[\"aligned_execution_tfs\"],\n            \"monthly_state\": resolved[\"monthly_state\"],\n            \"trigger_5m_state\": resolved[\"trigger_5m\"][\"state\"],\n        }\n    for c in candidate_sides:\n        cs = consensus_stats(c)\n        event_info = event_alignment(intel, symbol, c, m5, m15)\n        position_tf = choose_position_timeframe(c)\n        chain_info = alignment_chain(c)\n        aligned_names = cs[\"aligned_names\"]\n        aligned_count = cs[\"aligned_count\"]\n        opposed_count = cs[\"opposed_count\"]\n        strat = strategy_by_side[c]\n        blockers = []\n        smc_base_confirmed = any(\n            str(x).upper().replace(\"-\", \"_\").replace(\" \", \"_\") == \"SMC\"\n            or str(x).upper().startswith(\"SMC_\")\n            for x in strat.get(\"strategies\", [])\n        )\n        strategy_confirmed = strat[\"confirmed\"] and smc_base_confirmed\n        strategy_count = strat[\"count\"]\n        active_match = next((m for m in strategy_by_tf.get(position_tf, []) if m.get(\"signal\")==c), None)\n        strategy_category = \"9_STRATEGY_CONSENSUS\"\n        strategy_tier = strat[\"tier\"]\n        one_strategy_confirmed = bool(INSTITUTIONAL_ENGINE_ACTIVE and strategy_confirmed)\n        agreeing_signal_tfs = signal_timeframes(c)\n        monthly_gate = (monthly_side == c) if REQUIRE_MONTHLY_ALIGNMENT else True\n        timeframe_gate = bool(agreeing_signal_tfs)\n        lse_alignment_gate = timeframe_gate\n        three_price = False\n        level_map = {\n            \"INTRADAY\": level_rr(c, \"INTRADAY\", closed_entry_price(m15)),\n            \"SCALP\": level_rr(c, \"SCALP\", closed_entry_price(m5)),\n            \"SWING/HOLD\": level_rr(c, \"SWING/HOLD\", closed_entry_price(h1)),\n        }\n        chosen_category = strategy_category if strategy_confirmed else (\"SCALPING\" if position_tf in (\"5m\",\"15m\") else \"INTRADAY\")\n        if position_tf in (\"5m\", \"15m\"):\n            chosen_strategy = \"SCALP\"\n        elif position_tf == \"4h\":\n            chosen_strategy = \"SWING/HOLD\"\n        else:\n            chosen_strategy = \"INTRADAY\"\n        selected_strategy = chosen_strategy\n        structure_tf = {\"SCALP\": \"15m\", \"INTRADAY\": \"1h\", \"SWING/HOLD\": \"4h\"}.get(\n            selected_strategy, \"1h\"\n        )\n        structural_state = fresh_structural_break(data.get(structure_tf, []), c)\n        structural_invalidation = bool(structural_state.get(\"invalidated\"))\n        structural_state_by_side[c] = {\n            **structural_state,\n            \"timeframe\": structure_tf,\n            \"side\": c,\n        }\n        level, rr = level_map.get(selected_strategy, (None, None))\n        if selected_strategy == \"ADAPTIVE PRICE-MOVEMENT\":\n            level, rr = level_map[\"INTRADAY\"]\n        consensus_score = float(strat.get(\"score\", 0.0) or 0.0) if one_strategy_confirmed else 0.0\n        if monthly_side == c:\n            consensus_score = min(100.0, consensus_score + 8.0)\n        if cs[\"tf_count\"] >= 2:\n            consensus_score = min(100.0, consensus_score + min(12.0, cs[\"tf_count\"] * 3.0))\n        if aligned_count >= 3:\n            consensus_score = min(100.0, consensus_score + min(12.0, (aligned_count-2)*6.0))\n        consensus_score = min(\n            100.0,\n            consensus_score + min(16.0, len(agreeing_signal_tfs) * 4.0)\n        )\n        if three_price:\n            consensus_score = max(consensus_score, min(\n                100.0, 50.0 + cs[\"price_count\"] * 9.0 + cs[\"price_tf_count\"] * 4.0 - opposed_count * 4.0\n            ))\n        news_cooperation = event_info.get(\"state\", event_info.get(\"event_state\", \"NONE\"))\n        news_direction = event_info.get(\"direction\", event_info.get(\"signal\", \"NEUTRAL\"))\n        news_aligned = (\n            news_direction == c\n            and news_cooperation in (\"ALIGNED\", \"EXECUTABLE\", \"POST_EVENT_ALIGNED\", \"SUPPORTIVE\")\n        )\n        news_conflict = (\n            news_direction in (\"BUY\", \"SELL\")\n            and news_direction != c\n            and news_cooperation not in (\"NONE\", \"UNAVAILABLE\")\n        )\n        consensus_score = max(0.0, min(100.0, consensus_score + float(event_info.get(\"score_adjustment\", 0.0))))\n        if news_aligned:\n            consensus_score = min(100.0, consensus_score + 5.0)\n        elif news_conflict:\n            consensus_score = max(0.0, consensus_score - 5.0)\n        consensus_score = max(0.0, min(100.0, consensus_score + min(10.0, chain_info[\"count\"] * 1.5)))\n        adaptive_score_pre = adaptive_consensus_score(strategy_by_tf, c, adaptive_market_state(data, category))\n        consensus_score = round((consensus_score * 0.55) + (adaptive_score_pre * 0.45), 1)\n        fx_alignment = currency_pair_alignment(currency_strength, symbol, c)\n        if fx_alignment[\"state\"] == \"ALIGNED\":\n            consensus_score = min(100.0, consensus_score + fx_alignment[\"score\"])\n        elif fx_alignment[\"state\"] == \"CONFLICT\":\n            consensus_score = max(0.0, consensus_score + fx_alignment[\"score\"])\n        trigger_tf = {\"SCALP\": \"5m\", \"INTRADAY\": \"15m\", \"SWING/HOLD\": \"1h\", \"ADAPTIVE PRICE-MOVEMENT\": \"15m\"}.get(chosen_strategy, \"15m\")\n        try:\n            fresh_live = collect_live_quotes(client, [symbol], timeout=3.0).get(symbol, [])\n        except Exception as exc:\n            fresh_live = []\n            print(f\"LIVE QUOTE REFRESH WARNING {symbol}: {type(exc).__name__}: {exc}\")\n        trigger_state = execution_trigger_state(\n            data.get(trigger_tf, []),\n            {\"ticks\": fresh_live if fresh_live else deep_live_raw.get(symbol, [])},\n            c,\n            symbol=symbol,\n        )\n        trigger_crossed = bool(trigger_state.get(\"trigger_crossed\", False))\n        # Preserve the pre-live structural plan as a fallback. The authoritative\n        # potential plan may use the closed-candle trigger plan below, but it must\n        # never fall back to a sub-2R or TP-less plan.\n        base_level, base_rr = level, rr\n        trigger_safe = trigger_state.get(\"state\") != \"OVEREXTENDED\"\n        plan_current = trigger_state.get(\"current_price\")\n        closed_plan = candidate_plan(\n            data.get(trigger_tf, []), c, current_price=plan_current, symbol=symbol,\n            target_rows=[data.get(\"5m\", []), data.get(\"15m\", []), data.get(\"1h\", [])]\n        )\n        if closed_plan:\n            level = (closed_plan[\"entry\"], closed_plan[\"sl\"], closed_plan[\"tp\"], closed_plan[\"atr\"])\n            rr = closed_plan[\"rr\"]\n        enforced_level = enforce_min_target(symbol, level)\n        if level is not None and enforced_level is None:\n            # Preserve the genuine structural Entry/SL/TP for visibility. The gate blocks promotion; it does not erase the plan.\n            target_distance_now = target_distance_pips(symbol, level[0], level[2])\n            raw_risk = (\n                abs(float(level[0]) - float(level[1]))\n                if level[0] is not None and level[1] is not None\n                else None\n            )\n            if level[2] is not None and raw_risk is not None and raw_risk > 0:\n                rr = abs(float(level[2]) - float(level[0])) / raw_risk\n            else:\n                rr = None\n            rr_text = f\"{rr:.2f}R\" if rr is not None else \"UNAVAILABLE\"\n            blockers.append(\n                f\"STRUCTURAL_TARGET_OR_RR_GATE: target_distance={target_distance_now if target_distance_now is not None else 'UNAVAILABLE'} pips | \"\n                f\"RR={rr_text} | required RR>={MIN_CONFIRMED_RR:.1f}R\"\n            )\n        elif isinstance(enforced_level, (list, tuple)) and len(enforced_level) >= 5:\n            level = tuple(enforced_level[:4])\n            rr = enforced_level[4]\n\n        # SINGLE AUTHORITATIVE POTENTIAL PLAN:\n        # Prefer the closed-candle execution plan; otherwise use the already\n        # derived structural plan. A potential entry is emitted only when a\n        # genuine Entry/SL/TP exists and its actual R:R is >= 2.0R.\n        potential_plan = None\n        if (\n            isinstance(closed_plan, dict)\n            and closed_plan.get(\"entry\") is not None\n            and closed_plan.get(\"sl\") is not None\n            and closed_plan.get(\"tp\") is not None\n            and closed_plan.get(\"rr\") is not None\n            and float(closed_plan.get(\"rr\")) >= float(MIN_CONFIRMED_RR)\n        ):\n            potential_plan = dict(closed_plan)\n            potential_plan[\"status\"] = \"VALID_STRUCTURAL_RR\"\n            potential_plan[\"plan_basis\"] = \"CLOSED_CANDLE_STRUCTURAL_BREAKOUT\"\n        elif (\n            isinstance(base_level, (list, tuple))\n            and len(base_level) >= 3\n            and base_level[0] is not None\n            and base_level[1] is not None\n            and base_level[2] is not None\n            and base_rr is not None\n            and float(base_rr) >= float(MIN_CONFIRMED_RR)\n        ):\n            potential_plan = {\n                \"entry\": base_level[0], \"sl\": base_level[1], \"tp\": base_level[2],\n                \"atr\": base_level[3] if len(base_level) >= 4 else None,\n                \"rr\": float(base_rr), \"entry_trigger\": base_level[0],\n                \"status\": \"VALID_STRUCTURAL_RR\",\n                \"plan_basis\": \"CLOSED_CANDLE_STRUCTURAL_LEVELS\",\n            }\n        potential_level = (\n            (potential_plan.get(\"entry\"), potential_plan.get(\"sl\"),\n             potential_plan.get(\"tp\"), potential_plan.get(\"atr\"))\n            if potential_plan else None\n        )\n        potential_rr = potential_plan.get(\"rr\") if potential_plan else None\n        asset_condition = asset_market_condition(\n            symbol,\n            data.get(\"5m\", []),\n            fresh_live if fresh_live else deep_live_raw.get(symbol, [])\n        )\n        adaptive_state = adaptive_market_state(data, category)\n        adaptive_profile = adaptive_decision_profile(adaptive_state)\n        adaptive_score = adaptive_consensus_score(strategy_by_tf, c, adaptive_state)\n        adaptive_state[\"decision_profile\"] = adaptive_profile\n        adaptive_state[\"weighted_consensus_score\"] = adaptive_score\n        target_pips = target_distance_pips(symbol, level[0], level[2]) if level else None\n        target_pips_ok = (target_pips is not None and target_pips >= MIN_TARGET_PIPS) if category == \"forex\" else bool(level and rr is not None and rr >= MIN_CONFIRMED_RR)\n        early_entry_ok = early_entry_window_ok(trigger_state, EARLY_ENTRY_MINUTES)\n        rr_ok_now = rr is not None and rr >= MIN_CONFIRMED_RR and target_pips_ok\n        score_gate = consensus_score >= max(ENTRY_STRATEGY_SCORE_MIN, adaptive_profile[\"score_floor\"])\n        adaptive_strategy_gate = strategy_count >= adaptive_profile[\"required_strategies\"]\n        adaptive_opposition_gate = opposed_count <= min(MAX_OPPOSED_STRATEGIES_FOR_ENTRY, adaptive_profile[\"max_opposed\"])\n        quality_requirements = {\n            \"SCALP\": (\"5m\", \"15m\"),\n            \"INTRADAY\": (\"15m\", \"1h\"),\n            \"SWING/HOLD\": (\"1h\", \"4h\"),\n        }.get(chosen_strategy, (trigger_tf,))\n        quality_gate = all(quality.get(tf, {}).get(\"ok\", False) for tf in quality_requirements)\n        volatility_gate = volatility.get(trigger_tf, {}).get(\"state\") != \"EXTREME_HIGH\"\n        regime_gate = (\n            regime_side in (c, \"NEUTRAL\")\n            if chosen_strategy != \"SWING/HOLD\"\n            else regime_side == c\n        )\n        execution_gate = (\n            trigger_state.get(\"state\") == \"EXECUTABLE_TRIGGER_CROSSED\"\n            and float(trigger_state.get(\"execution_score\", 0.0)) >= EXECUTION_SCORE_MIN\n            and float(trigger_state.get(\"quote_age_seconds\") or 999.0) <= MAX_QUOTE_AGE_SECONDS\n        )\n        execution_price = trigger_state.get(\"executable_price\")\n        retracement_state = retracement_quality(data.get(trigger_tf, []), c, trigger_price=trigger_state.get(\"trigger_price\"), atr_value=atr(data.get(trigger_tf, [])) if data.get(trigger_tf) else None)\n        retracement_gate = retracement_state.get(\"ok\", False) if REQUIRE_RETRACEMENT_FOR_CONFIRMED else True\n        execution_level = None\n        execution_rr = None\n        execution_rr_gate = False\n        if (\n            execution_gate and execution_price is not None\n            and potential_level is not None\n            and potential_level[1] is not None and potential_level[2] is not None\n        ):\n            try:\n                ep = float(execution_price)\n                slp = float(potential_level[1])\n                tpp = float(potential_level[2])\n                if c == \"BUY\":\n                    target_ahead = tpp > ep\n                else:\n                    target_ahead = tpp < ep\n                actual_risk = abs(ep - slp)\n                actual_reward = abs(tpp - ep)\n                execution_rr = actual_reward / actual_risk if actual_risk > 0 else None\n                if target_ahead and execution_rr is not None and execution_rr >= MIN_CONFIRMED_RR:\n                    execution_level = (ep, slp, tpp, potential_level[3] if len(potential_level) >= 4 else None)\n                    execution_rr_gate = True\n            except (TypeError, ValueError, ZeroDivisionError):\n                execution_rr = None\n        # Confirmed entries use the actual executable bid/ask price, not the trigger reference.\n        # Recalculate R:R at that executable price so the displayed plan matches the trade that can actually be entered.\n        effective_rr = execution_rr if execution_rr is not None else rr\n        if execution_rr_gate:\n            level = execution_level\n            rr = execution_rr\n            rr_ok_now = execution_rr >= MIN_CONFIRMED_RR and (\n                target_distance_pips(symbol, level[0], level[2]) >= MIN_TARGET_PIPS\n                if category == \"forex\" else True\n            )\n        category_threshold = CATEGORY_SCORE_MIN.get(chosen_category, ENTRY_STRATEGY_SCORE_MIN)\n        category_score_gate = consensus_score >= category_threshold\n        tier_gate = strategy_confirmed and INSTITUTIONAL_ENGINE_ACTIVE and strategy_count >= MIN_CONFIRMED_STRATEGIES\n        direction_state = dynamic_direction_state(data, tf_consensus, monthly_side) if DYNAMIC_DIRECTION_ACTIVE else {\n            \"active_direction\": monthly_side, \"strategic_bias\": monthly_side, \"tactical_direction\": monthly_side,\n            \"mode\": \"LEGACY\", \"reversal_confirmed\": False, \"transition_watch\": False,\n            \"old_direction\": monthly_side, \"new_direction\": monthly_side, \"htf_confirmations\": 0,\n            \"execution_confirmations\": 0, \"h4_flip\": regime_side == c, \"structural_invalidation\": {\"state\":\"NONE\",\"invalidated\":False},\n            \"timeframe_directions\": tf_consensus,\n        }\n        is_reversal = bool(direction_state.get(\"reversal_confirmed\")) and direction_state.get(\"active_direction\") == c\n        is_transition_watch = bool(direction_state.get(\"transition_watch\")) and direction_state.get(\"new_direction\") == c\n        # Normal continuation keeps the strategic context. A genuine reversal can\n        # override the old 1MO direction only after the dynamic-direction engine\n        # confirms multi-timeframe evidence. Transition/watch never confirms.\n        reversal_context_gate = (\n            direction_state.get(\"execution_confirmations\", 0) >= REVERSAL_MIN_EXECUTION_CONFIRMATIONS\n            and (direction_state.get(\"htf_confirmations\", 0) >= REVERSAL_MIN_HTF_CONFIRMATIONS\n                 or bool(direction_state.get(\"structural_invalidation\", {}).get(\"invalidated\")))\n        )\n        context_gate = bool(chain_info.get(\"context_aligned\")) or (is_reversal and reversal_context_gate)\n        monthly_gate = (monthly_side == c) if REQUIRE_MONTHLY_ALIGNMENT else True\n        if is_reversal:\n            monthly_gate = True\n        regime_gate = (regime_side == c) if REQUIRE_4H_ALIGNMENT else regime_gate\n        if is_reversal and direction_state.get(\"h4_flip\"):\n            regime_gate = True\n        elif is_reversal and direction_state.get(\"structural_invalidation\", {}).get(\"invalidated\"):\n            # A fresh 4H break against the old strategic direction is sufficient\n            # to permit a tactical reversal while 4H consensus catches up.\n            regime_gate = True\n        opposition_gate = opposed_count <= MAX_OPPOSED_STRATEGIES_FOR_ENTRY\n        tactical_bias_gate = monthly_gate and opposition_gate and not is_transition_watch\n        entry_timing_gate = early_entry_ok if trigger_state.get(\"state\") == \"WAITING_FOR_TRIGGER\" else execution_gate\n        qualifies = (strategy_confirmed and tier_gate and category_score_gate and\n                     trigger_crossed and trigger_safe and entry_timing_gate and quality_gate\n                     and volatility_gate and regime_gate and execution_gate\n                     and tactical_bias_gate and context_gate and rr_ok_now and execution_rr_gate\n                     and retracement_gate and not structural_invalidation\n                     and adaptive_strategy_gate and adaptive_opposition_gate and score_gate)\n        reasons = [\n            f\"INSTITUTIONAL ENGINE: {strategy_count} production engine ({', '.join(strat.get('strategies',[])[:6])})\",\n            f\"CATEGORY: {chosen_category} | TIER: {strategy_tier} | STRATEGY SCORE: {strat.get('score',0):.1f}/100\",\n            f\"INSTITUTIONAL ENGINE: {aligned_count} production strategy ({', '.join(sorted(aligned_names))})\"\n        ]\n        if cs[\"tf_count\"]:\n            reasons.append(f\"TIMEFRAME BREADTH: {cs['tf_count']} timeframe(s)\")\n        reasons.append(f\"STRATEGIC BIAS (1MO): {monthly_side}\")\n        reasons.append(\n            f\"DYNAMIC DIRECTION: mode={direction_state.get('mode')} | active={direction_state.get('active_direction')} | \"\n            f\"tactical={direction_state.get('tactical_direction')} | HTF confirmations={direction_state.get('htf_confirmations')} | \"\n            f\"lower-TF confirmations={direction_state.get('execution_confirmations')}\"\n        )\n        if is_reversal:\n            reasons.append(\n                f\"REVERSAL OVERRIDE: {direction_state.get('old_direction')} -> {direction_state.get('new_direction')} \"\n                f\"after structural/multi-timeframe confirmation; 1MO remains context only\"\n            )\n        elif is_transition_watch:\n            reasons.append(\n                f\"REVERSAL WATCH: {direction_state.get('old_direction')} -> {direction_state.get('new_direction')} is developing; confirmation withheld\"\n            )\n        reasons.append(\n            f\"ADAPTIVE INSTITUTIONAL REGIME: {adaptive_state.get('regime')} | \"\n            f\"efficiency={adaptive_state.get('efficiency')} | volatility_pct={adaptive_state.get('volatility_percentile')} | \"\n            f\"directional_agreement={adaptive_state.get('directional_agreement')}\"\n        )\n        reasons.append(\n            f\"ADAPTIVE DECISION: weighted_score={adaptive_score:.1f}/100 | \"\n            f\"score_floor={adaptive_profile['score_floor']:.1f} | \"\n            f\"required_strategies={adaptive_profile['required_strategies']} | max_opposed={adaptive_profile['max_opposed']}\"\n        )\n        reasons.append(f\"ENTRY TRIGGER {trigger_tf.upper()}: {trigger_state.get('state')} — {trigger_state.get('reason')}\")\n        reasons.append(\n            f\"ASSET ENVIRONMENT: {asset_condition.get('environment')} | {asset_condition.get('state')} | \"\n            f\"bid={asset_condition.get('bid')} ask={asset_condition.get('ask')} | \"\n            f\"5M ATR={asset_condition.get('history_atr')} | spread/ATR={asset_condition.get('spread_atr')}\"\n        )\n        reasons.append(f\"EARLY ENTRY WINDOW: {EARLY_ENTRY_MINUTES:.0f}m minimum pre-move/controlled-trigger model\")\n        reasons.append(f\"RETRACEMENT QUALITY: {retracement_state.get('state')} | fraction={retracement_state.get('retracement_fraction')} | pullback_bars={retracement_state.get('pullback_bars')} | {retracement_state.get('reason')}\")\n        if target_pips is not None:\n            reasons.append(f\"TARGET DISTANCE: {target_pips:.1f} pips\")\n        if fx_alignment.get(\"state\") != \"UNAVAILABLE\":\n            reasons.append(f\"FX RELATIVE STRENGTH: {fx_alignment['state']} ({fx_alignment['score']:+.2f})\")\n        reasons.append(\n            \"SIGNAL TF AGREEMENT: \" + \", \".join(agreeing_signal_tfs)\n            if agreeing_signal_tfs else\n            \"SIGNAL TF AGREEMENT: none\"\n        )\n        if \"SMC\" in aligned_names:\n            reasons.append(\"SMC CONTEXT: aligned; no priority gate\")\n        else:\n            reasons.append(\"SMC CONTEXT: SMC base requirement not confirmed\")\n        if three_price:\n            reasons.append(f\"PRICE-MOVEMENT: {cs['price_count']} distinct methods across {cs['price_tf_count']} timeframes\")\n        if opposed_count:\n            reasons.append(f\"OPPOSITION: {opposed_count} distinct strategy method(s) opposed\")\n        if not strategy_confirmed or not INSTITUTIONAL_ENGINE_ACTIVE:\n          blockers.append(f\"MISSING: at least {MIN_CONFIRMED_STRATEGIES} distinct strategies from the 9 must agree\")\n        if not adaptive_strategy_gate:\n            blockers.append(f\"ADAPTIVE REGIME GATE: {adaptive_state.get('regime')} requires {adaptive_profile['required_strategies']} independent strategies; got {strategy_count}\")\n        if not adaptive_opposition_gate:\n            blockers.append(f\"ADAPTIVE CONFLICT GATE: {opposed_count} opposing methods exceed {adaptive_profile['max_opposed']} allowed in {adaptive_state.get('regime')}\")\n        if not score_gate:\n            blockers.append(f\"ADAPTIVE SCORE GATE: {consensus_score:.1f}/100 below {adaptive_profile['score_floor']:.1f}/100 for {adaptive_state.get('regime')}\")\n        if structural_invalidation:\n            blockers.append(\n                f\"INVALIDATED: fresh {structural_state.get('state')} on {structure_tf.upper()} \"\n                f\"closed candle; previous {c} setup removed from actionable competition\"\n            )\n        if not smc_base_confirmed:\n          blockers.append(\"MISSING: SMC base confirmation is required for a qualifying signal\")\n        if not one_strategy_confirmed:\n          blockers.append(f\"MISSING: {MIN_CONFIRMED_STRATEGIES}-of-9 consensus is not confirmed on the required timeframe\")\n        if not category_score_gate:\n            blockers.append(f\"MISSING: {chosen_category} score must be >= {category_threshold:.0f}/100 (now {consensus_score:.1f})\")\n        if not trigger_crossed:\n            blockers.append(f\"MISSING: {c} entry trigger at {trigger_state.get('trigger_price'):.6g} has not been crossed by the live price snapshot\")\n        if not trigger_safe:\n            blockers.append(\"BLOCKED: trigger crossed but price is overextended; wait for a fresh setup\")\n        if not monthly_gate:\n          blockers.append(f\"BLOCKED: strategic 1MO bias is {monthly_side}; {c} has not earned a dynamic reversal override\")\n        if is_transition_watch:\n          blockers.append(f\"WAIT: dynamic reversal is not yet confirmed ({direction_state.get('old_direction')} -> {direction_state.get('new_direction')}); do not flip direction early\")\n        if not timeframe_gate:\n          blockers.append(\"BLOCKED: no operational timeframe has a 2-of-9 consensus for this direction\")\n        if not quality_gate:\n            blockers.append(\"BLOCKED: candle data-quality gate failed on one or more execution timeframes\")\n        if not volatility_gate:\n            blockers.append(f\"BLOCKED: {trigger_tf.upper()} volatility is EXTREME_HIGH; wait for normalized execution conditions\")\n        if not regime_gate:\n            blockers.append(f\"BLOCKED: completed 4H regime is {regime_side}; confirmed {c} requires 4H alignment or a verified dynamic reversal break\")\n        if not opposition_gate:\n            blockers.append(f\"BLOCKED: {opposed_count} opposing strategy methods; maximum allowed is {MAX_OPPOSED_STRATEGIES_FOR_ENTRY}\")\n        if not execution_gate:\n            blockers.append(f\"BLOCKED: institutional top-of-book execution gate = {trigger_state.get('state')} | score {float(trigger_state.get('execution_score', 0.0)):.1f}/100 | {trigger_state.get('reason','unavailable')}\")\n        if not retracement_gate:\n            blockers.append(f\"BLOCKED: high-quality retracement is not ready — {retracement_state.get('reason','retracement condition failed')}; do not chase\")\n        if not target_pips_ok and target_pips is not None:\n            blockers.append(f\"MISSING: FX target distance must be >= {MIN_TARGET_PIPS:.0f} pips (now {target_pips:.1f})\")\n        if not entry_timing_gate:\n            blockers.append(f\"BLOCKED: trigger timing/control window is not valid; state={trigger_state.get('state')} | do not chase\")\n        if trigger_crossed and not execution_rr_gate:\n            blockers.append(f\"BLOCKED: executable-price R:R recheck failed (current plan R:R={float(rr):.2f}R)\" if rr is not None else \"BLOCKED: executable-price R:R recheck failed (R:R unavailable)\")\n        if not rr_ok_now:\n            blockers.append(\n                f\"MISSING: real execution R:R must be >= {MIN_CONFIRMED_RR:.1f}R (now {rr:.2f}R)\"\n                if rr is not None else\n                \"MISSING: real execution R:R unavailable\"\n            )\n        reasons.append(\n            \"EVENT CONTEXT ONLY: \" + str(event_info.get(\"reason\", \"no active event conflict\"))\n        )\n        if event_info.get(\"state\") not in (None, \"NONE\", \"UNAVAILABLE\"):\n            reasons.append(\n                f\"EVENT CONTEXT STATE: {event_info.get('state')} | not a mandatory confirmation gate\"\n            )\n        if qualifies and rr_ok_now:\n            entry_candidates.append({\n                \"status\": \"CONFIRMED_CANDIDATE\",\n                \"setup_stage\": \"CONFIRMED_CANDIDATE\",\n                \"strategy\":selected_strategy,\n                \"validated_strategy\":\"9_STRATEGY_CONSENSUS\",\n                \"entry_category\":chosen_category,\n                \"strategy_tier\":strategy_tier,\n                \"confirmed_strategy_count\":strategy_count if one_strategy_confirmed else 0,\n                \"confirmed_strategies\":sorted(aligned_names) if one_strategy_confirmed else [],\n                \"strategy_matches\":strat.get(\"all_matches\",[]),\n                \"side\":c,\n                \"level\":level,\n                \"entry\":level[0] if isinstance(level, (list, tuple)) and len(level) >= 3 else None,\n                \"sl\":level[1] if isinstance(level, (list, tuple)) and len(level) >= 3 else None,\n                \"tp\":level[2] if isinstance(level, (list, tuple)) and len(level) >= 3 else None,\n                \"atr\":level[3] if isinstance(level, (list, tuple)) and len(level) >= 4 else None,\n                \"rr\":rr,\n                \"plan_status\":potential_plan.get(\"status\") if isinstance(potential_plan, dict) else (\"STRUCTURAL_PLAN\" if potential_level else \"PLAN_UNAVAILABLE\"),\n                \"plan_basis\":\"CLOSED_CANDLE_STRUCTURAL_BREAKOUT\",\n                \"score_basis\":consensus_score,\n                \"signal_timeframes\": list(agreeing_signal_tfs),\n                \"position_timeframe\": position_tf,\n                \"alignment_chain\": chain_info,\n                \"monthly_bias\": monthly_side,\n                \"strategic_bias\": direction_state.get(\"strategic_bias\"),\n                \"tactical_direction\": direction_state.get(\"tactical_direction\"),\n                \"direction_mode\": direction_state.get(\"mode\"),\n                \"reversal_confirmed\": bool(direction_state.get(\"reversal_confirmed\")),\n                \"direction_state\": direction_state,\n                \"horizon\":horizon_for(selected_strategy),\n                \"reasons\":reasons,\n                \"execution_trigger\": trigger_state,\n                \"retracement_quality\": retracement_state,\n            })\n        elif qualifies:\n            setup_wait_candidates.append({\n                \"status\": \"WAITING_FOR_TRIGGER\",\n                \"setup_stage\": \"SETUP_READY\",\n                \"strategy\":selected_strategy,\n                \"validated_strategy\":\"9_STRATEGY_CONSENSUS\",\n                \"entry_category\":chosen_category,\n                \"strategy_tier\":strategy_tier,\n                \"confirmed_strategy_count\":strategy_count if one_strategy_confirmed else 0,\n                \"confirmed_strategies\":sorted(aligned_names) if one_strategy_confirmed else [],\n                \"strategy_matches\":strat.get(\"all_matches\",[]),\n                \"side\":c,\n                # Keep the conditional plan actually reconstructed above.\n                \"level\":potential_level,\n                \"entry\":potential_level[0] if isinstance(potential_level, (list, tuple)) and len(potential_level) >= 3 else None,\n                \"sl\":potential_level[1] if isinstance(potential_level, (list, tuple)) and len(potential_level) >= 3 else None,\n                \"tp\":potential_level[2] if isinstance(potential_level, (list, tuple)) and len(potential_level) >= 3 else None,\n                \"rr\":potential_rr,\n                \"score_basis\":consensus_score,\n                \"signal_timeframes\": list(agreeing_signal_tfs),\n                \"position_timeframe\": position_tf,\n                \"alignment_chain\": chain_info,\n                \"monthly_bias\": monthly_side,\n                \"strategic_bias\": direction_state.get(\"strategic_bias\"),\n                \"tactical_direction\": direction_state.get(\"tactical_direction\"),\n                \"direction_mode\": direction_state.get(\"mode\"),\n                \"reversal_confirmed\": bool(direction_state.get(\"reversal_confirmed\")),\n                \"direction_state\": direction_state,\n                \"horizon\":horizon_for(selected_strategy),\n                \"reasons\":blockers or [\"WAIT: risk-quality condition pending\"],\n                \"execution_trigger\": trigger_state,\n                \"retracement_quality\": retracement_state,\n            })\n        potential_ready = consensus_score >= POTENTIAL_SCORE_MIN and strategy_count >= 1 and INSTITUTIONAL_ENGINE_ACTIVE\n        near_complete = strategy_count < MIN_CONFIRMED_STRATEGIES and strategy_count >= 1\n        if not structural_invalidation and (potential_ready or near_complete):\n            missing_count = max(1, MIN_CONFIRMED_STRATEGIES - strategy_count)\n            # Only publish a potential entry when the authoritative plan has\n            # a genuine TP and actual R:R >= 2R. Never manufacture/extend TP.\n            if potential_plan is not None:\n                potential_candidates.append({\n                \"status\": f\"POTENTIAL_{c}\",\n                \"setup_stage\": \"SETUP_READY\" if consensus_score >= 70 and aligned_count >= 2 else \"DEVELOPING\",\n                \"strategy\":selected_strategy,\n                \"validated_strategy\":\"9_STRATEGY_CONSENSUS\",\n                \"entry_category\":chosen_category,\n                \"strategy_tier\":strategy_tier,\n                \"confirmed_strategy_count\":strategy_count if one_strategy_confirmed else 0,\n                \"confirmed_strategies\":sorted(aligned_names) if one_strategy_confirmed else [],\n                \"strategy_matches\":strat.get(\"all_matches\",[]),\n                \"side\":c,\n                \"level\":potential_level,\n                \"entry\":potential_level[0],\n                \"sl\":potential_level[1],\n                \"tp\":potential_level[2],\n                \"atr\":potential_level[3] if len(potential_level) >= 4 else None,\n                \"rr\":potential_rr,\n                \"plan_status\":potential_plan.get(\"status\"),\n                \"plan_basis\":potential_plan.get(\"plan_basis\",\"CLOSED_CANDLE_STRUCTURAL_BREAKOUT\"),\n                \"entry_trigger\":potential_plan.get(\"entry_trigger\", potential_level[0]),\n                \"retracement_quality\": retracement_state,\n                \"score_basis\":consensus_score,\n                \"signal_timeframes\": list(agreeing_signal_tfs),\n                \"position_timeframe\": position_tf,\n                \"alignment_chain\": chain_info,\n                \"monthly_bias\": monthly_side,\n                \"strategic_bias\": direction_state.get(\"strategic_bias\"),\n                \"tactical_direction\": direction_state.get(\"tactical_direction\"),\n                \"direction_mode\": direction_state.get(\"mode\"),\n                \"reversal_confirmed\": bool(direction_state.get(\"reversal_confirmed\")),\n                \"direction_state\": direction_state,\n                \"horizon\":horizon_for(selected_strategy),\n                \"reasons\":[\n                    (f\"POTENTIAL: strategy score {consensus_score:.1f}/100; final execution gates still pending\"\n                     if potential_ready and aligned_count >= 2 else\n                     f\"MISSING: {MIN_CONFIRMED_STRATEGIES}-of-9 strategy confirmation is not active\"\n                     if strategy_count < MIN_CONFIRMED_STRATEGIES else\n                     \"MISSING: 9-strategy consensus engine confirmation is not active\"),\n                    *( [f\"MISSING: entry trigger at {trigger_state.get('trigger_price'):.6g} has not been crossed\"] if not trigger_crossed else [] ),\n                     *( [\"BLOCKED: trigger is overextended; wait for a new closed-candle setup\"] if not trigger_safe else [] ),\n                    *( [f\"MISSING: R:R must reach {MIN_CONFIRMED_RR:.1f}R (current {rr:.2f}R)\" if rr is not None else \"MISSING: executable R:R unavailable\"] if not rr_ok_now else [] ),\n                    *( [f\"WAIT: high-quality retracement not ready — {retracement_state.get('reason','retracement condition pending')}\" ] if not retracement_gate else [] ),\n                ],\n            })\n    priority = {\"INTRADAY\":4,\"SCALP\":3,\"SWING/HOLD\":2,\"ADAPTIVE PRICE-MOVEMENT\":3}\n    entry_candidates.sort(key=lambda x:(priority[x[\"strategy\"]],x[\"score_basis\"],x[\"rr\"] or 0), reverse=True)\n    setup_wait_candidates.sort(key=lambda x:(priority[x[\"strategy\"]],x[\"score_basis\"],x[\"rr\"] or 0), reverse=True)\n    potential_candidates.sort(key=lambda x:(priority[x[\"strategy\"]],x[\"score_basis\"],x.get(\"rr\") or 0), reverse=True)\n\n    # POTENTIAL AUTHORITY: every conditional candidate is self-contained.\n    # Its direction, Entry/SL/TP/Trigger, timeframe, execution condition and\n    # chart candles are authoritative; the outer result cannot contaminate it.\n    for potential_candidate in potential_candidates:\n        p_side = str(potential_candidate.get(\"side\", \"\")).upper()\n        p_tf = str(potential_candidate.get(\"position_timeframe\") or \"15m\").lower()\n        p_level = potential_candidate.get(\"level\")\n        p_rr = potential_candidate.get(\"rr\")\n        if p_side not in (\"BUY\", \"SELL\") or not isinstance(p_level, (list, tuple)) or len(p_level) < 3:\n            continue\n        p_trade_type = \"SCALP\" if p_tf == \"5m\" else (\"INTRADAY\" if p_tf in (\"15m\", \"1h\") else (\"SWING/HOLD\" if p_tf == \"4h\" else \"INTRADAY\"))\n        p_exec = potential_candidate.get(\"execution_trigger\") or {}\n        p_event = event_alignment(intel, symbol, p_side, m5, m15)\n        p_exec_base = execution_quality_score(p_rr, event_level, p_level, p_event.get(\"state\", \"NONE\"), str(p_exec.get(\"state\", \"UNAVAILABLE\")))\n        p_market_exec = float(p_exec.get(\"execution_score\", 0.0) or 0.0)\n        p_exec_quality = round(0.55 * p_exec_base + 0.45 * p_market_exec, 1)\n        p_chart_source = data.get(p_tf, [])\n        p_chart_rows = [{\"timestamp\": row.get(\"timestamp\").isoformat() if hasattr(row.get(\"timestamp\"), \"isoformat\") else str(row.get(\"timestamp\")), \"open\": row.get(\"open\"), \"high\": row.get(\"high\"), \"low\": row.get(\"low\"), \"close\": row.get(\"close\"), \"volume\": row.get(\"volume\")} for row in p_chart_source[-120:]]\n        potential_candidate.update({\n            \"symbol\": symbol, \"trade_type\": p_trade_type, \"opportunity_class\": p_trade_type,\n            \"opportunity_style\": potential_candidate.get(\"strategy\") or p_trade_type,\n            \"execution_timeframe\": p_tf, \"chart_timeframe\": p_tf, \"chart_candles\": p_chart_rows,\n            \"strategy_score\": float(potential_candidate.get(\"score_basis\", 0.0) or 0.0),\n            \"execution_quality_score\": p_exec_quality, \"risk_score\": p_exec_quality,\n            \"event_alignment\": p_event, \"rss_news_risk\": rss_news_level,\n            \"smc_base_confirmed\": bool(smc_core(data.get(p_tf, [])).get(\"passed\")) if data.get(p_tf) else False,\n            \"blockers\": list(potential_candidate.get(\"reasons\") or []), \"levels\": list(p_level),\n            \"take_profit\": p_level[2], \"stop_loss\": p_level[1], \"potential_entry\": p_level[0],\n            \"entry_price\": p_level[0], \"entry_trigger\": potential_candidate.get(\"entry_trigger\", p_level[0]),\n            \"tp_status\": \"VALID_STRUCTURAL_RR\" if p_level[2] is not None and p_rr is not None and float(p_rr) >= MIN_CONFIRMED_RR else \"TP_UNSUPPORTED\",\n            \"signal_type\": f\"POTENTIAL_{p_side}\",\n        })\n    selected = entry_candidates[0] if entry_candidates else None\n    best_setup_wait = setup_wait_candidates[0] if setup_wait_candidates else None\n    buy_stats = consensus_stats(\"BUY\")\n    sell_stats = consensus_stats(\"SELL\")\n    buy_valid = not structural_state_by_side.get(\"BUY\", {}).get(\"invalidated\", False)\n    sell_valid = not structural_state_by_side.get(\"SELL\", {}).get(\"invalidated\", False)\n    if buy_valid and (not sell_valid or buy_stats[\"aligned_count\"] > sell_stats[\"aligned_count\"]):\n        side = \"BUY\"\n    elif sell_valid and (not buy_valid or sell_stats[\"aligned_count\"] > buy_stats[\"aligned_count\"]):\n        side = \"SELL\"\n    else:\n        side = \"NEUTRAL\"\n    if selected:\n        status = \"CONFIRMED_ENTRY\"\n        side = selected[\"side\"]\n        level = selected[\"level\"]\n        rr = selected[\"rr\"]\n        blockers = []\n        trade_type = selected[\"strategy\"]\n        horizon = selected[\"horizon\"]\n        strategy_gate = selected[\"reasons\"][0]\n        selected_consensus_score = selected[\"score_basis\"]\n    elif best_setup_wait:\n        status = \"WAITING_FOR_TRIGGER\"\n        pending_entry = level[0] if level else None\n        entry_quality = min(100, max(0, int(selected_consensus_score)))\n        side = best_setup_wait[\"side\"]\n        level = best_setup_wait[\"level\"]\n        rr = best_setup_wait[\"rr\"]\n        trade_type = best_setup_wait[\"strategy\"]\n        horizon = best_setup_wait[\"horizon\"]\n        strategy_gate = best_setup_wait[\"reasons\"][0] if best_setup_wait[\"reasons\"] else \"single-engine consensus established; risk-quality gate pending\"\n        blockers = list(best_setup_wait[\"reasons\"])\n        selected_consensus_score = best_setup_wait[\"score_basis\"]\n    else:\n        status = \"NO_ACTION\"\n        level, rr = level_rr(side, \"INTRADAY\") if side in (\"BUY\",\"SELL\") else (None,None)\n        trade_type = \"WAIT\"\n        horizon = horizon_for(\"WAIT\")\n        strategy_gate = \"2-of-9 strategy consensus is required — WAIT is not an entry\"\n        blockers = []\n        selected_consensus_score = max(\n            0.0,\n            float(max(buy_stats[\"aligned_count\"], sell_stats[\"aligned_count\"]) * 10.0)\n        )\n        if side == \"NEUTRAL\":\n            blockers.append(\"single-engine BUY/SELL evidence is tied or neutral\")\n        else:\n            side_stats = buy_stats if side == \"BUY\" else sell_stats\n            if side_stats[\"aligned_count\"] < 2:\n                blockers.append(f\"fewer than {MIN_CONFIRMED_STRATEGIES} distinct strategies currently agree\")\n            elif side_stats[\"price_count\"] < 3:\n                blockers.append(\"2-of-9 consensus exists but context/execution gates are not complete\")\n            else:\n                blockers.append(\"consensus exists but execution/risk conditions are not yet complete\")\n        if potential_candidates:\n            best = potential_candidates[0]\n            strategy_gate = best[\"strategy\"]\n            selected_consensus_score = max(selected_consensus_score, float(best.get(\"score_basis\", 0.0)))\n            blockers.append(\n                f\"BEST POTENTIAL: {best['side']} / {best['strategy']} — {len(best['reasons'])} condition(s) missing\"\n            )\n            blockers.extend(best[\"reasons\"][:3])\n    alignment = {\n        tf_name: dominant(strategy_by_tf.get(tf_name, []), minimum=2)\n        for tf_name in TF\n    }\n    four_h_votes = {\"BUY\": 0, \"SELL\": 0}\n    for method in strategy_by_tf.get(\"4h\", []):\n        sig = method.get(\"signal\")\n        if sig in four_h_votes:\n            four_h_votes[sig] += 1\n    if four_h_votes[\"BUY\"] > four_h_votes[\"SELL\"]:\n        regime_side = \"BUY\"\n    elif four_h_votes[\"SELL\"] > four_h_votes[\"BUY\"]:\n        regime_side = \"SELL\"\n    else:\n        regime_side = \"NEUTRAL\"\n    chart_timeframe = (selected.get(\"position_timeframe\") if selected and selected.get(\"position_timeframe\") in (\"4h\",\"1h\",\"15m\",\"5m\") else (\"15m\" if trade_type in (\"INTRADAY\",\"ADAPTIVE PRICE-MOVEMENT\") else (\"1h\" if trade_type == \"SWING/HOLD\" else \"5m\")))\n    chart_source = data.get(chart_timeframe, [])\n    chart_rows = [{\"timestamp\": row.get(\"timestamp\").isoformat() if hasattr(row.get(\"timestamp\"), \"isoformat\") else str(row.get(\"timestamp\")), \"open\": row.get(\"open\"), \"high\": row.get(\"high\"), \"low\": row.get(\"low\"), \"close\": row.get(\"close\"), \"volume\": row.get(\"volume\")} for row in chart_source[-120:]]\n    final_event_info = event_alignment(intel, symbol, side, m5, m15) if side in (\"BUY\",\"SELL\") else {\"state\":\"NONE\",\"gate\":True,\"score_adjustment\":0.0,\"events\":[],\"reason\":\"No directional event state\"}\n    strategy_score = float(selected_consensus_score) if side in (\"BUY\",\"SELL\") else 0.0\n    strategy_score = max(0.0, min(100.0, strategy_score))\n    execution_model = (selected.get(\"execution_trigger\") if selected else\n                       best_setup_wait.get(\"execution_trigger\") if best_setup_wait else\n                       (potential_candidates[0].get(\"execution_trigger\") if potential_candidates else {}))\n    execution_quality_base = execution_quality_score(rr, event_level, level, final_event_info.get(\"state\", \"NONE\"), (execution_model or {}).get(\"state\", \"UNAVAILABLE\"))\n    market_execution_score = float((execution_model or {}).get(\"execution_score\", 0.0) or 0.0)\n    execution_quality = round(0.55 * execution_quality_base + 0.45 * market_execution_score, 1)\n    final_strategy_summary = strategy_by_side.get(side, {\"count\":0,\"strategies\":[],\"categories\":[],\"tier\":\"NONE\",\"score\":0.0,\"all_matches\":[]}) if side in (\"BUY\",\"SELL\") else {\"count\":0,\"strategies\":[],\"categories\":[],\"tier\":\"NONE\",\"score\":0.0,\"all_matches\":[]}\n    def btc_special_analysis(candles, ticks, execution_model):\n        \"\"\"BTC-only regime layer. Order-flow/liquidation fields are proxies unless full LOB/funding/OI feeds are available.\"\"\"\n        out = {\n            \"active\": False, \"market\": \"BTC_24H_CRYPTO_ENV\", \"regime\": \"UNKNOWN\",\n            \"volatility_state\": \"UNKNOWN\", \"liquidity_state\": \"UNKNOWN\",\n            \"order_flow_proxy\": \"UNAVAILABLE\", \"liquidation_risk\": \"UNAVAILABLE\",\n            \"weekend_mode\": False, \"chase_risk\": \"UNKNOWN\",\n            \"reason\": \"not BTC/USD\"\n        }\n        if not candles:\n            return out\n        try:\n            rows = [r for r in candles[-48:] if all(r.get(k) is not None for k in (\"open\",\"high\",\"low\",\"close\"))]\n            if len(rows) < 10:\n                return out\n            closes = [float(r[\"close\"]) for r in rows]\n            ranges = [max(0.0, float(r[\"high\"])-float(r[\"low\"])) for r in rows]\n            atr = sum(ranges[-14:]) / max(1, min(14, len(ranges)))\n            recent_range = sum(ranges[-5:]) / 5.0\n            baseline_range = sum(ranges[-20:-5]) / max(1, len(ranges[-20:-5]))\n            vol_ratio = recent_range / baseline_range if baseline_range > 0 else 1.0\n            ret = (closes[-1] / closes[-6] - 1.0) if closes[-6] else 0.0\n            body = abs(closes[-1] - float(rows[-1][\"open\"])) / max(atr, 1e-12)\n            if vol_ratio >= 1.8 or body >= 1.5:\n                vol_state = \"HIGH\"\n            elif vol_ratio <= 0.65:\n                vol_state = \"COMPRESSED\"\n            else:\n                vol_state = \"NORMAL\"\n            if abs(ret) >= 0.012 and vol_state == \"HIGH\":\n                regime = \"EXPANSION\"\n            elif vol_state == \"COMPRESSED\":\n                regime = \"COMPRESSION\"\n            else:\n                regime = \"BALANCED\"\n            if ret > 0.001:\n                flow = \"BUY_PRESSURE_PROXY\"\n            elif ret < -0.001:\n                flow = \"SELL_PRESSURE_PROXY\"\n            else:\n                flow = \"BALANCED_FLOW_PROXY\"\n            em = execution_model or {}\n            spread_atr = em.get(\"spread_atr\")\n            if spread_atr is not None and float(spread_atr) <= 0.35:\n                liq = \"GOOD\"\n            elif spread_atr is not None and float(spread_atr) <= 0.90:\n                liq = \"ACCEPTABLE\"\n            else:\n                liq = \"STRESSED\"\n            liq_risk = \"HIGH\" if (vol_state == \"HIGH\" and abs(ret) >= 0.008) else (\"ELEVATED\" if vol_state == \"HIGH\" else \"NORMAL\")\n            chase = \"HIGH\" if body >= 1.5 or abs(ret) >= 0.01 else \"NORMAL\"\n            ts = rows[-1].get(\"timestamp\")\n            weekend = False\n            if ts:\n                try:\n                    weekend = parse_ts(ts).weekday() >= 5\n                except Exception:\n                    pass\n            out.update({\n                \"active\": True, \"regime\": regime, \"volatility_state\": vol_state,\n                \"liquidity_state\": liq, \"order_flow_proxy\": flow,\n                \"liquidation_risk\": liq_risk, \"weekend_mode\": weekend,\n                \"chase_risk\": chase,\n                \"volatility_ratio\": round(vol_ratio, 3),\n                \"recent_return\": round(ret * 100, 3),\n                \"atr_proxy\": atr,\n                \"reason\": \"24/7 BTC regime from recent closed-candle range/return plus live top-of-book execution quality\"\n            })\n        except Exception as exc:\n            out[\"reason\"] = f\"BTC special analysis unavailable: {type(exc).__name__}\"\n        return out\n    def opportunity_timing_bucket(timing):\n        \"\"\"Categorize time until the next scheduled execution-candle recheck; never predict a future trigger.\"\"\"\n        if not isinstance(timing, dict):\n            return \"WATCH\"\n        try:\n            recheck = parse_ts(timing.get(\"re_evaluate_at_utc\"))\n            minutes = max(0.0, (recheck - now_utc()).total_seconds() / 60.0)\n        except Exception:\n            return \"WATCH\"\n        if minutes <= 3:\n            return \"IMMEDIATE_0_3M\"\n        if minutes <= 5:\n            return \"VERY_NEAR_3_5M\"\n        if minutes <= 10:\n            return \"NEAR_5_10M\"\n        if minutes <= 15:\n            return \"NEAR_10_15M\"\n        if minutes <= 30:\n            return \"WATCH_15_30M\"\n        if minutes <= 60:\n            return \"WATCH_30_60M\"\n        return \"LATER_60M_PLUS\"\n\n    def signal_validity_for(symbol, status, trade_type, position_tf, execution_model, reference_timestamp):\n        env = asset_environment(symbol)\n        ttl_key = {\"SCALP\":\"5m\",\"INTRADAY\":\"15m\",\"SWING/HOLD\":\"1h\",\"ADAPTIVE PRICE-MOVEMENT\":\"15m\"}.get(trade_type, position_tf)\n        ttl_minutes = float(env.get(\"expiry_minutes\", {}).get(ttl_key, 30))\n        try:\n            ref_dt = parse_ts(reference_timestamp)\n        except Exception:\n            ref_dt = now_utc()\n        expires_at = ref_dt + timedelta(minutes=ttl_minutes)\n        now = now_utc()\n        expired = now >= expires_at\n        remaining_minutes = max(0.0, (expires_at - now).total_seconds() / 60.0)\n        reference_age_minutes = max(0.0, (now - ref_dt).total_seconds() / 60.0)\n        minimum_lead_ok = remaining_minutes >= float(EARLY_ENTRY_MINUTES)\n        early_reference_ok = reference_age_minutes >= float(EARLY_ENTRY_MINUTES)\n        exec_state = str((execution_model or {}).get(\"state\", \"UNAVAILABLE\"))\n        if expired:\n            validity = \"EXPIRED\"\n            reason = f\"signal lifetime {ttl_minutes:.0f}m elapsed from closed-candle reference\"\n        elif status.startswith(\"CONFIRMED\") and exec_state == \"EXECUTABLE_TRIGGER_CROSSED\" and minimum_lead_ok and early_reference_ok:\n            validity = \"VALID NOW | 3M WINDOW\"\n            reason = \"live executable bid/ask crossed the trigger while at least 3 minutes remain in the signal lifetime\"\n        elif exec_state == \"WAITING_FOR_TRIGGER\" and early_reference_ok and minimum_lead_ok:\n            validity = \"CONDITIONAL | 3M WINDOW\"\n            reason = \"structure is active; reference candle is at least 3 minutes old and at least 3 minutes remain before expiry\"\n        elif not early_reference_ok or not minimum_lead_ok:\n            validity = \"INVALID — 3M WINDOW\"\n            reason = f\"minimum 3-minute control window not satisfied: reference_age={reference_age_minutes:.2f}m, remaining={remaining_minutes:.2f}m\"\n        elif exec_state in (\"STALE_QUOTE\",\"WIDE_SPREAD\",\"MICROSTRUCTURE_UNSTABLE\",\"OVEREXTENDED\"):\n            validity = \"INVALID — EXECUTION CONDITION\"\n            reason = f\"live execution state is {exec_state}\"\n        elif status == \"NO_ACTION\":\n            validity = \"NOT VALID — NO ACTION\"\n            reason = \"no qualifying directional setup\"\n        else:\n            validity = \"CONDITIONAL\"\n            reason = \"setup exists but execution confirmation is incomplete\"\n        return {\n            \"state\": validity,\n            \"created_at\": ref_dt.isoformat(),\n            \"expires_at\": expires_at.isoformat(),\n            \"ttl_minutes\": ttl_minutes,\n            \"expired\": expired,\n            \"remaining_minutes\": round(remaining_minutes, 3),\n            \"reference_age_minutes\": round(reference_age_minutes, 3),\n            \"minimum_lead_minutes\": float(EARLY_ENTRY_MINUTES),\n            \"minimum_lead_ok\": bool(minimum_lead_ok and early_reference_ok),\n            \"reason\": reason,\n            \"reference_candle_timestamp\": ref_dt.isoformat(),\n        }\n    btc_analysis = btc_special_analysis(data.get(\"5m\", []), [], execution_model) if symbol == \"BTC/USD\" else {\"active\": False}\n    if symbol == \"BTC/USD\" and btc_analysis.get(\"active\"):\n        if btc_analysis.get(\"liquidity_state\") == \"STRESSED\":\n            blockers.append(\"BTC liquidity stressed: execution spread/top-of-book quality is insufficient\")\n        if btc_analysis.get(\"chase_risk\") == \"HIGH\" and status.startswith(\"CONFIRMED\"):\n            status = \"WAITING_FOR_TRIGGER\"\n            side = side\n            blockers.append(\"BTC expansion/chase risk: require retracement/trigger confirmation\")\n    def opportunity_timing(symbol, execution_tf, side, level, status):\n        \"\"\"Give WAIT candidates a concrete re-evaluation time and a conditional watch window without predicting a future signal.\"\"\"\n        tf_minutes = {\"5m\":5, \"15m\":15, \"1h\":60, \"4h\":240, \"1d\":1440}\n        tf = str(execution_tf or \"15m\").lower()\n        minutes = tf_minutes.get(tf, 15)\n        now = now_utc().replace(second=0, microsecond=0)\n        total = now.hour * 60 + now.minute\n        next_total = ((total // minutes) + 1) * minutes\n        days = next_total // 1440\n        next_dt = now.replace(hour=0, minute=0) + timedelta(days=days, minutes=next_total % 1440)\n        env = asset_environment(symbol)\n        expiry = int(env.get(\"expiry_minutes\", {}).get(tf, max(minutes * 3, 15)))\n        # WAIT is a monitoring window, not a promise that price will trigger.\n        window_start = next_dt\n        window_end = next_dt + timedelta(minutes=expiry)\n        eat = timezone(timedelta(hours=3))\n        out = {\n            \"status\": \"MONITORING_WINDOW\" if status != \"CONFIRMED_ENTRY\" else \"ACTIVE_ENTRY_WINDOW\",\n            \"execution_timeframe\": tf,\n            \"re_evaluate_at_utc\": next_dt.isoformat(),\n            \"re_evaluate_at_eat\": next_dt.astimezone(eat).isoformat(),\n            \"potential_window_start_utc\": window_start.isoformat(),\n            \"potential_window_end_utc\": window_end.isoformat(),\n            \"potential_window_start_eat\": window_start.astimezone(eat).isoformat(),\n            \"potential_window_end_eat\": window_end.astimezone(eat).isoformat(),\n            \"window_duration_minutes\": expiry,\n            \"time_basis\": \"next closed execution candle + asset-specific monitoring expiry\",\n            \"timezone\": \"UTC + EAT (Africa/Nairobi)\",\n        }\n        if isinstance(level, (list, tuple)) and len(level) >= 3 and level[0] is not None and level[1] is not None and side in (\"BUY\", \"SELL\"):\n            entry, sl, tp = float(level[0]), float(level[1]), level[2]\n            risk = abs(entry - sl)\n\n            out.update({\n                \"expected_entry\": entry,\n                \"expected_stop_loss\": sl,\n                \"expected_structural_take_profit\": float(tp) if tp is not None else None,\n                \"expected_rr\": (abs(float(tp)-entry)/risk if tp is not None and risk > 0 else None),\n                \"expected_take_profit\": float(tp) if tp is not None else None,\n                \"wait_condition\": (\n                    \"WAIT until a real opposing structural target supports the minimum required R:R; then revalidate entry/SL/TP\"\n                    if tp is None else \"No WAIT condition — genuine structural TP is available\"\n                ),\n            })\n        return out\n\n    trigger_reference_ts = (\n        (trigger_state.get(\"reference_candle_timestamp\") if isinstance(trigger_state, dict) else None)\n        or (data.get(trigger_tf, [{}])[-1].get(\"timestamp\") if data.get(trigger_tf) else now_utc().isoformat())\n    )\n    signal_validity = signal_validity_for(\n        symbol, status, trade_type, position_tf, execution_model, trigger_reference_ts\n    )\n    timing_info = opportunity_timing(\n        symbol,\n        (selected.get(\"position_timeframe\") if selected else best_setup_wait.get(\"position_timeframe\") if best_setup_wait else position_tf),\n        side,\n        level,\n        status,\n    )\n    timing_bucket = opportunity_timing_bucket(timing_info)\n    sniper_class = sniper_style(sniper, side, position_tf)\n    opportunity_class = sniper_class if sniper_class != \"NONE\" else (trade_type if trade_type else \"WATCH\")\n    results.append({\n        \"symbol\":symbol,\"category\":category,\"status\":status,\"side\":side,\"trade_type\":trade_type,\"horizon\":horizon,\n        \"entry_category\": (selected.get(\"entry_category\") if selected else best_setup_wait.get(\"entry_category\") if best_setup_wait else (potential_candidates[0].get(\"entry_category\") if potential_candidates else \"WAIT\")),\n        \"strategy_tier\": (selected.get(\"strategy_tier\") if selected else best_setup_wait.get(\"strategy_tier\") if best_setup_wait else (potential_candidates[0].get(\"strategy_tier\") if potential_candidates else final_strategy_summary.get(\"tier\",\"NONE\"))),\n        \"confirmed_strategy_count\": final_strategy_summary.get(\"count\",0),\n        \"confirmed_strategies\": final_strategy_summary.get(\"strategies\",[]),\n        \"strategy_categories\": final_strategy_summary.get(\"categories\",[]),\n        \"strategy_matches\": final_strategy_summary.get(\"all_matches\",[]),\n        \"opportunity_types\": sorted(set(opportunity_type_classifier(side, m15_support, sniper) + ([trade_type] if trade_type else []) + ([sniper_class] if sniper_class != \"NONE\" else []))),\n        \"setup_types\": opportunity_type_classifier(side, m15_support, sniper),\n        \"sniper\": sniper,\n        \"data_quality\": quality,\n        \"volatility_regime\": volatility,\n        \"position_timeframe\": (\n            selected.get(\"position_timeframe\", selected.get(\"signal_timeframes\", [\"UNSPECIFIED\"])[0])\n            if selected else (best_setup_wait.get(\"position_timeframe\", best_setup_wait.get(\"signal_timeframes\", [\"UNSPECIFIED\"])[0])\n            if best_setup_wait else \"UNSPECIFIED\")\n        ),\n        \"score\":strategy_score,\"strategy_score\":strategy_score,\n        \"execution_quality_score\":execution_quality,\"risk_score\":execution_quality,\"method_score\":support_score(m15_support,side) if side in (\"BUY\",\"SELL\") else 0.0,\n        \"execution_calibration\": {\n            \"source\": EXECUTION_CALIBRATION_SOURCE,\n            \"min_confirmed_score\": EXECUTION_SCORE_MIN,\n            \"high_quality_score\": EXECUTION_HIGH_QUALITY_SCORE,\n            \"high_quality\": market_execution_score >= EXECUTION_HIGH_QUALITY_SCORE\n        },\n        \"rr\":rr,\"levels\":level,\"alignment\":alignment,\"blockers\":blockers,\"events\":events,\"event_level\":event_level,\"event_alignment\":final_event_info,\n        \"fresh_structural_state\": structural_state_by_side.get(side, {}),\n        \"structural_invalidation\": bool(structural_state_by_side.get(side, {}).get(\"invalidated\", False)),\n        \"tp_policy\":\"STRUCTURAL_RR\",\n        \"tp_status\":\"VALID_STRUCTURAL_RR\" if (isinstance(level,(list,tuple)) and len(level)>=3 and rr is not None and float(rr) >= MIN_CONFIRMED_RR) else \"TP_UNSUPPORTED\",\n        \"tp_formula\":\"TP = genuine opposing structural target; report actual R:R (1:2, 1:3, 1:20, etc.)\",\n        \"opportunity_class\": opportunity_class,\n        \"opportunity_style\": (\"SNIPER SCALP\" if sniper_class == \"SNIPER SCALP\" else \"SNIPER INTRADAY\" if sniper_class == \"SNIPER INTRADAY\" else (\"SNIPER_ENTRY\" if sniper_class == \"SNIPER\" else trade_type)),\n        \"execution_timeframe\": (selected.get(\"position_timeframe\") if selected else best_setup_wait.get(\"position_timeframe\") if best_setup_wait else position_tf if side in (\"BUY\",\"SELL\") else \"UNSPECIFIED\"),\n        \"trading_session\": current_trading_session(),\n        \"trading_session_name\": current_trading_session().get(\"name\"),\n        \"trading_session_quality\": current_trading_session().get(\"quality\"),\n        \"target_distance_pips\": target_distance_pips(symbol, level[0], level[2]) if isinstance(level, (list, tuple)) and len(level) >= 3 and level[2] is not None else None,\n        \"opportunity_timing\": timing_info,\n        \"smc_base_required\": True,\n        \"smc_base_confirmed\": bool(smc_base_confirmed),\n        \"news_strategy_cooperation\": {\n            \"enabled\": True,\n            \"state\": news_cooperation,\n            \"direction\": news_direction,\n            \"aligned_with_strategy\": bool(news_aligned),\n            \"conflicts_with_strategy\": bool(news_conflict),\n            \"role\": \"CONFLUENCE_AND_EVENT_RISK_ONLY\"\n        },\n        \"sniper_micro_trigger\": {\n            \"enabled\": True,\n            \"mode\": \"ADDITIONAL_ENTRY_MODE\",\n            \"window_minutes\": [1, 3],\n            \"base_strategy\": \"SMC\",\n            \"requires_smc\": True,\n            \"requires_live_bid_ask\": True,\n            \"state\": (\n                \"ARMED_1_3M\"\n                if (\n                    smc_base_confirmed\n                    and side in (\"BUY\", \"SELL\")\n                    and timing_bucket == \"IMMEDIATE_0_3M\"\n                )\n                else \"NOT_ARMED\"\n            )\n        },\n        \"opportunity_timing_bucket\": timing_bucket,\n        \"minutes_to_recheck\": max(0.0, (parse_ts(timing_info.get(\"re_evaluate_at_utc\")) - now_utc()).total_seconds() / 60.0) if timing_info.get(\"re_evaluate_at_utc\") else None,\n        \"critical_levels\": critical_levels(data.get(\"1h\", []), symbol),\n        \"fx_currency_strength\": currency_strength.get(\"ranked\", []),\n        \"fx_strength_alignment\": currency_pair_alignment(currency_strength, symbol, side),\n        \"signal_type\": (\"CONFIRMED_BUY\" if status == \"CONFIRMED_ENTRY\" and side == \"BUY\" else \"CONFIRMED_SELL\" if status == \"CONFIRMED_ENTRY\" and side == \"SELL\" else \"WAIT/TP_UNSUPPORTED\" if side in (\"BUY\",\"SELL\") and (level is None or not (isinstance(level,(list,tuple)) and len(level)>=3 and level[2] is not None)) else f\"POTENTIAL_{side}\" if side in (\"BUY\",\"SELL\") else \"NO_DIRECTION\"),\n        \"potential_entry\": level[0] if isinstance(level, (list, tuple)) and len(level) >= 1 else None,\n        \"entry_trigger\": level[0] if isinstance(level, (list, tuple)) and len(level) >= 1 else None,\n        \"entry_price\": execution_model.get(\"executable_price\") if isinstance(execution_model, dict) and execution_model.get(\"state\") == \"EXECUTABLE_TRIGGER_CROSSED\" else None,\n        \"stop_loss\": level[1] if isinstance(level, (list, tuple)) and len(level) >= 2 else None,\n        \"take_profit\": level[2] if isinstance(level, (list, tuple)) and len(level) >= 3 else None,\n        \"take_profit_levels\": ([level[2]] if isinstance(level,(list,tuple)) and len(level)>=3 and level[2] is not None and rr is not None else []),\n        \"structural_reference_targets\": (target_ladder({\"SCALP\": m5, \"INTRADAY\": m15, \"SWING/HOLD\": h1}.get(trade_type, m15), side, level[0]) if isinstance(level,(list,tuple)) and len(level)>=1 and side in (\"BUY\",\"SELL\") else []),\n        \"entry_basis\": \"conditional trigger derived from latest CLOSED execution candle; forming candle never votes\",\n        \"signal_reason\": (\n            f\"{side} | category={((selected.get('entry_category') if selected else best_setup_wait.get('entry_category') if best_setup_wait else 'WAIT'))} | \"\n            f\"confirmed strategies={final_strategy_summary.get('count',0)} | \"\n            f\"{', '.join(final_strategy_summary.get('strategies',[])[:6]) or 'no complete strategy cluster'} | \"\n            f\"agree across {', '.join(sorted(consensus_stats(side)['aligned_tfs'])) or 'no operational'}; \"\n            f\"monthly master bias={monthly_side}; opposed strategies=\"\n            f\"{', '.join(sorted(consensus_stats(side)['opposed_names'])) or 'none'}\"\n            if side in (\"BUY\",\"SELL\") else \"No directional consensus\"\n        ),\n        \"execution_trigger\": (\n            selected.get(\"execution_trigger\") if selected else\n            best_setup_wait.get(\"execution_trigger\") if best_setup_wait else\n            (potential_candidates[0].get(\"execution_trigger\") if potential_candidates else None)\n        ),\n        \"execution_model\": execution_model,\n        \"asset_environment\": asset_condition,\n        \"asset_strategy_profile\": (asset_environment(symbol) if str(symbol).upper() == \"XAU/USD\" else {\"name\": \"STANDARD_FX_ENV\"}),\n        \"btc_special_analysis\": btc_analysis,\n        \"entry_validity\": signal_validity,\n        \"signal_expiry\": signal_validity,\n        \"executable_entry\": execution_model.get(\"executable_price\") if isinstance(execution_model, dict) else None,\n        \"bid\": execution_model.get(\"bid\") if isinstance(execution_model, dict) else None,\n        \"ask\": execution_model.get(\"ask\") if isinstance(execution_model, dict) else None,\n        \"spread\": execution_model.get(\"spread\") if isinstance(execution_model, dict) else None,\n        \"spread_bps\": execution_model.get(\"spread_bps\") if isinstance(execution_model, dict) else None,\n        \"quote_age_seconds\": execution_model.get(\"quote_age_seconds\") if isinstance(execution_model, dict) else None,\n        \"execution_state\": execution_model.get(\"state\") if isinstance(execution_model, dict) else \"NO_LIVE_QUOTE\",\n        \"slippage_proxy_atr\": execution_model.get(\"slippage_proxy_atr\") if isinstance(execution_model, dict) else None,\n        \"implementation_shortfall_bps\": execution_model.get(\"implementation_shortfall_bps\") if isinstance(execution_model, dict) else None,\n        \"adverse_move_bps\": execution_model.get(\"adverse_move_bps\") if isinstance(execution_model, dict) else None,\n        \"spread_cost_bps\": execution_model.get(\"spread_cost_bps\") if isinstance(execution_model, dict) else None,\n        \"yield_diff\":yield_diff,\"correlations\":correlations,\"rss_news_risk\":rss_news_level,\"rss_news_events\":rss_news_events,\n        \"fvg\":{\"15m\":m15_fvg,\"5m\":m5_fvg},\"fvg_signals\":{\"15m\":m15_fvg_sig,\"5m\":m5_fvg_sig},\n        \"methods\":m15_support,\"confirmation_methods\":m15_support,\"trigger_methods\":m5_support,\"h1_methods\":h1_support,\n        \"regime\":regime_side,\"technical_score\":strategy_score,\"strategy_gate\":strategy_gate,\n        \"monthly_bias\": monthly_side,\n        \"signal_timeframes\": sorted(signal_timeframes(side)) if side in (\"BUY\",\"SELL\") else [],\n        \"position_timeframe\": (\n            selected.get(\"position_timeframe\") if selected else\n            best_setup_wait.get(\"position_timeframe\") if best_setup_wait else\n            position_tf if side in (\"BUY\",\"SELL\") else \"UNSPECIFIED\"\n        ),\n        \"alignment_chain\": alignment_chain(side) if side in (\"BUY\",\"SELL\") else {\"agreed\":[],\"count\":0,\"ratio\":0.0},\n        \"timeframe_consensus\": tf_consensus,\n        \"strategy_consensus\": (\n            {\n                \"aligned\": sorted(consensus_stats(side)[\"aligned_names\"]) if side in (\"BUY\",\"SELL\") else [],\n                \"opposed\": sorted(consensus_stats(side)[\"opposed_names\"]) if side in (\"BUY\",\"SELL\") else [],\n                \"timeframes\": sorted(consensus_stats(side)[\"aligned_tfs\"]) if side in (\"BUY\",\"SELL\") else [],\n            }\n        ),\n        \"potential\":potential_candidates[0] if potential_candidates else None,\n        \"setup_wait\":best_setup_wait,\n        \"smc_context\":{\"1mo\":smc_monthly,\"1d\":smc_1d,\"4h\":smc_4h,\"1h\":smc_1h,\"15m\":smc_15m,\"5m\":smc_5m},\n        \"supporting\":{\"1h\":h1_support,\"15m\":m15_support,\"5m\":m5_support},\n        \"chart_timeframe\": chart_timeframe,\"chart_candles\": chart_rows,\n    })\ndef top_opportunity_quality_gate(r):\n    # This is a display/qualification gate only; it never creates a trade.\n    if str(r.get(\"symbol\", \"\")).upper() not in TOP_OPPORTUNITY_SYMBOLS:\n        return False\n    if r.get(\"side\") not in (\"BUY\", \"SELL\"):\n        return False\n    deep_score = float(r.get(\"strategy_score\", 0.0) or 0.0)\n    execution_quality = float(r.get(\"execution_quality_score\", 0.0) or 0.0)\n    strategy_count = int(r.get(\"confirmed_strategy_count\", 0) or 0)\n    rr = r.get(\"rr\")\n    tp = r.get(\"take_profit\")\n    tp_status = str(r.get(\"tp_status\", \"\"))\n    rr_ok = rr is not None and float(rr) >= TOP_OPPORTUNITY_MIN_RR\n    tp_ok = tp is not None and tp_status == \"VALID_STRUCTURAL_RR\"\n    quality_ok = (\n        deep_score >= TOP_OPPORTUNITY_MIN_DEEP_SCORE\n        and execution_quality >= TOP_OPPORTUNITY_MIN_EXECUTION_QUALITY\n        and strategy_count >= TOP_OPPORTUNITY_MIN_STRATEGIES\n        and rr_ok and tp_ok\n    )\n    if not quality_ok:\n        return False\n    if r.get(\"structural_invalidation\"):\n        return False\n    return r.get(\"status\") in (\"WAITING_FOR_TRIGGER\", \"CONFIRMED_ENTRY\", \"NO_ACTION\", \"WAIT/TP_UNSUPPORTED\")\n\ndef replacement_rank(r):\n    a = r.get(\"fx_strength_alignment\") or {}\n    return (1 if a.get(\"state\") == \"ALIGNED\" else 0, float(a.get(\"score\", 0.0) or 0.0), 1 if r.get(\"status\") == \"CONFIRMED_ENTRY\" else 0, float(r.get(\"strategy_score\", 0.0) or 0.0), float(r.get(\"rr\", 0.0) or 0.0))\nreplacement_pool = [r for r in results if r.get(\"category\") == \"forex\" and r.get(\"side\") in (\"BUY\",\"SELL\")]\nreplacement_pool.sort(key=replacement_rank, reverse=True)\nfor rank, r in enumerate(replacement_pool, 1):\n    r[\"replacement_rank\"] = rank\nrejected_target_pairs = [r for r in results if any(\"STRUCTURAL_TP_BELOW_\" in str(b) for b in r.get(\"blockers\", []))]\nbest_replacement = next((r for r in replacement_pool if r.get(\"status\") in (\"CONFIRMED_ENTRY\",\"WAITING_FOR_TRIGGER\")), None)\nreplacement_engine = {\n    \"active\": True,\n    \"strongest_vs_weakest\": (f\"{currency_strength['ranked'][0]['currency']}/{currency_strength['ranked'][-1]['currency']}\" if len(currency_strength.get(\"ranked\", [])) >= 2 else \"UNAVAILABLE\"),\n    \"rejected_for_structural_target\": [r.get(\"symbol\") for r in rejected_target_pairs],\n    \"replacement_selected\": best_replacement.get(\"symbol\") if best_replacement else None,\n    \"replacement_side\": best_replacement.get(\"side\") if best_replacement else None,\n    \"replacement_rank\": best_replacement.get(\"replacement_rank\") if best_replacement else None,\n    \"replacement_reason\": \"highest-ranked independently qualified FX pair after all gates\" if best_replacement else \"no independently qualified FX replacement currently available\",\n}\nprint(f\"PAIR REPLACEMENT ENGINE: ACTIVE | strongest-vs-weakest={replacement_engine['strongest_vs_weakest']} | rejected-target-pairs={len(rejected_target_pairs)} | replacement={replacement_engine['replacement_selected'] or 'NONE'}\")\nentries = sorted([r for r in results if r.get(\"status\") == \"CONFIRMED_ENTRY\" or str(r.get(\"status\", \"\")).startswith(\"CONFIRMED_\")],\n                 key=lambda x: (-x[\"strategy_score\"],))\n# Select up to five genuinely confirmed entries. Do not downgrade valid confirmations merely because\n# another pair ranked higher; all 20 FX pairs compete on the same fresh scan.\nentries = entries[:MAX_CONFIRMED_ENTRIES_PER_SCAN]\nintraday_entries = sorted([r for r in entries if r[\"trade_type\"] == \"INTRADAY\"], key=lambda x: x[\"strategy_score\"], reverse=True)\nscalp_entries = sorted([r for r in entries if r[\"trade_type\"] == \"SCALP\"], key=lambda x: x[\"strategy_score\"], reverse=True)\nswing_entries = sorted([r for r in entries if r[\"trade_type\"] == \"SWING/HOLD\"], key=lambda x: x[\"strategy_score\"], reverse=True)\nwaiting = sorted(\n    [r for r in results if r.get(\"status\") == \"WAITING_FOR_TRIGGER\"],\n    key=lambda x: float(x.get(\"strategy_score\", 0) or 0),\n    reverse=True,\n)\ndef print_candidate(title, rank, r):\n    lv = r[\"levels\"]\n    rr_text = f\"{r['rr']:.2f}R\" if r[\"rr\"] is not None else \"UNAVAILABLE\"\n    print(f\"{title} #{rank} {r['symbol']} | {r['side']} | {r['trade_type']} | OPPORTUNITIES {', '.join(r.get('opportunity_types', [])) or 'CONSENSUS'} | STRATEGY SCORE {r['strategy_score']:.1f}/100 | EXECUTION CONDITION {r['execution_quality_score']:.1f}/100 | R:R {rr_text}\")\n    print(f\"   🎯 OPPORTUNITY CLASS: {r.get('opportunity_class','UNSPECIFIED')} | STYLE: {r.get('opportunity_style','UNSPECIFIED')}\")\n    print(f\"   ⏱ EXECUTION TIMEFRAME: {(r.get('execution_timeframe') or r.get('position_timeframe') or 'UNSPECIFIED').upper()} | 🌍 SESSION: {(r.get('trading_session') or {}).get('name','UNAVAILABLE')} | {(r.get('trading_session') or {}).get('quality','UNAVAILABLE')}\")\n    print(\"   Alignment:\", \" | \".join(f\"{k.upper()} {v}\" for k,v in r[\"alignment\"].items()))\n    print(\"   MONTHLY MASTER BIAS:\", r.get(\"monthly_bias\", \"UNAVAILABLE\"))\n    print(\"   SIGNAL TIMEFRAMES:\", \", \".join(r.get(\"signal_timeframes\", [])) or \"NONE\")\n    print(\"   FVG:\", \" | \".join(f\"{tf.upper()} {sig}\" for tf,sig in r[\"fvg_signals\"].items()))\n    if lv:\n        print(f\"   Entry {lv[0]:.6g} | SL {lv[1]:.6g} | TP {lv[2]:.6g} | ATR {lv[3]:.6g}\")\n    print(\"   ENTRY BASIS:\", r.get(\"entry_basis\", \"latest closed execution candle close\"))\n    tp_levels = r.get(\"take_profit_levels\", [])\n    if tp_levels:\n        print(\"   TP LADDER:\", \" | \".join(f\"TP{i+1} {x:.6g}\" for i, x in enumerate(tp_levels)))\n    print(\"   WHY THIS SIGNAL:\", r.get(\"signal_reason\", \"consensus explanation unavailable\"))\n    why = \"; \".join(m[\"why\"] for m in r[\"methods\"] if m[\"signal\"] == r[\"side\"])\n    if r.get(\"trade_type\") == \"ADAPTIVE PRICE-MOVEMENT\":\n        print(\"   MODE: independent price-movement consensus; SMC base context is still evaluated\")\n    else:\n        print(\"   SMC CONTEXT:\", r[\"smc_context\"].get(\"15m\", {})[\"why\"], f\"| core score {r[\"smc_context\"].get(\"15m\", {})[\"score\"]}/90\")\n    print(\"   SUPPORTING METHODS:\", why or \"no supporting confirmation\")\n    print(\"   Event/news risk:\", r[\"event_level\"], \"|\", r.get(\"event_alignment\", {}).get(\"state\", \"UNAVAILABLE\"))\n    print(\"   RSS risk intelligence:\", r.get(\"rss_news_risk\", \"UNAVAILABLE\"))\n    if r.get(\"rss_news_events\"):\n        print(\"   Relevant news:\", \" | \".join(\n            x.get(\"title\", \"\") for x in r[\"rss_news_events\"][:3]\n        ))\n    print(\"   Yield differential:\", f\"{r['yield_diff']:+.3f} percentage points\" if r[\"yield_diff\"] is not None else \"UNAVAILABLE\")\n    print(\"   Correlation context:\", \"; \".join(f\"{b} {c:+.2f}\" for b, c in r[\"correlations\"]) or \"UNAVAILABLE\")\n    print(\"   Strategy gate:\", r[\"strategy_gate\"])\n    sc = r.get(\"strategy_consensus\", {})\n    if sc:\n        print(\"   INSTITUTIONAL ENGINE:\", \", \".join(sc.get(\"aligned\", [])) or \"NONE\",\n              \"| opposed:\", \", \".join(sc.get(\"opposed\", [])) or \"NONE\",\n              \"| TFs:\", \", \".join(sc.get(\"timeframes\", [])) or \"NONE\")\n    if r[\"trade_type\"] == \"INTRADAY\":\n        print(\"   Required gates: 2-of-9 strategy consensus with sufficient breadth | real >=2R | event/news context only (not a mandatory gate)\")\n    elif r[\"trade_type\"] == \"SCALP\":\n        print(\"   Required gates: 2-of-9 strategy consensus with lower-TF agreement | real >=2R | event/news context only (not a mandatory gate)\")\n    elif r[\"trade_type\"] == \"SWING/HOLD\":\n        print(\"   Required gates: 2-of-9 strategy consensus with higher-TF agreement | real >=2R | event/news context only (not a mandatory gate)\")\n    elif r.get(\"trade_type\") == \"ADAPTIVE PRICE-MOVEMENT\":\n        print(\"   Required gates: 3 distinct price-movement methods across >=2 TFs | real >=2R | event/news context only (not a mandatory gate)\")\n    if r[\"blockers\"]:\n        print(\"   NEXT CONDITION:\")\n        for b in (r.get(\"blockers\") or [])[:4]:\n            print(f\"     • {b}\")\n    print()\nprint()\nprint(\"=\" * 72)\nprint(\"🔥 SMC FOREX / MULTI-ASSET INTELLIGENT ENTRY RANKING\")\nprint(\"=\" * 72)\nprint(\"9-STRATEGY ENGINE: 9 EQUAL RULE MODULES | MONTHLY MASTER FILTER | ANY-TF SIGNALS ONLY WITH 2-OF-9 AGREEMENT\")\nprint(\"At least 2 distinct strategies must agree, with completed monthly context, required timeframe context, real >=2R, risk gates, and live top-of-book trigger.\")\nprint(\"Directional strategy denominator: 9; neutral strategies are informational and do not count as opposition.\")\nadaptive_entries = [r for r in entries if r.get(\"trade_type\") == \"ADAPTIVE PRICE-MOVEMENT\"]\nsniper_ranked = sorted(\n    [r for r in results if r.get(\"sniper\")],\n    key=lambda r: (float((r.get(\"sniper\") or {}).get(\"score\", 0) or 0), float((r.get(\"sniper\") or {}).get(\"rr\", 0) or 0)),\n    reverse=True,\n)\nsniper_ranked = []\ndeveloping_count = sum(\n    1 for r in results\n    if r.get(\"potential\") or r.get(\"status\") == \"WAITING_FOR_TRIGGER\"\n)\nprint(f\"Confirmed: {len(entries)} | Developing: {developing_count} | Intraday: {len(intraday_entries)} | Scalp: {len(scalp_entries)} | Swing/Hold: {len(swing_entries)} | Adaptive: {len(adaptive_entries)} | Precision candidates: {len(sniper_ranked)} | Waiting: {len(waiting)}\")\nprint(f\"Instruments fully analyzed: {complete} | Incomplete: {incomplete}\")\nprint()\nprint(\"DAILY INTELLIGENCE + MONTHLY MASTER FILTER\")\nprint(\"  Monthly filter: completed 1MO single-engine consensus; lower-TF signals require agreement\")\nprint(f\"  Economic calendar records: {len(intel['calendar'])}\")\nprint(f\"  Macro: {'AVAILABLE' if intel['macro'].get('cpi_yoy') or intel['macro'].get('policy_rate') else 'UNAVAILABLE'}\")\nprint(f\"  Government yields: {', '.join(sorted(intel['yields'])) if intel['yields'] else 'UNAVAILABLE'}\")\nprint(f\"  COT: {', '.join(intel['cot']) if intel['cot'] else 'UNAVAILABLE'}\")\nprint(f\"  External RSS news: {len(intel['news'])} items\" if intel[\"news\"] else \"  External RSS news: UNAVAILABLE\")\nprint(f\"  RSS feeds configured: {len(NEWS_URLS)}\")\nprint(f\"  Live feed capability: {intel['live']}\")\nprint(\"  ML: LOCAL STATISTICAL MODEL (no institutional ML claimed)\")\nprint()\nprint(\"=\" * 72)\nprint(\"\\U0001F4CA ANALYZED CURRENCIES — OPPORTUNITY CLASS + TIMEFRAME + TRADING SESSION\")\nprint(\"Every completed analysis is listed here, including WATCH/NO_ACTION instruments.\")\nfor r in sorted(results, key=lambda x: x.get(\"symbol\", \"\")):\n    session = r.get(\"trading_session\") or current_trading_session()\n    cls = r.get(\"opportunity_class\") or \"NO_CURRENT_OPPORTUNITY\"\n    style = r.get(\"opportunity_style\") or \"NONE\"\n    tf = r.get(\"execution_timeframe\") or r.get(\"position_timeframe\") or \"UNSPECIFIED\"\n    status = r.get(\"status\") or \"UNAVAILABLE\"\n    print(f\"\\n💱 {r.get('symbol','UNKNOWN')} | {r.get('side','NEUTRAL')} | STATUS {status}\")\n    print(f\"   🎯 OPPORTUNITY CLASS: {cls} | STYLE: {style}\")\n    print(f\"   ⏱ EXECUTION TIMEFRAME: {str(tf).upper()}\")\n    print(f\"   📚 SIGNAL TIMEFRAMES: {', '.join(r.get('signal_timeframes', [])) or 'NONE / CONDITIONAL'}\")\n    print(f\"   🌍 TRADING SESSION: {session.get('name','UNAVAILABLE')} | {session.get('quality','UNAVAILABLE')}\")\n    print(f\"   🕐 SESSION CLOCK: UTC {session.get('utc','UNAVAILABLE')} | EAT {session.get('eat','UNAVAILABLE')}\")\n    print(f\"   📊 OPPORTUNITY TYPES: {', '.join(r.get('opportunity_types', [])) or 'NONE'}\")\n    print(f\"   🔎 PRECISION/SNIPER: {'YES' if r.get('sniper') and r.get('sniper',{}).get('side') == r.get('side') else 'NO'}\")\nprint()\nprint(\"=\" * 72)\nprint(\"🎯 TOP OPPORTUNITIES — COMPLETE 20-FX COMPETITION\")\nprint(\"All 20 FX pairs completed the same deep institutional pipeline before ranking.\")\nprint(\"Confirmation still requires monthly/context alignment, 2-of-9 strategy consensus, genuine structural TP, >=2R, and fresh live bid/ask trigger gates.\")\nall_potential = []\nfor r in results:\n    if r.get(\"side\") not in (\"BUY\",\"SELL\"): continue\n    if r.get(\"potential\") or r.get(\"potential_entry\") is not None or r.get(\"status\") in (\"WAITING_FOR_TRIGGER\", \"WAIT/TP_UNSUPPORTED\"):\n        all_potential.append(r)\nall_potential.sort(key=lambda x:(float(x.get(\"strategy_score\",0) or 0), float(x.get(\"execution_quality_score\",0) or 0)), reverse=True)\nfor r in all_potential[:TOP_N]:\n    timing=r.get(\"opportunity_timing\") or {}\n    print(f\"\\U0001F440 {r['symbol']} | {r['side']} | {r.get('trade_type','UNSPECIFIED')} | TF {r.get('position_timeframe') or 'UNSPECIFIED'} | {r.get('status','POTENTIAL')}\")\n    print(f\"   ENTRY {r.get('entry_price') if r.get('entry_price') is not None else 'WAIT'} | TRIGGER {r.get('entry_trigger') if r.get('entry_trigger') is not None else r.get('potential_entry') or 'WAIT'} | SL {r.get('stop_loss') or 'WAIT'} | TP {r.get('take_profit') or 'WAIT — minimum required R:R unsupported'} | R:R {r.get('rr') if r.get('rr') is not None else 'WAIT'}\")\n    print(f\"   SIGNAL TFs: {', '.join(r.get('signal_timeframes',[])) or 'CONDITIONAL'} | CLASS: {r.get('opportunity_class') or ', '.join(r.get('setup_types',[]) or r.get('opportunity_types',[]) or ['POTENTIAL'])} | SESSION: {(r.get('trading_session') or {}).get('name','UNAVAILABLE')}\")\n    print(f\"   RE-EVALUATE: {timing.get('re_evaluate_at_eat') or 'NEXT SCAN'} | POTENTIAL WINDOW: {timing.get('potential_window_start_eat') or 'NEXT WINDOW'} -> {timing.get('potential_window_end_eat') or 'WINDOW END'}\")\n    print(f\"   BLOCKERS/CONDITIONS: {' | '.join((r.get('blockers') or (r.get('potential') or {}).get('reasons') or ['awaiting trigger/context/risk confirmation'])[:4])}\")\nif not all_potential: print(\"No near-complete potential setups.\")\nprint()\nprint(\"\\U000023F3 WAITING_FOR_TRIGGER / BLOCKERS\")\nwaiting_pool = [r for r in results if r.get(\"status\") in (\"WAITING_FOR_TRIGGER\", \"WAIT/TP_UNSUPPORTED\")]\nwaiting_pool.sort(key=lambda x:float(x.get(\"strategy_score\",0) or 0), reverse=True)\nfor r in waiting_pool[:TOP_N]:\n    timing=r.get(\"opportunity_timing\") or {}\n    print(f\"\\U000023F3 {r['symbol']} | {r['side']} | {r.get('trade_type','WAIT')} | TF {r.get('position_timeframe') or 'UNSPECIFIED'} | {r.get('signal_type','WAIT')}\")\n    print(f\"   EXPECTED ENTRY {r.get('entry_price') if r.get('entry_price') is not None else 'WAIT'} | TRIGGER {r.get('entry_trigger') if r.get('entry_trigger') is not None else r.get('potential_entry') or 'UNAVAILABLE'} | SL {r.get('stop_loss') or 'UNAVAILABLE'} | TP {r.get('take_profit') or 'WAIT — minimum required R:R unsupported'}\")\n    print(f\"   RE-EVALUATE: {timing.get('re_evaluate_at_eat') or 'NEXT SCAN'} | POTENTIAL WINDOW: {timing.get('potential_window_start_eat') or 'NEXT WINDOW'} -> {timing.get('potential_window_end_eat') or 'WINDOW END'}\")\n    [print(f\"     • {b}\") for b in (r.get(\"blockers\") or [\"trigger/context/risk gate pending\"])[:4]]\nif not waiting_pool: print(\"No active waiting blockers.\")\nprint()\nprint(\"🔎 PRECISION OPPORTUNITIES — SWEEP + DISPLACEMENT/MSS + FRESH FVG\")\nprint(\"Precision engine is a separate setup classifier, not an extra consensus vote. It never independently confirms an entry.\")\nif sniper_ranked:\n    for rank, r in enumerate(sniper_ranked[:TOP_N], 1):\n        s = r.get(\"sniper\") or {}\n        print(f\"🎯 #{rank} {r.get('symbol','?')} | {s.get('side',r.get('side','WAIT'))} | {s.get('status','WAIT')} | PRECISION SCORE {float(s.get('score',0) or 0):.1f}/100 | R:R {float(s.get('rr',0) or 0):.2f}R | Entry {s.get('entry','WAIT')} | SL {s.get('sl','WAIT')} | TP1 {s.get('tp','WAIT')}\")\n        print(f\"   ENTRY ZONE: {s['entry_zone'][0]:.6g}–{s['entry_zone'][1]:.6g} | distance {s['distance_atr']:.2f} ATR | in zone {s['in_entry_zone']}\")\n        print(f\"   Sweep {s['liquidity_sweep']} | Displacement {s['displacement']} | MSS {s['mss']} | Structure {s['structure']} | FVG {s['fvg_signal']} | HTF {s['higher_tf_context']}\")\n        print()\nelse:\n    print(\"No complete precision opportunity detected in the deep candidate set.\")\nprint()\nprint(\"🎯 TOP 5 FX OPPORTUNITY CARDS — 20-PAIR GLOBAL COMPETITION\")\nprint(\"-\" * 72)\n# TOP OPPORTUNITY cards are deep-analysis results from the complete 20-FX pool.\n# XAU/USD and BTC/USD are not eligible for this FX-only ranking.\ncard_rows = [\n    r for r in results\n    if top_opportunity_quality_gate(r)\n]\ncard_rows.sort(key=lambda x: float(x.get(\"strategy_score\", x.get(\"score\", 0)) or 0), reverse=True)\nif card_rows:\n    for r in card_rows[:TOP_N]:\n        side = r.get(\"side\")\n        status = r.get(\"status\") or (\"DEVELOPING\" if r.get(\"stage\") == \"DEVELOPING\" else \"DEVELOPING\")\n        trade = str(r.get(\"trade_type\") or r.get(\"opportunity_class\") or \"SCALP\").upper()\n        style = \"SCALP\" if \"SCALP\" in trade else trade\n        exec_tf = r.get(\"execution_timeframe\") or r.get(\"position_timeframe\") or (\"5M\" if \"5m\" in (r.get(\"timeframes\") or []) else \"5M\")\n        signal_tfs = r.get(\"signal_timeframes\") or r.get(\"timeframes\") or [\"1h\",\"15m\",\"5m\"]\n        session = r.get(\"trading_session\") or current_trading_session()\n        session_name = session.get(\"name\",\"UNAVAILABLE\") if isinstance(session, dict) else str(session)\n        session_quality = session.get(\"quality\",\"UNAVAILABLE\") if isinstance(session, dict) else \"UNAVAILABLE\"\n        clock = r.get(\"session_clock\") or {}\n        if not isinstance(clock, dict):\n            clock = {}\n        print(f\"💱 {r.get('symbol','UNKNOWN')} | {side} | STATUS {status}\")\n        print()\n        print(f\"🎯 OPPORTUNITY CLASS: {r.get(\"opportunity_class\") or trade}\")\n        print(f\"⏱ OPPORTUNITY TIMING: {r.get(\"opportunity_timing_bucket\",\"WATCH\")} | RECHECK IN {float(r.get(\"minutes_to_recheck\",0.0) or 0.0):.1f}m\")\n        print(f\"STYLE: {r.get(\"opportunity_style\") or style}\")\n        print()\n        print(f\"⏱ EXECUTION TIMEFRAME: {str(exec_tf).upper()}\")\n        print(f\"📚 SIGNAL TIMEFRAMES: {', '.join(str(x).upper() for x in signal_tfs)}\")\n        print()\n        print(f\"🌍 TRADING SESSION: {session_name}\")\n        print(f\"SESSION QUALITY: {session_quality}\")\n        print()\n        print(\"🕐 SESSION CLOCK:\")\n        print(f\"UTC {clock.get('utc') or now_utc().strftime('%Y-%m-%d %H:%M:%S')}\")\n        print(f\"EAT {clock.get('eat') or now_eat().strftime('%Y-%m-%d %H:%M:%S')}\")\n        print()\n        opp_types = r.get(\"opportunity_types\") or r.get(\"setup_types\") or [\"Directional setup\"]\n        print(\"📊 OPPORTUNITY TYPES: \" + \", \".join(str(x) for x in opp_types))\n        sniper = r.get(\"precision_sniper\") or r.get(\"sniper\")\n        sniper_yes = bool(sniper and isinstance(sniper, dict) and sniper.get(\"side\") == side)\n        print(f\"🔎 PRECISION/SNIPER: {'YES' if sniper_yes else 'NO'}\")\n        print()\nelse:\n    print(\"No directional opportunity cards available.\")\nprint()\nsections = [\n    (\"📈 CONFIRMED INTRADAY SIGNALS\", intraday_entries),\n    (\"⚡ CONFIRMED SCALP SIGNALS\", scalp_entries),\n    (\"🏦 CONFIRMED SWING/HOLD SIGNALS\", swing_entries),\n    (\"🟢 CONFIRMED ADAPTIVE PRICE-MOVEMENT SIGNALS\", adaptive_entries),\n]\nany_entry = False\nfor heading, bucket in sections:\n    if bucket:\n        any_entry = True\n        print(heading)\n        for rank, r in enumerate(bucket[:TOP_N], 1):\n            print_candidate(heading.split()[0], rank, r)\n        print()\n    else:\n        print(f\"{heading}: NONE\")\n        print()\ndeveloping_setups = any(\n    r.get(\"potential\") or r.get(\"status\") == \"WAITING_FOR_TRIGGER\"\n    for r in results\n)\nif not any_entry and not developing_setups:\n    print(\"⏸️ NO_ACTION — no qualified or developing setup currently satisfies the scanner gates\")\n    print()\nelif not any_entry:\n    print(\"⏳ NO_CONFIRMED_ENTRY — developing setups exist; waiting for trigger, alignment, risk, or execution gates\")\n    print()\nsetup_waiting = sorted(\n    [r for r in results if r.get(\"status\") == \"WAITING_FOR_TRIGGER\"],\n    key=lambda x: (\n        float(x.get(\"strategy_score\", 0) or 0),\n        float(x.get(\"rr\", -1) if x.get(\"rr\") is not None else -1),\n    ),\n    reverse=True,\n)\nprint(\"🟡 VALID SETUPS — WAIT / RISK CONDITION\")\nif setup_waiting:\n    for rank, r in enumerate(setup_waiting[:TOP_N], 1):\n        rr_val = r.get(\"rr\")\n        rr_text = f\"{float(rr_val):.2f}R\" if rr_val is not None else \"UNAVAILABLE\"\n        print(f\"🟡 #{rank} {r.get('symbol','?')} | {r.get('side','WAIT')} | {r.get('trade_type','WAIT')} | STRATEGY SCORE {float(r.get('strategy_score',0) or 0):.1f}/100 | EXECUTION CONDITION {float(r.get('execution_quality_score',0) or 0):.1f}/100 | R:R {rr_text}\")\n        print(f\"   Holding horizon: {r.get('horizon','UNSPECIFIED')}\")\n        for reason in (r.get(\"blockers\") or [])[:4]:\n            print(f\"   • {reason}\")\n        print(\"   STATUS: WAITING_FOR_TRIGGER\")\n        print(\"   Confirmation requires all remaining execution/risk gates to pass.\")\n        print()\nelse:\n    print(\"No fully formed setup is currently waiting only on a risk-quality condition.\")\nprint()\ntop_opportunity_results = [\n    r for r in results\n    if top_opportunity_quality_gate(r)\n]\ntop_opportunity_results.sort(\n    key=lambda x: (\n        1 if x.get(\"status\") == \"CONFIRMED_ENTRY\" else 0,\n        float(x.get(\"execution_quality_score\", 0) or 0),\n        float(x.get(\"strategy_score\", 0) or 0),\n        float(x.get(\"rr\", -1) if x.get(\"rr\") is not None else -1),\n    ),\n    reverse=True,\n)\nprint(\"🎯 TOP OPPORTUNITY CANDIDATES — DEEP QUALIFICATION\")\nif top_opportunity_results:\n    for r in top_opportunity_results:\n        rr = r.get(\"rr\")\n        rr_text = f\"{float(rr):.2f}R\" if rr is not None else \"DEVELOPING/UNAVAILABLE\"\n        print(\n            f\"   {r.get('symbol')} | {r.get('side','WAIT')} | \"\n            f\"{r.get('status','NO_ACTION')} | SCORE {float(r.get('strategy_score',0) or 0):.1f} | \"\n            f\"R:R {rr_text} | EXEC QUALITY {float(r.get('execution_quality_score',0) or 0):.1f}\"\n        )\nelse:\n    print(\"   NONE — no FX setup currently satisfies the deep-data gates.\")\nprint()\n\npotential_display = []\nfor r in results:\n    p = r.get(\"potential\")\n    if r.get(\"status\") in (\"WAITING_FOR_TRIGGER\", \"NO_ACTION\") and p:\n        pl = p.get(\"level\")\n        pr = p.get(\"rr\")\n        valid_potential = (\n            isinstance(pl, (list, tuple)) and len(pl) >= 3\n            and all(x is not None and isinstance(x, (int, float)) and math.isfinite(float(x)) for x in pl[:3])\n            and pr is not None and float(pr) >= float(MIN_CONFIRMED_RR)\n        )\n        if not valid_potential:\n            continue\n        missing = [x for x in (p.get(\"reasons\") or []) if x.startswith(\"MISSING:\")]\n        potential_display.append((len(missing), -float(r.get(\"strategy_score\",0) or 0), r, p))\npotential_display.sort(key=lambda x: (x[0], x[1]))\nprint(\"👀 REFERENCE SETUPS — NOT ORDERS\")\nif potential_display:\n    for rank, (_, __, r, p) in enumerate(potential_display[:TOP_N], 1):\n        potential_rr = p.get(\"rr\")\n        potential_level = p.get(\"level\")\n        potential_eq = execution_quality_score(\n            potential_rr,\n            r.get(\"event_level\", \"NONE\"),\n            potential_level,\n            (r.get(\"event_alignment\") or {}).get(\"state\", \"NONE\"),\n        ) if potential_rr is not None and isinstance(potential_level, (list, tuple)) and len(potential_level) >= 2 else 0.0\n        print(f\"👀 #{rank} {r.get('symbol','?')} | {p.get('side','WAIT')} | {p.get('strategy','POTENTIAL')} | STRATEGY SCORE {float(r.get('strategy_score',0) or 0):.1f}/100 | POTENTIAL EXECUTION CONDITION {potential_eq:.1f}/100 | R:R {float(potential_rr):.2f}R\" if potential_rr is not None else f\"👀 #{rank} {r.get('symbol','?')} | {p.get('side','WAIT')} | {p.get('strategy','POTENTIAL')} | STRATEGY SCORE {float(r.get('strategy_score',0) or 0):.1f}/100 | POTENTIAL EXECUTION CONDITION {potential_eq:.1f}/100 | R:R UNAVAILABLE\")\n        level = p.get(\"level\")\n        valid_level = (\n            isinstance(level, (list, tuple)) and len(level) >= 3\n            and all(isinstance(x, (int, float)) and math.isfinite(float(x)) for x in level[:3])\n        )\n        if valid_level:\n            entry_px, sl_px, tp_px = [float(x) for x in level[:3]]\n            rr_px = p.get(\"rr\")\n            rr_label = (\n                f\"{float(rr_px):.2f}R\"\n                if isinstance(rr_px, (int, float)) and math.isfinite(float(rr_px))\n                else \"UNAVAILABLE\"\n            )\n            print(f\"   REFERENCE ENTRY {entry_px:.6g} | SL {sl_px:.6g} | TP {tp_px:.6g} | R:R {rr_label}\")\n            print(f\"   PLAN: {p.get('plan_basis','CLOSED_CANDLE_STRUCTURAL_BREAKOUT')} | PLAN STATUS: {p.get('plan_status','STRUCTURAL_PLAN')}\")\n            p_tfs = {\"SCALP\": m5, \"INTRADAY\": m15, \"SWING/HOLD\": h1}.get(p.get(\"strategy\"), m15)\n            p_targets = target_ladder(p_tfs, p[\"side\"], entry_px)\n            if p_targets:\n                print(\"   STRUCTURAL TP LADDER:\", \" | \".join(f\"TP{i+1} {float(x):.6g}\" for i, x in enumerate(p_targets)))\n        else:\n            print(\"   REFERENCE ENTRY/SL/TP: PLAN_UNAVAILABLE — one or more structural levels are unavailable; no synthetic levels generated\")\n        print(f\"   Holding horizon: {p.get('horizon','UNSPECIFIED')}\")\n        for reason in (p.get(\"reasons\") or []):\n            print(f\"   • {reason}\")\n        print(\"   STATUS: WAITING_FOR_TRIGGER\")\n        print()\nelse:\n    print(\"No near-complete potential setups.\")\nprint()\nprint(\"⏳ WAITING_FOR_TRIGGER / BLOCKERS\")\nif waiting:\n    for rank, r in enumerate(waiting[:TOP_N], 1):\n        print(f\"⏳ #{rank} {r.get('symbol','?')} | {r.get('side','WAIT')} | CLASS {r.get('opportunity_class') or r.get('trade_type','WAIT')} | TF {(r.get('execution_timeframe') or r.get('position_timeframe') or 'UNSPECIFIED').upper()} | SESSION {(r.get('trading_session') or {}).get('name','UNAVAILABLE')} | STATUS WAITING_FOR_TRIGGER | STRATEGY SCORE {float(r.get('strategy_score',0) or 0):.1f}/100 | EXECUTION CONDITION {float(r.get('execution_quality_score',0) or 0):.1f}/100 | EVENT/NEWS {r.get('event_level','NONE')}\")\n        print(\"   Holding horizon: NOT APPLICABLE — WAIT / no entry\")\n        print(f\"   OPPORTUNITY TIMING: {r.get('opportunity_timing_bucket','WATCH')} | RECHECK IN {float(r.get('minutes_to_recheck',0.0) or 0.0):.1f}m | {r.get('opportunity_timing',{}).get('re_evaluate_at_eat','NEXT SCAN')}\")\n        for b in (r.get(\"blockers\") or [])[:5]:\n            print(f\"   • {b}\")\n        print()\nelse:\n    print(\"No additional WAIT candidates.\")\nprint()\nelapsed = time.monotonic() - SCAN_START\nprint(\"=\" * 72)\nprint(\"SCAN STATUS\")\nprint(\"=\" * 72)\nprint(f\"Elapsed scanner time: {elapsed:.1f}s / configured budget {RUNTIME_MINUTES*60:.1f}s\")\nprint(f\"Market-scan budget (with safety reserve): {SCAN_BUDGET_SECONDS:.1f}s\")\nprint(f\"Catalog instruments discovered: {len(catalog)}\")\nprint(f\"Eligible instruments selected: {len(instruments)}\")\nprint(f\"Full-universe FX pairs staged: {len(top_opportunity_candidates)} / {len(TOP_OPPORTUNITY_SYMBOLS)}\")\nprint(\"Predictive preselection: DISABLED\")\nprint(f\"Instruments deep-analyzed for TOP OPPORTUNITIES: {processed}\")\nprint(f\"TOP-5 FX competition symbols available: {len(opportunity_symbols_found)} / {len(TOP_OPPORTUNITY_SYMBOLS)}\")\nprint(\"Predictive fast filter: DISABLED\")\nprint(f\"Fully analyzed: {complete}\")\nprint(f\"Skipped as incomplete: {incomplete}\")\nprint(f\"Completed analysis errors: {errors}\")\nprint(\"9-STRATEGY ENGINE: SMC | Market Structure | Liquidity | Supply/Demand | Price Action | Fibonacci | Trend | RSI+MACD | Session\")\nprint(\"SIGNAL QUALIFICATION: 2-of-9 strategy consensus; fresh structural state; real >=2R; live bid/ask trigger; events are context only\")\nprint(\"STRATEGY ENGINE: SMC BASE + 8 CONFLUENCE MODULES; existing 2-of-9 confirmation remains, but SMC must be one of the confirming strategies\")\nprint(\"SNIPER: Sweep+MSS | Sweep+FVG | OB+Sweep | Breaker+MSS | FVG+Displacement | S/D+Sweep | CHoCH+FVG | S/R Sweep | PDH/PDL | Asian\")\nprint(\"SCALPING: EMA20/50 | VWAP | Bollinger | RSI Divergence | MACD | Stochastic | Breakout/Retest | Range Rejection | Momentum | Session Breakout\")\nprint(\"INTRADAY: HTF Bias/LTF | Structure Trend | Break/Retest | Trend+FVG | OB Continuation | S/D Continuation | Breakout/Continuation\")\nprint(\"CURRENCY: Strong/Weak | Strength+Structure | Strength+Sweep | Strength+Breakout | Strength+FVG\")\nprint(\"ADVANCED: Liquidity+MSS+FVG | Liquidity+OB+FVG | HTF Liquidity+LTF MSS | Session+Displacement | HTF Bias+Sweep+FVG+Momentum\")\nprint(\"RISK/CONTEXT: Economic Calendar + RSS News | Event Reaction | Yield Differential | Correlation | Macro\")\nprint(\"Timeframes: 1MO | 1D | 4H | 1H | 15M | 5M (all LSE-native; no synthetic timeframe)\")\nprint(\"Architecture: FAST/DEEP -> ADAPTIVE REGIME CLASSIFIER -> REGIME-WEIGHTED 9-STRATEGY CONSENSUS -> SMC/STRUCTURE/LIQUIDITY -> NEWS CONTEXT -> RETRACEMENT/NO-CHASE -> LIVE EXECUTION GATES\")\nprint(\"Entry categories: SNIPER | SCALPING | INTRADAY | CURRENCY_STRENGTH | ADVANCED_CONFLUENCE\")\nprint(\"Signal horizons: SCALP | INTRADAY | SWING/HOLD\")\nprint(\"Output states: WATCH | DEVELOPING | SETUP_READY | POTENTIAL_BUY | POTENTIAL_SELL | WAITING_FOR_TRIGGER | CONFIRMED_ENTRY | NO_ACTION\")\nprint(\"TELEGRAM OUTPUT: confirmed entries are separated from FAST WATCH; every signal is pair-scoped; BUY/SELL status uses visual markers; ꧁𖡹⃢JAMES𖣔⚠︎꧂ institutional branding enabled\")\nprint(\"Opportunity timing: IMMEDIATE_0_3M | VERY_NEAR_3_5M | NEAR_5_10M | NEAR_10_15M | WATCH_15_30M | WATCH_30_60M | LATER_60M_PLUS\")\nprint(\"SNIPER ADD-ON: optional 1-3 minute micro-trigger window; scanner remains SCALP/INTRADAY/SWING-HOLD capable and no trigger is forced\")\nprint(\"Scalp: 5M/15M execution opportunity; 2-of-9 strategy consensus + required context + real 2R/news gates | SNIPER SCALP when precision score and 5M execution conditions qualify\")\nprint(\"Intraday: 15M/1H execution opportunity; 2-of-9 strategy consensus + required context + real 2R/news gates | SNIPER INTRADAY when precision score and 15M/1H conditions qualify\")\nprint(\"Scalp: 2-of-9 strategy consensus with lower-TF agreement + real 2R/news gates\")\nprint(\"Swing/Hold: 4H/1H higher-timeframe opportunity; 2-of-9 strategy consensus + required context + real 2R/news gates\")\nprint(\"Event intelligence: context only — aligned/conflicting/high-impact events may adjust context/score but never act as a mandatory ALIGNED/EXECUTABLE gate; news never creates a trade alone\")\nprint(\"Top opportunities: strongest 5 from the complete 20-FX competition; developing setups may show genuine structural TP, but entry/SL/TP are confirmed only after all gates pass.\")\nprint(\"Unavailable intelligence: reported as unavailable and excluded from technical score\")\nprint(\"Automatic trading: DISABLED\")\nif runtime_limited or processed < len(top_opportunity_candidates):\n    print(\"⚠️ SCAN INCOMPLETE — one or more FX instruments could not complete the mandatory full-universe pass\")\nelse:\n    print(\"✅ SCAN COMPLETE — PROCESS EXITING CLEANLY\")\npayload = {\n    \"version\": \"v12.66.0\",\n    \"generated_at\": datetime.now(timezone.utc).isoformat(),\n    \"fx_top_5_global\": top_opportunity_results[:TOP_N],\n    \"fx_top_5_symbols\": [r.get(\"symbol\") for r in top_opportunity_results[:TOP_N]],\n    \"fx_competition_universe\": sorted(TOP_OPPORTUNITY_SYMBOLS),\n    \"entries\": entries[:TOP_N],\n    \"intraday_entries\": intraday_entries[:TOP_N],\n    \"scalp_entries\": scalp_entries[:TOP_N],\n    \"swing_entries\": swing_entries[:TOP_N],\n    \"adaptive_entries\": adaptive_entries[:TOP_N],\n    \"sniper_opportunities\": sniper_ranked[:TOP_N],\n    \"setup_waiting\": setup_waiting[:TOP_N],\n    \"potential\": [x[2] for x in potential_display[:TOP_N]],\n    \"waiting\": waiting[:TOP_N],\n    \"broader_potential_setups\": all_potential[:TOP_N],\n    \"waiting_blocker_setups\": waiting_pool[:TOP_N],\n    \"analyzed_currency_context\": [{\n        \"symbol\": r.get(\"symbol\"), \"side\": r.get(\"side\"), \"status\": r.get(\"status\"),\n        \"opportunity_class\": r.get(\"opportunity_class\"), \"opportunity_style\": r.get(\"opportunity_style\"),\n        \"execution_timeframe\": r.get(\"execution_timeframe\"), \"signal_timeframes\": r.get(\"signal_timeframes\", []),\n        \"trading_session\": r.get(\"trading_session\"), \"trading_session_quality\": r.get(\"trading_session_quality\")\n    } for r in sorted(results, key=lambda x: x.get(\"symbol\", \"\"))],\n    \"complete\": complete,\n    \"incomplete\": incomplete,\n    \"processed\": processed,\n    \"errors\": errors,\n    \"catalog_count\": len(catalog),\n    \"eligible_count\": len(instruments),\n    \"full_universe_analysis\": {\n        \"enabled\": True,\n        \"universe\": \"20_FX_PAIRS\",\n        \"eligible\": len(TOP_OPPORTUNITY_SYMBOLS),\n        \"queued_for_deep_analysis\": len(top_opportunity_candidates),\n        \"preselection_removed\": 0,\n        \"selection_policy\": \"NO_PREDICTIVE_FAST_FILTER\",\n        \"data_quality_gate_only\": True,\n    },\n    \"deep_analysis_count\": len(top_opportunity_candidates),\n    \"intelligence\": intel,\n    \"currency_strength\": currency_strength,\n    \"pair_replacement_engine\": replacement_engine,\n    \"critical_level_model\": \"prior period/day/week + recent swing + Fibonacci 38.2/50/61.8 + round level + volume-weighted typical-price proxy\",\n    \"rss_feeds_configured\": len(NEWS_URLS),\n    \"rss_items\": len(intel.get(\"news\", [])),\n    \"runtime_seconds\": time.monotonic() - SCAN_START,\n    \"api_governor\": {\n        \"request_count\": API_REQUEST_COUNT,\n        \"rate_limited_429_count\": API_RATE_LIMITED_COUNT,\n        \"quota_circuit_breaker_engaged\": API_QUOTA_BLOCKED,\n        \"final_min_interval_seconds\": API_MIN_INTERVAL_SECONDS,\n        \"cooldown_seconds\": API_429_COOLDOWN_SECONDS,\n        \"max_cooldown_seconds\": API_429_MAX_COOLDOWN_SECONDS,\n        \"policy\": \"global quota circuit breaker; first provider 429 stops further candle REST requests for this run\"\n    },\n    \"score_definitions\": {\n        \"strategy_score\": \"0-100 breadth/quality score for the 9-strategy consensus; score never confirms an entry by itself\",\n        \"monthly_alignment\": \"1MO is strategic context, not a permanent directional lock. Continuations normally align to 1MO; tactical reversals may override it only after the dynamic-direction engine confirms multi-timeframe evidence plus structural invalidation/flip. REVERSAL_WATCH never confirms an entry.\",\n        \"signal_timeframes\": \"Only operational LSE execution timeframes (4H/1H/15M/5M) count; 5M is the signal base; at least 2 of 9 strategies must be directionally confirmed on the chosen timeframe and required context\",\n        \"execution_quality_score\": \"0-100 execution-condition quality; not a win probability and never a standalone entry confirmation. Conditional/non-executable plans are capped below confirmed-entry quality.\",\n        \"score\": \"backward-compatible alias of strategy_score\",\n        \"status_rule\": \"CONFIRMED_ENTRY requires adaptive 9-strategy evidence, valid market state, structural SL/TP, >=2R at actual executable bid/ask, event/data/volatility/spread/chase gates and a fresh live trigger. Continuations require strategic/context alignment; reversals may override the prior 1MO/4H direction only when the dynamic-direction engine confirms the new side. Potential/watch states are never entries.\",\n        \"potential_rule\": \"POTENTIAL is reference-only. It is never an order, never a win probability, and must be revalidated at the next closed candle before entry.\",\n        \"preselection\": \"Disabled; all 20 FX pairs receive the same deep institutional pipeline before ranking.\",\n        \"institutional_execution_model\": \"Live LSE bid/ask ticks determine executable side, quote freshness, spread cost, trigger crossing, chase distance, micro-price stability, arrival-price implementation shortfall and a bounded execution score. This is top-of-book execution modelling, not full order-book/queue market-impact modelling because LSE Tick data exposes bid/ask/price/volume but not full depth.\",\n         \"execution_calibration\": {\n         \"source\": EXECUTION_CALIBRATION_SOURCE,\n         \"min_confirmed_score\": EXECUTION_SCORE_MIN,\n         \"high_quality_score\": EXECUTION_HIGH_QUALITY_SCORE\n     },\n        \"execution_side_rule\": \"BUY uses ASK for trigger/fill readiness; SELL uses BID; mid/last is never substituted for the executable side.\",\n    },\n    \"strategy_definitions\": [\"SMC\",\"Market Structure\",\"Liquidity\",\"Supply/Demand\",\"Price Action\",\"Fibonacci\",\"Trend\",\"RSI+MACD\",\"Session\"],\n    \"minimum_confirmed_strategies\": MIN_CONFIRMED_STRATEGIES,\n    \"risk_policy\": {\n        \"max_confirmed_entries_per_scan\": MAX_CONFIRMED_ENTRIES_PER_SCAN,\n        \"max_opposed_strategies_for_entry\": MAX_OPPOSED_STRATEGIES_FOR_ENTRY,\n        \"require_monthly_alignment\": REQUIRE_MONTHLY_ALIGNMENT,\n        \"require_4h_alignment\": REQUIRE_4H_ALIGNMENT,\n        \"dynamic_direction_active\": DYNAMIC_DIRECTION_ACTIVE,\n        \"reversal_min_htf_confirmations\": REVERSAL_MIN_HTF_CONFIRMATIONS,\n        \"reversal_min_execution_confirmations\": REVERSAL_MIN_EXECUTION_CONFIRMATIONS,\n        \"require_actual_executable_rr_recheck\": REQUIRE_EXECUTABLE_RR_RECHECK,\n        \"manual_mt5_rule\": \"The scanner does not place MT5 orders. Manual execution must copy the confirmed executable entry, SL and TP exactly; if any field differs or TP is blank, do not enter.\",\n    },\n    \"strategy_engine\": {\n         \"name\": \"9_STRATEGY_CONSENSUS\",\n         \"status\": STRATEGY_ENGINE_STATUS,\n         \"reason\": STRATEGY_ENGINE_REASON,\n         \"minimum_confirmed_strategies\": MIN_CONFIRMED_STRATEGIES,\n         \"required_for_confirmed_entry\": True\n    },\n}\noutput_path = os.getenv(\"SCANNER_JSON\", \"scan_results.json\")\nwith open(output_path, \"w\", encoding=\"utf-8\") as fh:\n    json.dump(payload, fh, ensure_ascii=False, default=str)\nprint(f\"JSON HANDOFF: {output_path}\")\nPY\n}\nif [ \"${SCAN_MODE}\" = \"UNTIL_POSITION\" ]; then\n  max_minutes=\"${UNTIL_POSITION_MAX_MINUTES:-55}\"\n  poll_seconds=\"${POLL_SECONDS:-30}\"\n  case \"$max_minutes\" in\n    ''|*[!0-9]*) max_minutes=55 ;;\n  esac\n  case \"$poll_seconds\" in\n    ''|*[!0-9]*) poll_seconds=30 ;;\n  esac\n  if [ \"$max_minutes\" -lt 1 ]; then max_minutes=1; fi\n  if [ \"$max_minutes\" -gt 55 ]; then max_minutes=55; fi\n  if [ \"$poll_seconds\" -lt 5 ]; then poll_seconds=5; fi\n  if [ \"$poll_seconds\" -gt 300 ]; then poll_seconds=300; fi\n  deadline=$(( $(date +%s) + max_minutes * 60 ))\n  cycle=0\n  echo \"🔎 UNTIL_POSITION mode enabled: full multi-timeframe rescans continue until a confirmed position is found or the safety limit expires.\"\n  while true; do\n    cycle=$((cycle + 1))\n    echo \"========== SCAN CYCLE ${cycle} ==========\"\n    scan_once\n    if python -c 'import json; d=json.load(open(\"scan_results.json\", encoding=\"utf-8\")); raise SystemExit(0 if any(str(x.get(\"status\",\"\")).upper() in (\"CONFIRMED_ENTRY\", \"CONFIRMED\") or str(x.get(\"status\",\"\")).upper().startswith(\"CONFIRMED_\") for x in d.get(\"entries\", [])) else 1)'; then\n      echo \"✅ CONFIRMED POSITION FOUND — stopping repeated scan cycles.\"\n      break\n    fi\n    if [ \"$(date +%s)\" -ge \"$deadline\" ]; then\n      echo \"⏱️ UNTIL_POSITION safety limit reached without a confirmed position.\"\n      break\n    fi\n    echo \"🟡 No confirmed position yet. Waiting ${poll_seconds}s before the next full scan.\"\n    sleep \"$poll_seconds\"\n  done\nelse\n  scan_once\n  python -c 'import json; json.load(open(\"scan_results.json\", encoding=\"utf-8\")); print(\"✅ SCAN REPORT JSON VALID — no-opportunity/WAIT states are valid outcomes.\")'\nfi\n"
    - name: Send final scanner report to Telegram
      if: ${{ success() && hashFiles('scan_results.json') != '' }}
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        TELEGRAM_CHANNEL_URL: ${{ vars.TELEGRAM_CHANNEL_URL || 'https://t.me/marketwitches' }}
        SCANNER_JSON: scan_results.json
      run: 'set -euo pipefail

        python telegram_notify.py

        '
    - name: Upload scanner results
      if: ${{ success() && hashFiles('scan_results.json') != '' }}
      uses: actions/upload-artifact@v6
      with:
        name: scanner-results
        path: scan_results.json
        if-no-files-found: error
    - name: Upload MT5 handoff
      if: ${{ success() && hashFiles('mt5_signal.json') != '' }}
      uses: actions/upload-artifact@v6
      with:
        name: mt5-signal
        path: mt5_signal.json
        if-no-files-found: error
    if: ${{ always() && needs.validate_workflow.result == 'success' }}
